<!--
============================================================
THIS FILE IS NOT PART OF THE ABN APPLICATION.
============================================================
PURPOSE: Session memory for Jacob + Claude (claude.ai chat).
Used so that when Jacob starts a new Claude chat, he pastes
the raw URL and Claude instantly knows where we are.

RAW URL (paste in new Claude chat):
https://raw.githubusercontent.com/abn-systems/abn-session-memory/main/JACOB_SESSION.md

THIS FILE HAS ZERO IMPACT ON:
- Backend code or tests
- Frontend or dashboard
- Deployments or CI/CD
- Any ABN system functionality

DO NOT modify this file as part of any feature or bug fix.
DO NOT mix this with ABN application memory or logic.

CLAUDE CODE RULES FOR THIS FILE:
1. After every completed batch: update this file with what was built
2. After every completed batch: update CHAT_LOG.md with decisions made
3. Then run: git add JACOB_SESSION.md CHAT_LOG.md
4. Then run: git commit -m "chore: update session memory after [batch name]"
5. Then run: git push
6. This triggers auto-sync to public mirror at abn-session-memory repo
7. Jacob uses the raw URL in new chats — never loses context

WHY THIS EXISTS:
Claude.ai chat has a file limit. When the chat gets too long,
Jacob opens a new chat. Without this file, all context is lost.
This file prevents that. It is the single source of truth for
what has been built, what prompts were given, and what is next.
============================================================
-->

# ABN — Session Memory

## ÅTERHÄMTNING EFTER /clear
1. cd C:\Users\Jacob\Downloads\abn
2. git log --oneline -5
3. git status
4. cat CLAUDE.md | head -40
5. cat JACOB_SESSION.md

ABN lever i: GitHub + denna disk + dessa markdown-filer. Aldrig i chatt-minnet.

## JUST NU
Status: Batch BACKEND-TENANT-2c-2 (tenant-isolation på proposal READ-endpoints) KLAR — PR open, väntar på Jacobs granskning. BACKEND ONLY (proposals.py). Stängde proposal-read-läckan (id-only loads → cross-tenant read = dataläcka). ÅTERANVÄNDE _load_tenant_proposal_or_404 (2a). Rewired 3 reads: GET /{id} (detalj), GET /{id}/report (report_refs från scoped proposal's egen output_summary → sammansatt ägarskap inneboende), och list (var caller-param: utan param returnerade ALLA tenants → nu ALLTID scopad till current.tenant_id med fail-closed-sentinel; totals/pending från den scopade queryn → ingen count-läcka). Varje read fick `current = Depends(require_auth)` (reads VIEWER+; require_role på approve/reject orörd). 404 cross-tenant (missing == cross-tenant: kropp = bara {"detail":…}), tenant-load FÖRST. Tog bort den nu döda id-only `_get_or_404` (föräldralös efter denna batch). Ny test_backend_tenant_2c_2.py (10). API-kompat: tenant_id-param kvar men ignorerad (API-CLEANUP). Shipped Tuari-3 Approval Center (getProposals()/getProposal(id)) funkar same-tenant. Full backend-svit 2099 passed (2089+10). SÄKRARE wording: 2a+2b+2c-1+2c-2 gör AGENT- + PROPOSAL-ytorna tenant-säkra; 2c-3 (report-reads) + TENANT-DISCOVERY-EXTRA återstår. INGEN reports.py/agent-reads/Tier-1/2/role/frontend/migration/schema/runtime-ändring.
Repo-sökväg: C:\Users\Jacob\Downloads\abn
Main-branch: 5b9ac4f — APP-1→2c + CI-BUDGET-1 + RAIL-1 + ROLE-UI-1 + BACKEND-TENANT-2a (#144) + 2b (#145) + 2c-1 (#146) merged. BACKEND-TENANT-2c-2 är PR-only ovanpå detta.
Senast: BACKEND-TENANT-2c-2 klar — proposals.py list/get/report rewired till current.tenant_id (404 cross-tenant; missing == cross-tenant); list var caller-param → nu alltid scopad + fail-closed-sentinel + totals/pending scopade; report_refs från scoped proposal's egen output_summary (sammansatt ägarskap inneboende); döda _get_or_404 borttagen. Ny test_backend_tenant_2c_2.py (10). Full backend-svit 2099 passed (2089+10). PR open, EJ mergad.
Nästa: BACKEND-TENANT-2c-3 (reports GET {id} + {id}/download — använd _load_tenant_run_or_404; sista 2c-pjäsen) · TENANT-DISCOVERY-EXTRA (övriga tenant-bärande route-familjer: roi.py/transparency/dna_phase/friday_report/mind/compliance/connectors/tenant_settings m.fl. — ~23 filer i route-inventory) · TENANT-HELPER-1 (konsolidera de 5 kopierade _load_tenant_*-loaders EFTER familjen är klar). SEDAN: API-CLEANUP · ROLE-UI-2 · BRANDING/LANDING-DISCOVERY · RAIL-2 · APP-3 · APP-6. Kvalitet & korrekthet #1.
VIKTIGT: SÄKRARE completion-formulering — 2a+2b+2c-1 (agent-reads)+2c-2 (proposal-reads) gör de AUDITERADE AGENT- + PROPOSAL-ytorna tenant-säkra; report-reads (2c-3) + övriga familjer (TENANT-DISCOVERY-EXTRA, ~23 filer) återstår. PÅSTÅ INTE att multi-tenant-server-tiern är fullt tenant-säker förrän 2c-3 + EXTRA är klara. API-CLEANUP-lista (ignorerade caller tenant_id-params): agents list/roi-summary/anomaly-trend + ai-act×3/instructions/intelligence/benchmark (2b) + instruct/generate-from-process (body, 2b) + proposals list (2c-2). Control-room honesty + ABN Operator Assistant (en delad, aldrig per-agent) = PERMANENTA CLAUDE.md-regler. Alla öppna PR hålls vid CLEAN — INTE auto-merge. DESIGN BEVARAD (v7).

## ABN V1 — AUGUST RELEASE PLAN (canon / kistan)
The compass for EVERY scope decision until launch (target: August 2026).
Standard unchanged: ship nothing poor; the moat is trust. August = focus, not
haste. Re-check this plan at each Fas boundary.

**THE V1 PROMISE (must be CODE-TRUE to ship honestly):** "ABN is a local,
verifiable, controlled AI workforce for business work: it observes approved
systems, maps processes, compiles agents that prove every value, and never acts
on risk without human approval. Data never leaves the node." V1 may be NARROW
(few connectors, few agent types, insight/report/draft + controlled low-risk
action) but every word above must be true. A narrow trustworthy V1 beats a broad
shaky one.

**🟢 MUST for V1 (cannot ship without — never cut):**
  1. Safety Spine **Fas 1 (Runtime)** — S2F1-1 status honesty ✅, S2F1-2 order
     guard ✅, S2F1-3 RunLock ✅, S2F1-4 retry+idempotency ✅, **S2F1-5a
     transactional outbox ⏳** = reliability foundation.
  2. Safety Spine **Fas 2 (Blueprint integrity)** MUST-subset — BlueprintQualityScore
     hard floors, signed BrainProfile/ToolBinding, ArtifactSet RCU pinning (the
     parts that make "compiles agents that prove every value" true; not every
     refinement).
  3. Safety Spine **Fas 3 (Fleet)** MUST-subset — AgentHealthScore hard floors +
     auto-pause, Safe Mode levels (the parts that make "controlled" true;
     Supervisor Reconciliation = BORDE).
  4. **Compliance as runtime** — EU AI Act risk classification + ComplianceGate
     (no activation beyond reporter without human-oversight defined + audit +
     data-minimization proof) + basic Legal Centre docs (MUST-subset, NOT the
     full Legal Intelligence Kernel).
  5. **No-Data PROVABLE** — NoPayloadProof (schema scan + runtime payload-policy
     + LLM-gateway redaction proof): the moat must be demonstrable, not asserted.
  6. **One agent family END-TO-END** (observe → graph → compile → OPERA-V →
     verify → report/draft → human approval → audit) on real-ish data (e.g.
     Finance Invoice Auditor or Logistics) — proves the whole chain.
  7. **Tuari (customer-control surface)** — Trust Card per agent, Run Inspector
     /"Why" view, approval UX, pause/autonomy controls. "Humans keep control"
     must be visible + usable.
  8. **OPS-TODOS (gate live operation — schedule, don't leave to last week):**
     Hetzner `alembic upgrade head`; Fortnox org-nr + API key (first live run);
     Stripe live 4 env-vars + webhook; Google OAuth verify hello@abnplatform.com;
     code-signing EV cert (~$300/yr) for the Windows installer; Tauri
     install/verify; release-sync stale landing download links.

**🟡 BORDE (strong, non-blocking — add if time):** more connectors, more agent
types, Supervisor Reconciliation, drift monitor, deeper Tuari polish, ABN Eval
Harness (adversarial suites), broader Failure Containment Map.

**🔴 AFTER V1 (deferred — NEVER pull forward):** Sentinel Prime (premium
security family; cross-customer = separate deep review vs No-Data; offensive
NEVER); Hidden Value Engine + 50-industry/200-agent demand map (engine
GENERATES agents, never hand-built); LMA/AIR-V (LLM minimization & routing);
full Legal Intelligence Kernel (auto-fetch FLAGS, human approves obligation
atoms, never autonomous legal interpretation); P2b-ii deep OPERA dry-run sim
harness; agent chat-panel; agents-in-daily-tools (Gmail/Teams/Slack);
customer-built agents; quantum (NIST PQC principle only).

**THE PATH (ordered, never jump):** S2F1-5a Outbox (closes Fas 1) → Fas 2
MUST-subset → Fas 3 MUST-subset → Compliance-runtime MUST → NoPayloadProof →
one agent family end-to-end → Tuari control surface → ops-todos (interleave,
don't defer) → V1 launch-readiness check → ship. Each step: Discovery before
big/behaviour-changing batches; one batch = one PR; hold at CLEAN; never
auto-merge; honest naming; Customer-Surface Sync Rule + Technical-Confidentiality
Rule (WHAT not HOW on public surfaces) apply throughout.

**TIMELINE:** June→August ≈ 2.5 months — feasible for a NARROW excellent V1 IF
scope holds to 🟢. Risk = scope creep, not time (pace proven: one batch = one PR,
~14 merged this session, zero regressions). Schedule risk = ops-todos + Fas 2/3
MUST-subset + the one end-to-end agent family — sequence those.

**DISCIPLINE:** 🟢 only until shippable; 🟡 if time; 🔴 never before launch.
Re-check at each Fas boundary: is 🟢 still the minimum true V1? Cut 🟡 ruthlessly
under pressure; never cut a 🟢; never pull a 🔴 forward.

## ABN ROADMAP v2 (authoritative — Jacob's nano-detail canon)
THREE GUIDING STARS (apply to every track): (1) **3 master gates** —
GraphAdmissibility, AgentNeed, BlueprintReadiness — ALL other scores feed
these three (anti score-chaos); (2) **hard floors > blended scores** (a junk
sub-score must veto, not average away); (3) **unbroken chain of proof**:
DNA→Graph→Genesis→Compiler→Simulation→Runtime→Verification→Audit→Tuari.
Build weights as MECHANISM, calibrate against real data (no false precision).
MND batches build nano-detail from canon; metaphors (Google-Translate =
ABN-WIR; coffee-machine = Autonomous-Engine; chip/robot-kit =
Component-Manifest) are quoted into the relevant track's prompt.

- **Spår 1 — motor-integritet:** [73 ✅ run() status honesty] [74 ✅ Critic+ESE
  on the live path] → **75 (this) Failure Taxonomy v1** → Build 2 (RAL
  attestation + cross-reference + Finding.attested write-back) → P2(a) hard
  conformance floor + VariantConformanceMap → P2(b) 5-level SimulationGate /
  SafeFailureRate.
- **Spår 2 — Safety Spine** (reads the Failure Taxonomy): PhaseContractValidator,
  RunStatusReducer (weakest-link), RuntimeStateMachine + TransitionGuard,
  RunLock, idempotency-key, Transactional Outbox, BlueprintQualityScore
  hard-floors, Signed BrainProfile / ToolBinding, ABN-RCU snapshots,
  AgentHealth (consumes auto_pause_weight), Safe Mode, Supervisor
  Reconciliation.
- **Spår 3 — DNA / Agent-OS:** A Observer Mode 0–6 / Event-Envelope / case-key /
  no-loss / Permission-Manifest; B ABN-WIR (translation core) / 6 graph levels /
  GraphAdmissibility / Work-Units / GenesisPackage; C Autonomous-Engine-as-
  compiler / BrainProfile + BrainRiskFit / ToolFit-floors / Verification-
  Compiler; D Trust-Kernel / Coordinator / Jurisdiction / File-Engine; E local
  Control-Plane SQLite / AgentHealth; F Evolution / Shadow / Kill-switch /
  Abstract-Feedback; G Tuari control room.
- **Spår 4:** internal brains (17-step loop), agents-in-daily-tools
  (Gmail/Teams/Slack), internal chat-panel, V1 launch → reshaped vision.

## TODO — Design-inspiration från Claude desktop-appen
Jacob: "Ta inspiration från Claude-appen — de har chat, kod, design, fungerar utan problem. ABN-appen ska vara så snabb och bra." Den nya v7 sage-designen är på plats men UX-flowet (chat-tab på AgentDetailPage, kod-blocken på /api, navigationen i sidofältet) kan slipas mot Claude-appens kvalitet i en framtida batch. Notering för senare — inte i scope för Batch 35.

Last updated: 2026-05-28 (after Batch 34)
Repo: https://github.com/abn-systems/ABN
Raw URL (public mirror — auto-synced from main):
https://raw.githubusercontent.com/abn-systems/abn-session-memory/main/JACOB_SESSION.md

## How Jacob uses this file
In any new Claude chat, paste this URL and say "read this":
https://raw.githubusercontent.com/abn-systems/abn-session-memory/main/JACOB_SESSION.md

## What is built (as of 2026-05-25)
- Backend: **1193 tests passing** + abn-security Go suite all-green (V1 baseline 939, then per-batch additions through Batch 22B; full per-batch accounting in the dated entries below), V1 feature-complete; TIER 3 EXECUTE_CHANGE now end-to-end functional (opt-in)
- **Production deploy live** (Batch 7): Hetzner CAX21 server `abn-core` at 46.225.165.62 (Frankfurt, Ubuntu 24.04, GDPR DPA signed). All four containers running under Docker Compose at `/opt/abn`. `https://api.abnplatform.com/health` returns `{"status":"ok","env":"production"}`. Vercel landing on `abnplatform.com` + `www.abnplatform.com` both Valid Configuration.
- Frontend: Tauri desktop dashboard live, Swedish UI, NSIS installer built (`ABN_1.0.0_x64-setup.exe`)
- Landing: 33 prerendered static pages live on Vercel (abn-nine.vercel.app), full dark theme
- Auth: Clerk + RBAC (NODE_ADMIN / AGENT_MANAGER / VIEWER / AUDITOR) + named-employee invite flow live
- AgentDetailPage.tsx: built and **fully hydrated** by the new Finding model + 3 endpoints (Batch 1)
- MarketplacePage.tsx: **wired to real DB** (Batch 7) — install / disable / re-enable via the existing `Connector` table; no fake client-side Set anymore
- Post-commit + post-merge git hooks: auto-create Jacob-authored deploy commits so Vercel Hobby plan deploys every push

## Batches done
- Sessions 1-5: Full backend, auth, landing, Tauri dashboard, legal centre, all solution / company pages, mega-menu nav, dark footer, /api, /transparency, /pricing, /changelog, /status — locked in as of session 5
- **Batch 1 (DONE 2026-05-24)** — Finding model + AgentDetailPage hydration
- **Batch 2 (DONE 2026-05-24)** — AgentSettings + TenantSettings + AgentActivityLog
- **Batch 3 (DONE 2026-05-24)** — Production CI/CD pipeline (3 required gates + 80 % coverage)
- **Batch 4 (DONE 2026-05-24)** — Multi-platform Tauri build workflow + download page redesign
- **Batch 5 (DONE 2026-05-24)** — ConnectorsPage live-feed (real-time Observer-Layer events)
- **Batch 6 (DONE 2026-05-24)** — Stripe payments (Subscription + InvoiceLog + billing API + BillingPage)
- **Batch 7 (DONE 2026-05-25)** — MarketplacePage wired to real backend (`initiateConnect` / `disableConnector` / `enableConnector`, real installed state from `Connector` table, OAuth redirect support). 8 new backend tests. 1018 total.
- **Batch 8 (DONE 2026-05-25)** — Migration deploy fix: replaced `Base.metadata.create_all()` startup with `alembic upgrade head` in `main.py` lifespan. No new tests (0 net — startup-path change only); 1018 still pass at 84.35% coverage. Manual one-time `alembic stamp head` required on the Hetzner production DB before the first restart — documented in CLAUDE.md `## Production DB — one-time stamp`.
- **Batch 9 (DONE 2026-05-25)** — Full automation chain wired: connector activate → DNA → Process Graph → Agent creation runs as a background task on every `POST /api/connectors/{type}/connect`. New `AgentScheduler` (daemon thread, 60 s tick, blueprint-cron driven, per-tenant lock). Plan limits enforced (starter=2 / professional=25 / enterprise=∞) via `enforce_agent_limit_or_raise`. Observer `CONNECTOR_RESOURCES` now derived from the YAML catalogue — all 18 catalogue types covered (delivery-only YAMLs yield empty resource lists). 18 new tests. Backend: **1036 passed**, 84.45% coverage.
- **Batch 10 (DONE 2026-05-25)** — DNS + public API endpoint wired: `landing/vercel.json` carries the apex→`www` 308 redirect; `landing/next.config.mjs` adds five global security headers (HSTS / X-Frame-Options / X-Content-Type-Options / Referrer-Policy / Permissions-Policy); `Settings.public_api_url` + surfaced on `GET /health` (`"public_url"` field); CORS allow-list extended with `https://www.abnplatform.com` + `https://abnplatform.com`. CLAUDE.md gains a `## DNS & Domains` section with the manual registrar + Vercel steps Jacob still has to perform. 1036 tests still green, 33 landing pages still build clean.
- **Infra event (2026-05-25)** — Production deploy: domain DNS + email + Hetzner host + Docker Compose + Nginx/TLS + live `api.abnplatform.com`. Tracked here for cross-session memory; not numbered as a feature batch.
- **Batch 22B (DONE 2026-05-26)** — TIER 3 EXECUTE_CHANGE second half: EXECUTOR archetype + AVM tier-3 rules + Fortnox write-back + RollbackRecord + OPERA pre-flight + rollback API. New `Tenant.tier3_enabled` column (default False — every tenant opted-out until DPA addendum signed) + `RollbackRecord` table, both in Alembic `b32c7e2ba5e5` (raw SQL `ALTER TABLE / CREATE INDEX IF NOT EXISTS` per Batch 12/16/17/18 lesson). `_derive_archetype_handbook` gained `tier3_enabled: bool = False` kwarg — returns EXECUTOR only when tenant opted-in + platform `enable_execute_change=True` + blueprint has an EXECUTE_CHANGE step. AVM step 5b downgrades (never rejects) EXECUTOR → PLANNER + tier 3 → 2 when: regulated industry forbids EXECUTE_CHANGE OR EXECUTOR without `execute_change=true` flag OR tier 3 unsigned OR EXECUTE_CHANGE step lacks `rollback_action`. Three Fortnox write capabilities (`fortnox_update_invoice_status` — `cancelled`/`reminded` only, never `paid`; `fortnox_create_reminder`; `fortnox_update_cost_center`) each do three-layer opt-in check (platform + per-system + per-tenant) → verify_blueprint_via_security_service (cross-process to abn-security) → GET prior state → PUT new state → persist RollbackRecord. New `KIND_EXECUTE` constant + 5th `execute` category in ACTION_MAP. OPERA `_tier3_preflight_or_raise` runs before Observe phase — tier 1/2 skip cost-free, tier 3 verifies signature via abn-security or raises `TierThreeVerificationError` (new `agent_engine/errors.py`). New API: `GET /api/admin/rollback-records` + `POST /api/admin/rollback/{id}` (NODE_ADMIN only, idempotent, audit-logged via ABNActivityLog). `_ROLLBACK_DISPATCH` map routes action_type → reverse Nango call. New env vars `ENABLE_EXECUTE_CHANGE` + `FORTNOX_WRITE_ENABLED` (canonical names; both default False). 13 new tests in `test_tier3_executor.py` + 1 test renamed (`test_action_map_has_all_four_categories` → `all_five_categories`). Backend: **1193 passed** (1180 + 13). Go: 7/7 packages green. Landing: 33/33. **Architectural decisions:** (a) Two tier-3 switches (platform `enable_execute_change` + per-tenant `tier3_enabled`) — DPA addendum is platform-level legal opt-in; tenant flag is operational opt-in; both required (defence-in-depth); (b) AVM ALWAYS downgrades, never rejects — keeps human-in-the-loop pattern active even on misconfigured blueprints (rule #6); (c) `paid` status deliberately NOT in allowlist — financial-record changes need auditor sign-off, V1 stays at the safe boundary; (d) signature verified cross-process TWICE per tier-3 step (Mission Layer pre-flight + per-capability re-verify) — Mission Layer bug can't bypass the gate. **What's next (Batch 22C):** per-tenant DPA addendum PDF, `Tenant.tier3_dpa_signed_at` column, "Aktivera Tier 3" panel on AdminPage with consent capture, store `connection_id` on RollbackRecord at write-time.
- **Batch 22A (DONE 2026-05-26)** — TIER 3 foundation: abn-security compiled + cross-language blueprint signatures. The Go service that's been in the repo unchecked since handbook ch. 11 was first scaffolded is now **compiled, vetted, and tested for the first time** — under `golang:1.22-alpine` via local Docker, matching what the CI `security-go` job has always run. New Go package `abnsecurity/internal/blueprint` (stdlib-only, 75% coverage) with `BlueprintVerifier` (HMAC-SHA256 over canonical JSON + tier-3 flag enforcement + audit logging via the existing `audit.Logger`). New HTTP endpoint `POST /verify-blueprint` on abn-security; request `{"blueprint": "<base64 JSON>"}`, response `VerifyResult{valid, reason, agent_id, tenant_id, tier}`, statuses 200/400/503 (503 when `ABN_NODE_SIGNING_KEY` empty — fail-closed). New Python module `backend/trust/blueprint_signer.py` — single source of truth for the canonical byte stream (`json.dumps(bp, sort_keys=True, separators=(",", ":"))`). The legacy `blueprint_generator.sign_blueprint` and `verify_blueprint` are now thin wrappers that delegate to it — existing import paths backward-compatible, but the canonical form (and the default key, now `node_signing_key`) is unified. New `verify_blueprint_via_security_service` helper for the cross-process pre-flight (fail-closed on any error). Settings + `.env.example` get `NODE_SIGNING_KEY` and `ABN_SECURITY_SERVICE_URL` entries (the legacy `BLUEPRINT_SIGNING_KEY` stays for backward compat). Generator emits an explicit warning when `node_signing_key` is unset or set to the placeholder default. Closes the **abn-security-not-compiled known-issue** from CLAUDE.md (open since handbook ch. 11). 9 new Go tests + 9 new Python tests (incl. an explicit Go-↔-Python interop assertion: `test_canonical_form_matches_go_reference` computes the expected signature byte-by-byte and compares — drift detector). Backend: **1180 passed** (1171 + 9). Go: 7/7 packages pass. Landing 33/33. **Architect deviations from spec, all rule-driven:** (a) New package `abnsecurity/internal/blueprint` (module is `abnsecurity`, not the spec's `github.com/abn-systems/abn-security`); (b) Reused `ABN_NODE_SIGNING_KEY` env var instead of spec's new `ABN_SIGNING_KEY` (rule #2 — one env var per secret); (c) Canonical signer module is the new SSoT — legacy `sign_blueprint`/`verify_blueprint` migrated to delegate (rule #2); (d) `WithBlueprintVerifier` builder method on `api.Server` so the constructor signature stays backward-compatible. **What's next (Batch 22B):** `EXECUTOR` archetype, AVM tier-3 rules, `Fortnox.create_invoice` write capability, OPERA Mission Layer pre-flight verify, write-back rollback semantics.
- **Batch 21 (DONE 2026-05-26)** — Wire Proposal creation into OPERA runtime. New `backend/agent_runtime/proposals_persistence.py` with `save_proposal_from_run(db, *, agent_id, tenant_id, run_id, blueprint, output, impact_eur, confidence) -> Optional[int]`. Mirrors `findings_persistence.save_findings_from_output` shape exactly (rule #2): kwargs-only, idempotent per `(run_id, tenant_id)`, fail-silent at every step. The helper calls `evaluate_commitment` internally (avoids threading `commitment_decision` through `OPERARunResult` — keeps the runner unaware of the gate). Only writes when the gate's decision is `save_proposal` (tier 2 + confidence ≥ 0.50); tier-1 + below-floor are no-ops. Derives `action_type` from `blueprint.steps[]` via `_PROPOSE_ACTION_MAP` (`propose.schedule_change → "schedule_change"`, `propose.cost_reduction → "threshold_adjustment"`, `propose.draft_email → "report_delivery"`, etc); unknown propose-class actions default to `"agent_pause"` (safest fallback — proposal still surfaces, operator pauses agent for review). Derives `target_system` from `blueprint.delivery` / `connectors` / `active_connections` in that fallback order. `impact_summary` is aggregate-only ("3 fynd · potentiell besparing EUR 18 600"). Wired in `OPERARunner._save_run_record` between the Agent Memory writes (Batch 17) and the Batch-18 notification dispatch block — the existing Batch-18 query-and-fan picks up the newly-created rows automatically; **deliberately did NOT add a redundant `deliver_proposal_notification` call** (rule #1, the spec's TASK 2 second half would have duplicated Batch 18). **Deliberately skipped TASK 3** (draft_email wiring from commitment_gate) — the gate doesn't emit `draft_email`; Batch 20's capability is invoked directly via the `propose.draft_email` ACTION_MAP entry, separate concern. **Deliberately skipped TASK 4 OPERARunResult mutation** — helper computes the decision internally; threading a dataclass field gains nothing. 10 new tests in `tests/test_proposal_wiring.py` covering helper correctness (tier-2 creates row, agent_id/tenant_id/status/impact correct), resilience (fail-silent on broken DB, tier-1 no-op, idempotent return), and end-to-end (Batch-18 query finds Batch-21 row, `GET /api/proposals` returns the new row with `status="pending"`). Resolves the Batch-18 "no production OPERA path creates Proposal rows" gap flagged in CLAUDE.md. Backend: **1171 passed** (1161 + 10), no regressions. Landing 33/33. Frontend typecheck clean (no frontend changes).
- **Batch 20 (DONE 2026-05-26)** — Email capability (read inbox → draft → human approves → never auto-send). New `EmailDraft` model + Alembic revision `05da8d0f9d26` with `to_fingerprints` / `cc_fingerprints` (SHA-256 lists, never raw addresses) + `subject_template` / `body_template` / `context_summary` (templates validated by `_validate_template_no_raw_values` — rejects 4+ digit runs and literal `@domain` strings). Extended `gmail.yaml` + `outlook.yaml` with `direction: both` + `resources[]` declaring the messages endpoint, 5-min poll, full block list for subject/body/raw addresses. New normalisers `observer/normalisers/gmail_messages.py` + `outlook_messages.py` — only `id` / `thread_id` / `event_time` / `label_ids` / `has_attachments` / `from_domain` survive; PII Guardian runs as defensive second pass. New `backend/agent_engine/capabilities/email.py` with three functions: `read_inbox` (filters pre-normalised events by from_domain / labels / attachments), `draft_email` (persists pending EmailDraft, fingerprints recipients, validates templates), `send_approved_draft` (refuses to run unless `status == "approved"` — defence-in-depth on top of API RBAC). Registered in `CAPABILITY_REGISTRY` + ACTION_MAP gets `fetch_data.gmail`, `fetch_data.outlook`, `propose.draft_email`, `report.send_approved_email`. New `backend/api/routes/email_drafts.py` — `GET /api/agents/{id}/drafts` + `GET /api/drafts` + `POST /api/drafts/{id}/approve` + `POST /api/drafts/{id}/reject`. NODE_ADMIN | AGENT_MANAGER. Approve handler cross-checks supplied cleartext recipients against stored fingerprints; mismatch refuses send. Writes `ApprovalRecord(proposal_id="draft:{id}")` — reuses the existing Proposal audit table (rule #2). API never returns `to_fingerprints` / `cc_fingerprints` — only `recipient_count` / `cc_count`. New frontend `DraftsPage.tsx` at `/drafts` route (sidebar "Utkast"), two sections (pending with ✅/❌ + approve/reject modals, resolved greyed-out), 30s TanStack Query refetch, Swedish copy. 13 new tests covering normaliser blocking + domain-only extraction (4), draft creation idempotency + fingerprinting + raw-value rejection (3), send guard refuses non-approved (2), API privacy contract + decisions + ApprovalRecord writes (4). Fixed one pre-existing test assertion in `test_delivery.py` that asserted `gmail.direction == "outbound"` (now `both` since Batch 20 extended it). Backend: **1161 passed** (1148 + 13), frontend 60 tests + Vite build OK, landing 33/33.
- **Batch 19 (DONE 2026-05-26)** — Admin Dashboard + ABN Node landing copy. New `backend/api/routes/admin.py` with three endpoints — `GET /api/admin/overview` (aggregate dashboard: tenant + plan, agent counts, 7-day runs/findings/impact_eur from ROILedger, pending proposals, users + node-admin cap, connector health, scheduler statuses, DB + LLM-gateway health, subscription), `GET /api/admin/activity-log` (filterable abn_activity_log with activity_type + days + limit query params), `GET /api/admin/schedulers` (live status() from all three scheduler singletons). All three guard via the existing `require_role(Role.NODE_ADMIN)` dependency. **Surface A only — never cross-tenant.** Surface B (fleet admin) audited + rejected: would invert the No-Data Guarantee; Stripe Dashboard + GitHub Releases + external uptime cover ABN-internal needs without that. New `frontend/src/pages/AdminPage.tsx` at `/admin` route, four sections (Overview tiles, Systemstatus, Användare, Aktivitetslogg) mirroring BillingPage.tsx pattern (TanStack Query 30s refetch, same Card/StatusBadge/Table primitives, Swedish copy). New "Admin" sidebar link with shield icon. Landing copy: `"Ladda ner ABN"` → `"Ladda ner ABN Node"` headline + page metadata; new "Vad är ABN Node?" explainer block above the download accordion (two short Swedish paragraphs about per-customer isolated Nodes). 9 new tests in `tests/test_admin_routes.py` covering auth (4: requires_node_admin × 2, non_admin_gets_403, unauthenticated), shape (1: full payload contract), correctness (3: agent counts, 7-day window filtering, proposal pending), schedulers (1). Reuses `require_role` / `MAX_NODE_ADMINS_PER_ORG` from auth.rbac (rule #1 — extend, never duplicate the RBAC machinery). Backend: **1148 passed** (1139 + 9), frontend: 60 tests + Vite build OK, landing: 33/33 static pages.
- **Batch 18 (DONE 2026-05-26)** — Teams/Slack notifications + approval buttons. Extended `delivery/slack_deliverer.py` with `deliver_proposal` (Block Kit `blocks[]` with header + fields + actions block; two `button` elements styled primary/danger; `value` carries `"<approve|reject>:<proposal_id>:<dispatch_token>"`). Extended `delivery/teams_deliverer.py` with `deliver_proposal` (Adaptive Card v1.4 with TextBlock + FactSet + two `Action.Submit` actions). New `delivery.router.compute_dispatch_token` (HMAC-SHA256) + `verify_dispatch_token` (constant-time) + `DeliveryRouter.deliver_proposal_notification` (Slack first, Teams fallback, refuses to dispatch without `NOTIFICATION_SIGNATURE_SECRET` — fail-closed). New `NotificationDispatch` table (Alembic `4b0b88e1b9f7`) correlates `message_id → proposal_id` with status lifecycle. Two new routes: `POST /api/slack/interactions` (HMAC `v0:{ts}:{raw}` signature verify against `SLACK_SIGNING_SECRET`, 5-min replay window, always 200 OK) + `POST /api/teams/interactions` (V1 bearer-token check against `TEAMS_VALIDATION_TOKEN`, JWT-via-JWKS as documented V2 path). Both call the existing `proposals._record_decision` (rule #2 — single source of truth for the approval contract — never re-implemented). OPERA `_save_run_record` fires `deliver_proposal_notification` for any pending Proposal rows created during the run (fail-silent). Three new Settings: `slack_signing_secret`, `teams_validation_token`, `notification_signature_secret` — all default empty so a fresh install never accepts unverified interactions. 14 new tests in `tests/test_slack_teams_notifications.py` covering Block Kit / Adaptive Card structure, HMAC token, dispatch persistence, signature verify, replay-attack rejection, approve+reject flows, idempotent double-approve, always-200-OK, and the Teams approve flow. Reuses Stripe webhook pattern (signature-verify-then-act + always-200), existing `BaseDeliverer._proxy` (Nango plumbing), `ApprovalRecord` model (immutable audit). Backend: **1139 passed** (1125 + 14), landing 33/33.
- **Batch 17 (DONE 2026-05-26)** — Agent Memory (three layers). New `backend/agent_memory/` package — `short_term.py` (7-day TTL, reinforcement counter, no_raw_values_check policy), `long_term.py` (promotion at >=3 reinforcements, decay 0.8 at 6 mo inactive, never deletes). Extended `agent_evolution/evolution.py` with `persist_state` + `load_state` (TTL-cached read-through). Three new tables (`agent_evolution_state`, `agent_short_term_memory`, `agent_long_term_memory`) in one Alembic revision `7ab8394c998a` — raw SQL `CREATE INDEX IF NOT EXISTS` per Batch 16 lesson. OPERA Plan phase now loads memory via `_load_agent_memory` (fail-silent, safe-empty defaults). `_save_run_record` writes short-term for findings with conf ≥ 0.7, persists evolution state, runs long-term promotion — all fail-silent. New API routes `GET /api/agents/{id}/memory` + `GET /api/agents/{id}/evolution` — aggregate metadata only, `fact_key` and `fact_payload` never exposed via API even though they're No-Data-safe (stable correlation handle = metadata leakage). 14 new tests in `tests/test_agent_memory.py`. Reuses `trust/fingerprint.py` (memory keys), existing `AgentEvolutionEngine.compute_state` (unchanged, persistence added in place), `findings` table (source of reinforcements). Backend: **1125 passed** (1111 + 14), landing 33/33.
- **Batch 16 (DONE 2026-05-26)** — Friday Report viral-loop e-mail. New `backend/friday_report/` package (generator, email_builder, sender, scheduler). Aggregates ROILedger + AgentRun + Agent rows for the closed Sat→Fri week into `FridayReportData` (EUR saved, hours saved, agents active, runs completed, top-5 agents). HTML+plaintext e-mail with ABN purple `#5A3FC0` header band + gold `#F2C94C` money accent, Swedish copy, deterministic render. SendGrid sender with API-key auth (skips Nango — Friday Report aggregates ARE the No-Data-safe figures, so SendGridDeliverer's number-strip would be wrong here). Scheduler is a daemon thread mirroring `AgentScheduler` — cron `"0 7 * * 5"` Europe/Stockholm with ±5 min grace window, three-layer idempotency (cron + already_sent_this_week guard + `(tenant_id, week_ending)` unique index). New `FridayReportLog` table — Alembic revision `e0dde7bb974c`. API: `GET /api/friday-report/preview` (read-only, accepts optional `report_date` query), `POST /api/friday-report/send` (manual trigger, dry_run support), `GET /api/friday-report/history`. Wired into `main.py` lifespan after `AgentScheduler`. 12 new tests in `tests/test_friday_report.py` covering aggregation correctness, deterministic e-mail render, idempotent sends, dry-run, failure capture, scheduler already-sent guard, and the 3 API routes. **Bonus hotfix:** discovered + fixed the `8f8acda9625d_align_schema_drift` migration — `postgresql_if_not_exists=True` is NOT honoured on SQLAlchemy 2.0 `Index()` objects (only on `Table.create()`), so the prior Batch 12 migration would have crashed on Hetzner. Rewrote all 7 `op.create_index(..., postgresql_if_not_exists=True)` calls to raw SQL `op.execute("CREATE INDEX IF NOT EXISTS ...")`. Backend: **1111 passed** (1099 + 12).

## Batch 1 — Finding model + API endpoints (DONE 2026-05-24)
**Prompt given:** Add Finding model, Alembic migration, wire into OPERA, GET /api/agents/{id}/findings, /trend, /runs/{run_id}/verification, surface tools_used + insight_layer on GET /api/agents/{id}. 15+ tests. All 939 existing must pass.

**Result:**
- New `findings` table in `backend/database/models.py` (id UUID, run_id, agent_id, tenant_id, title, description, severity, impact_eur, delta_pct, attested, source_system, field_name, agent_value, source_value, created_at).
- Alembic revision `c4f1ad9e8a72_add_findings_table.py` (depends on `88e0d47ce0ae`).
- Single source of truth `backend/agent_runtime/findings_persistence.py::save_findings_from_output` — idempotent per `run_id`, called from both `OPERARunner._save_run_record` and AAEA `executor.execute` post-ESE so the same helper covers both paths.
- New endpoints in `backend/api/routes/agents.py`:
  - `GET /api/agents/{id}/findings?run_id=&since=&limit=` — latest run by default, optional run pin / date filter
  - `GET /api/agents/{id}/trend?months=N` — per-month aggregation (impact_eur sum, findings count, avg confidence, runs count)
  - `GET /api/agents/{id}/runs/{run_id}/verification` — RAL field-level attestation from `abn_attestations`
- `GET /api/agents/{id}` extended with `tools_used: list[str]` (from blueprint) and `insight_layer: dict | None` (AGP — stored value wins, derived via `select_insight_layer` otherwise).
- 23 new tests in `backend/tests/test_findings_api.py` covering helper idempotency, severity normalisation, string-numeric coercion, OPERA wire-up, all three endpoints' happy paths + edge cases + 4xx validation, get_agent extensions.
- Full backend suite: **962 passed**, 0 failed. No regressions.
- CLAUDE.md "Known issues" entry for the missing `findings` table flipped open → resolved.

## Batch 2 — Customer settings + per-agent activity log (DONE 2026-05-24)
**Prompt given:** Add AgentSettings, TenantSettings and AgentActivityLog models + Alembic migration; wire AgentActivityLog into OPERA after each run; new endpoints GET/PUT /api/agents/{id}/settings (must NOT regenerate the signed blueprint), GET /api/agents/{id}/activity, GET/PUT /api/tenant/settings; register new router in main.py; 15+ tests; all 962 baseline tests still green.

**Result:**
- Three new tables in `backend/database/models.py`:
  - `agent_settings` — one row per agent, override layer on top of the signed blueprint (schedule, enabled, threshold_pct, output_formats, delivery_emails, notify_on, language, updated_at, updated_by). Override semantics: AgentSettings wins when present, blueprint default otherwise.
  - `tenant_settings` — one row per tenant (language, timezone, currency, date_format, logo_url, primary_color, notification_emails, slack_webhook, updated_at). Separate from `Tenant.policy` (Blueprint Generator governance) and `Tenant.delivery_config` (Type-2 channel routing).
  - `agent_activity_log` — append-only per-agent event stream (run_completed, setting_changed, paused, resumed). Indexed on (agent_id, occurred_at) for fast "last N rows".
- Alembic revision `d7e2f9b1c3a4_add_settings_and_activity_log.py` (depends on `c4f1ad9e8a72`).
- New single source of truth `backend/agent_runtime/activity_log.py::log_run_completed` + `log_setting_changed`. Idempotent per (agent_id, run_id, event_type) — second call updates the row, never duplicates.
- `OPERARunner._save_run_record` now calls `log_run_completed` so every OPERA run automatically appends a "run_completed" activity row alongside the AgentRun + Finding persistence (one place, three side effects, no duplication).
- New endpoints in `backend/api/routes/agents.py`:
  - `GET /api/agents/{id}/settings` — returns AgentSettings row OR blueprint defaults (`is_override: false` when defaulting).
  - `PUT /api/agents/{id}/settings` — upsert, validates enum fields (notify_on, output_formats) + threshold_pct range, stamps `updated_at` + `updated_by`, **does NOT regenerate the blueprint** (signature stays unchanged — asserted in test), writes a `setting_changed` activity row.
  - `GET /api/agents/{id}/activity?limit=N` — newest first, default 30, max 200.
- New file `backend/api/routes/tenant_settings.py` with `GET /api/tenant/settings?tenant_id=…` and `PUT /api/tenant/settings?tenant_id=…`. Registered in `main.py`.
- 20 new tests in `backend/tests/test_settings_and_activity_api.py`. Full backend suite: **982 passed**.

## Batch 3 — Production CI/CD pipeline (DONE 2026-05-24)
**Prompt given:** Replace `.github/workflows/ci.yml` with a production-grade pipeline. Required checks per branch protection: `Backend — 982 tests`, `Frontend — build check`, `Landing — build check`. Backend job runs full pytest with coverage gated at `--cov-fail-under=80`. Target build time under 5 minutes. Do not break `sync-session-memory.yml`. All 982 tests must still pass locally before push.

**Result:**
- Verified local coverage = **84 %** before committing → coverage gate is realistic.
- Rewrote `.github/workflows/ci.yml`:
  - **Three required jobs** (referenced by branch protection):
    - `Backend — 982 tests` — Python 3.12, ruff + mypy (best-effort, `|| true`), pytest with `--cov-fail-under=80`, uploads htmlcov artifact.
    - `Frontend — build check` — Node 20, `npm ci && npm run build` for the Tauri dashboard.
    - `Landing — build check` — Node 20, `npm ci && npm run build` for the Next.js public site.
  - **`all-green` aggregator** depending on the three above — a single status check the branch-protection rule can reference.
  - **Three advisory jobs preserved** from the prior pipeline (no duplication; they cover services not in the new spec): `abn-llm-gateway — test`, `abn-security — vet, build & test`, `Build & push images`. They run alongside but are not required for merge.
- Updated `docker` job's `needs:` array to reference the new job names (`backend-tests`, `frontend-build`) instead of the deleted `backend` / `dashboard` job names.
- `sync-session-memory.yml` untouched.
- Added a `## Branch Protection (set manually in GitHub)` section to CLAUDE.md under `## Session Management`, with the exact required-check names that match the new job names.
- Full backend suite locally: **982 passed**, coverage 84 %.

## Batch 4 — Multi-platform Tauri build + download page redesign (DONE 2026-05-24)
**Prompt given:** Update `frontend/src-tauri/tauri.conf.json` bundle targets from `["nsis"]` → `["nsis", "msi", "dmg", "deb", "appimage"]`. Create `.github/workflows/build-release.yml` with a 7-cell matrix (Windows x64/ARM64, macOS ARM64/x64/Universal, Linux x64/ARM64) using `tauri-apps/tauri-action@v0`, triggered by `release: created` and `workflow_dispatch`. Rewrite `landing/app/company/download/page.tsx` to a three-column macOS/Windows/Linux layout with per-variant download icons + collapsible older-releases accordion. Don't touch `ci.yml`, `sync-session-memory.yml`, backend, or tests.

**Result:**
- `tauri.conf.json` bundle targets extended to 5 formats; Tauri auto-selects per OS at build time.
- New workflow `.github/workflows/build-release.yml`:
  - 7-cell matrix across `windows-latest`, `macos-latest`, `ubuntu-22.04`.
  - **Bug fix vs the spec:** the spec passed `--target X` (a flag string) into `dtolnay/rust-toolchain`'s `targets:` input — which expects raw target triples. Split the matrix into `target` (for rust-toolchain) and `args` (for tauri-action) so every cell actually runs.
  - macOS Universal cell installs BOTH `x86_64-apple-darwin` and `aarch64-apple-darwin` so Tauri's `--target universal-apple-darwin` lipo-merge works.
  - All Apple signing + Tauri-updater secrets wired via env vars; absent secrets ⇒ unsigned build (Tauri behaviour).
  - `ci.yml` and `sync-session-memory.yml` left untouched (verified with `git diff --stat`).
- Download page rewritten:
  - **Split into two files** (server `page.tsx` + client `DownloadView.tsx`) so `export const metadata` (server-only) and `'use client'` (needed for `useState`) can coexist. The spec collapsed both into one file which Next.js App Router rejects.
  - Three-column grid per release block: macOS (3 variants), Windows (4), Linux (6).
  - Each variant: live download icon (only when `coming: false` AND `file: not null`) → `${RELEASE_BASE}/download/v${version}/${file}`, else "Snart" placeholder.
  - "View release notes →" per release (one English phrase; everything else Swedish).
  - Older releases collapsed inside `<button>`-driven `useState`-tracked accordions; `OLDER_RELEASES: Release[] = []` ready for v1.1+ entries.
  - **Restored the `<a` opening tags** that got eaten in the spec's markdown paste — otherwise the file wouldn't parse.
- `cd landing && npm run build` → ✓ 33 static pages, zero errors / warnings.

## Batch 5 — ConnectorsPage live-feed (DONE 2026-05-24)
**Prompt given:** Add a Live-flöde panel to ConnectorsPage that polls the last 10 Observer-Layer events every 30 s. Build only what's missing — don't rewrite ConnectorsPage.tsx or MarketplacePage.tsx. New backend endpoint `GET /api/connectors/live-feed?tenant_id=&limit=`. New frontend helpers `getLiveFeed` (client.ts) and `formatRelative` (format.ts). 5+ tests. All 982 baseline tests still green.

**Result:**
- Backend (`backend/api/routes/connectors.py`): new `GET /api/connectors/live-feed` declared BEFORE the int-typed `GET /{connector_id}` so the literal-path match wins (regression test included). Returns `LiveFeedEvent[]` — `id, domain, event_type, case_key, source_system, created_at`. Tenant-scoped; `limit` capped at 50 server-side. Reads existing `Event` rows that already passed Trust-Layer scrubbing — no further redaction needed.
- Frontend `client.ts`: appended `LiveFeedEvent` interface + `getLiveFeed(tenantId, limit=10)` using the existing axios `api` wrapper (not raw `fetch` — matches the other 20+ helpers in the file). Failure semantics: returns `[]` instead of throwing so a Node with zero events shows an empty panel, not a red error banner.
- Frontend `lib/format.ts`: added `formatRelative(iso)` — relative time ("Xs sedan", "X min sedan", "X tim sedan") with absolute-date fallback for >24 h. Pure, dependency-free.
- Frontend `pages/ConnectorsPage.tsx`: extended imports + added `<LiveFeed tenantId={tenantId} />` invocation directly before the closing `</div>` of the existing page body. New `LiveFeed` component is a sibling function in the same file — the existing `ConnectorsPage` function body is unchanged (rule "do not rewrite"). Renders domain-colour-coded chips (Bokföring/Schema/Logistik/Dokument), `event_type · case_key`, relative timestamp + the No-Data footer line.
- 8 new backend tests in `tests/test_live_feed_api.py`: empty list, newest-first ordering, default-limit 10, explicit-limit, 50-row cap, strict tenant scoping, literal-path-vs-int-param regression check, response shape.
- Full backend suite: **990 passed** (982 + 8). Frontend: typecheck clean, **60 tests green**, Vite build green.

## Batch 6 — Stripe payments (DONE 2026-05-24)
**Prompt given:** Add Stripe billing — `Subscription` + `InvoiceLog` models, Alembic migration, billing API (`/checkout-session`, `/portal-session`, `/subscription`, `/webhook`), BillingPage on the dashboard, wire route + nav, `stripe==8.7.0` in requirements, 15+ tests. Webhook signature ALWAYS verified (fail-closed). All 990 baseline tests still green.

**Result:**
- Five spec bugs fixed during the build (Engineering rule #6, fail-closed > spec-literal):
  1. `from core.database import get_db` → `from database.session import get_db` (the spec path doesn't exist).
  2. `from core.logging import get_logger` → stdlib `logging.getLogger` (no such module).
  3. `STRIPE_*` UPPER_CASE field names → lowercase to match existing Settings convention (`case_sensitive=False` handles UPPER_CASE env vars on the `.env` side).
  4. `stripe.error.SignatureVerificationError` → `stripe.SignatureVerificationError` (the alias namespace was removed in stripe-python 8.x).
  5. `invoice.paid` tenant resolution stored the Stripe customer-id in the `tenant_id` column. Fixed: now looks up the Subscription by `stripe_customer_id` to map back to ABN's tenant_id.
- New models: `Subscription` (one row per tenant — plan, status, stripe ids, period-end, cancel flag) and `InvoiceLog` (append-only receipts, idempotent on `stripe_invoice_id`). Both in `backend/database/models.py`. No card data, no bank data, no PII — Stripe holds the payment instrument.
- Alembic revision `e5b8c1f3d29a_add_subscriptions_and_invoice_log.py` (depends on `d7e2f9b1c3a4`).
- New `backend/api/routes/billing.py`:
  - `POST /api/billing/checkout-session` — creates Stripe Checkout, returns `checkout_url`. 503 when Stripe unconfigured (fail-closed safety on fresh installs).
  - `POST /api/billing/portal-session` — Stripe Customer Portal; 404 when no Subscription.
  - `GET  /api/billing/subscription` — returns row state, or starter/inactive defaults for new tenants.
  - `POST /api/billing/webhook` — signature verification FIRST (fail-closed); handles `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`. Idempotent across all four. Unknown events return 200 so Stripe doesn't retry indefinitely.
- `core/config.py` extended with `stripe_secret_key`, `stripe_webhook_secret`, `stripe_professional_price_id`, `stripe_starter_price_id`. All empty by default → billing routes 503 until real values supplied.
- `backend/.env.example` extended with the four UPPER_CASE env vars.
- `backend/requirements.txt` extended with `stripe==8.7.0`.
- `main.py` registers the new router alongside the others.
- New frontend `BillingPage.tsx` on the `/billing` route — two plan cards (Starter free / Professional €299/mo), "Uppgradera till Professional" → Stripe Checkout redirect, "Hantera prenumeration" → Stripe Customer Portal redirect, period-end notice + cancel-at-period-end orange flag.
- `frontend/src/api/client.ts` extended with `SubscriptionStatus` interface + `getSubscription` / `createCheckoutSession` / `createPortalSession` helpers (paths WITHOUT `/api` prefix — the axios baseURL already includes it; spec had double prefix bug).
- `App.tsx` routes `/billing` → `<BillingPage />` (additive).
- `Layout.tsx` nav extended with "Fakturering" entry + new IconBilling SVG (additive).
- 20 new tests in `backend/tests/test_billing_api.py` — 503 fail-closed gates, checkout-session happy path + 422 unknown plan + 404 unknown tenant + starter-price-id selection, portal-session 404 + happy path, webhook signature gate (missing header / invalid sig / unconfigured secret), all four event handlers, idempotency on replay, unknown-event-type 200, tenant lookup via stripe_customer_id (not from payload).
- Full backend suite: **1010 passed** (990 + 20). Frontend: typecheck clean, **60 tests green**, Vite build green. Every Stripe SDK call mocked — zero real network calls in tests.

## Production deploy event — DNS + email + Hetzner + Docker + TLS (2026-05-25)
(Originally logged as Batch 7; relabeled to an infra event so the Batch 7 slot can hold the MarketplacePage wire-up per the user's explicit numbering. Facts below intact.)
**Goal:** flip `abnplatform.com` from a marketing-only site into a real production stack — domain email, public landing, live `api.abnplatform.com` host serving the FastAPI backend.

**Delivered:**
- **Proton Mail domain** `abnplatform.com` fully configured — VERIFIED, MX, SPF, DKIM, DMARC all green. Three mailboxes live:
  - `hello@abnplatform.com` — sales / general
  - `reports@abnplatform.com` — automated agent-report sender
  - `legal@abnplatform.com` — legal / DPA / security
- **Vercel DNS** fixed — both `abnplatform.com` and `www.abnplatform.com` show **Valid Configuration**. Landing site continues to deploy from `origin/main` via the Jacob-authored post-merge hook.
- **Hetzner CAX21 server** provisioned — name `abn-core`, IP `46.225.165.62`, Frankfurt region, Ubuntu 24.04 LTS, GDPR-compliant DPA signed with Hetzner.
- **Docker + Docker Compose** installed; repo cloned to `/opt/abn`. All four containers running:
  - `abn-postgres` (PostgreSQL 16, persistent volume)
  - `abn-core` (FastAPI on :8000)
  - `abn-llm-gateway` (:8086)
  - `abn-security` (:8085)
- **Nginx + Let's Encrypt SSL** configured in front of the abn-core container. `https://api.abnplatform.com/health` returns `{"status":"ok","env":"production"}` over TLS.

**Verification:** the public health endpoint is reachable, the four containers are up, and `api.abnplatform.com` resolves to `46.225.165.62`.

## Batch 7 — MarketplacePage wired to real backend (DONE 2026-05-25)
**Prompt given:** Wire MarketplacePage to the real `Connector` backend — no new models or migrations (the `Connector` table already IS the tenant↔system join). Extend `client.ts` with `disableConnector` + `last_synced_at`; rewrite MarketplacePage to read from the live DB rows instead of a local Set; replace the "Kompatibel" badge with status pills (Aktiv / Inaktiv / Kompatibel); 8 new backend tests; all 1010 existing tests must still pass.

**Result:**
- Engineering rule #1: greps confirmed `initiateConnect` and `getConnectors` already existed; `disableConnector` did not. Extended what existed, added only what was missing — no duplication of `deleteConnector` (which is a one-way soft delete; PATCH `disableConnector` is bidirectional and supports re-enable).
- Caught FOUR bugs in the spec before writing code:
  1. Spec said `initiateConnect(tenantId, item.id)` — but `item.id` is a marketplace agent id like `'invoice-audit'`, not a connector type. Fixed to `initiateConnect(agent.requires, tenantId)`. Standalone agents (`requires=null`) render an "Inkluderad" pill, no install button.
  2. Spec named the response field `oauth_url` — the backend returns `connect_url`. Used the real field name.
  3. Spec called the new interface `ConnectorRow` with `id: string` — the existing `Connector` interface uses `id: number` matching the SQLAlchemy Integer PK. Extended the existing interface with `last_synced_at` rather than creating a duplicate.
  4. Spec said `POST /api/connectors/{type}/connect?tenant_id=…` (query param) — the existing backend takes `tenant_id` in the body. Kept the body form (Engineering rule #1: don't break the live contract).
- Frontend: `Connector` interface gains `last_synced_at: string | null`; `getConnectors(tenantId?, activeOnly?)` extends without breaking existing callers (both `() => getConnectors()` wrappers in ConnectorsPage + SettingsPage updated); `disableConnector(id, enabled=false)` + `enableConnector(id)` PATCH the row (pair of explicit helpers per Batch 7 contract; the `disableConnector(id, true)` form still works).
- MarketplacePage: installedMap derived from live `Connector` rows (`activeOnly=false` so disabled rows show "Inaktiv"). Per-connector-type loading spinner + inline error string. Optimistic invalidation on success. "Installera" → initiateConnect → `window.location.href = res.connect_url` (Nango OAuth redirect, per spec — popup form is in ConnectorsPage if we want it later).
- Backend tests: 8 in new `tests/test_marketplace_wire.py` — grouped by FLOW (empty → install → upsert idempotency → disable → re-enable → list → soft-delete → live-feed regression) rather than by route. Deliberately overlaps the route-level tests at the boundaries; the docstring explains why so future maintainers don't dedupe blindly. One test hit a `gps` connector that isn't in the YAML catalogue; switched to `hogia` (which is).
- Full backend suite: **1018 passed** (1010 + 8). Frontend: typecheck ✓, 60 tests ✓, Vite build ✓.

## Batch 8 — Migration deploy fix (DONE 2026-05-25)
**Prompt given:** Fix the migration deploy gap found in the prior audit (no script, Makefile target, compose/systemd/k8s step ran `alembic upgrade head` on the live server — startup used `create_all`, which never replays migration history). Replace `init_db()` in `main.py` lifespan with `alembic upgrade head`; document a one-time `alembic stamp head` on the Hetzner DB; fix the aspirational sentence in `docs/deployment.md`. Full test suite must stay green.

**Result:**
- `backend/main.py` lifespan: removed `from database.init_db import init_db` (line 20) and the `init_db()` call (line 93); replaced with `Config("alembic.ini")` + `command.upgrade(alembic_cfg, "head")`. The `init_db()` function itself stays — `scripts/setup.sh` still uses `python -m database.init_db` for fresh local dev installs.
- `backend/sidecar_entry.py`: comment update to reflect the new startup path + note that the PyInstaller bundle must include `alembic.ini` + `alembic/versions/` (separate batch when the desktop installer is next rebuilt).
- `CLAUDE.md` new section `## Production DB — one-time stamp (MANUAL — already done or do now)` with the exact `ssh root@46.225.165.62` → `cd /opt/abn/backend` → `docker compose exec abn-core alembic stamp head` → `alembic upgrade head` sequence and why a stamp is needed (Hetzner DB was bootstrapped via `create_all`, so `alembic_version` is empty/absent; running `upgrade head` against it without stamping first would try to recreate every existing table and fail).
- `docs/deployment.md` line 260: aspirational "Database migrations run automatically on startup" replaced with accurate text covering both the every-startup `alembic upgrade head` and the first-deploy `alembic stamp head` precondition.
- Verified by grep that no test imports `main.app` — every test builds a fresh `FastAPI()` and overrides `get_db`. The lifespan change is invisible to the test surface. Suite stays at **1018 passed, 84.35% coverage**.
- Flagged but not changed (separate batch): `Config("alembic.ini")` is CWD-relative; works in Docker (`WORKDIR=/app`) and systemd (`WorkingDirectory=/opt/abn/backend`); the PyInstaller desktop bundle does NOT bundle alembic files, so a fresh desktop install would fail on first run. Fix is a `datas=[…]` line in `abn-core.spec`.

## Batch 9 — Full automation chain (DONE 2026-05-25)
**Prompt given:** Wire the full Friday→Monday flow. Connector activate must trigger DNA → Process Graph → Agent creation automatically. Add an AgentScheduler that runs OPERA on the blueprint cron. Add plan-based agent limits (starter=2 / professional=25). Extend Observer `CONNECTOR_RESOURCES` to all 18 catalogue types. 15+ new tests. All 1018 existing must still pass.

**Result:**
- Engineering rule #1 flagged FIVE spec-vs-reality conflicts before code:
  1. Quality threshold: spec said "≥ 0.6", `process_graph/runner.py` already calibrates at AUTO=0.85 / VERIFIED=0.70 with an existing `agent_decision` field. Used the existing field per rule #2.
  2. `DNAPhaseOrchestrator(db, tenant_id)` doesn't exist — the real signature is `(db, config: DNAPhaseConfig, progress_callback)`. Constructed a real config.
  3. `ProcessGraphRunner(db, tenant_id)` arg order is wrong — real is `(tenant_id, db, domain, mode)`. Used the real signature.
  4. All three chain calls are `async` — wrapped them in an `asyncio.run` shim so the sync FastAPI BackgroundTasks queue can drive them.
  5. Hardcoding `CONNECTOR_RESOURCES` for 18 types would duplicate the YAML catalogue. Built `_build_connector_resources()` that reads YAML at module load + a `_seconds_to_cron` translator (rule #2).
- `api/routes/connectors.py`:
  - `BackgroundTasks` injected into `initiate_connect`.
  - New `_trigger_automation_chain(tenant_id, connector_type)` runs DNA → Graph → Agent end-to-end, each step in its own try/except so a fresh-install tenant with zero events fails defensively. Owns its own `SessionLocal()` since the request scope is closed by the time the task fires.
  - `_schedule_automation_chain` adapter wraps the async chain in `asyncio.run` for the BackgroundTasks queue.
- `agent_engine/scheduler.py` (NEW): `AgentScheduler` mirrors `ObserverScheduler`. Daemon thread, 60 s tick, `croniter`-based, `_running_tenants: set[str]` for one-run-per-tenant. Reads `agent.blueprint["schedule"]["cron"]` (also accepts legacy string form). Wired into `main.py` lifespan after the Observer scheduler.
- `api/routes/agents.py`: new `_PLAN_AGENT_LIMITS` dict (starter=2 / professional=25 / enterprise=10000), `get_tenant_plan_limits(db, tenant_id)`, `_count_active_agents(db, tenant_id)`, `enforce_agent_limit_or_raise(db, tenant_id)`. Gate added to `POST /api/agents/generate-from-process` — only on the non-idempotent path (existing agent for the process graph still returns 200 with `created=false`).
- `observer/scheduler.py`: `CONNECTOR_RESOURCES` rebuilt at import time from YAML. Outbound-direction YAMLs (`slack_delivery`, `sendgrid`) yield empty lists. Legacy hardcoded `gps` preserved as fallback (no `gps.yaml`).
- Two real bugs caught + fixed during test runs:
  1. `_is_due` had a wrong `_DUE_WINDOW` upper-bound check that rejected late-firing cron windows. Removed; `nxt <= now` is the right condition.
  2. `_is_due` short-circuited to True on `last_run_at=None` BEFORE validating the cron, so a malformed cron with no last-run was treated as "due now". Moved `croniter.is_valid` up.
- 18 new tests in `tests/test_automation_chain.py`: YAML coverage (3), automation chain (3), AgentScheduler (6), plan limits (5), regression (1).
- Full backend suite: **1036 passed** (1018 + 18), 84.45 % coverage.

## Batch 10 — DNS + public API endpoint (DONE 2026-05-25)
**Prompt given:** Wire DNS + public API endpoint for `www.abnplatform.com` and `api.abnplatform.com`. Add `vercel.json` with apex→www 308 redirect, security headers on `next.config`, `public_api_url` + `allowed_origins` in backend Settings, CORS reading from settings, `/health` returning `public_url`, replace any hardcoded localhost / `abn-nine.vercel.app` references. Additive only — no router or middleware re-architecting. 1036 tests must stay green and `npm run build` must produce 33 pages.

**Result:**
- Engineering rule #1/#2 flagged FOUR spec-vs-reality conflicts before code:
  1. `Settings` already has `cors_origins` + `cors_origins_list` (config.py:45 / 119). Spec said to add a parallel `allowed_origins` — **kept `cors_origins`** as the single source of truth (rule #2). Extended its default to include `https://www.abnplatform.com` + `https://abnplatform.com`.
  2. `main.py:181` CORS middleware already reads `settings.cors_origins_list` — Task 4 was essentially already done. Added an empty-list `["*"]` fallback for dev safety per the user's intent.
  3. Spec said `next.config.js`; the real file is `next.config.mjs` (ES module). Edited in place.
  4. Spec Task 6 said "replace any hardcoded `localhost:8000` / `localhost:3000` / `abn-nine.vercel.app` in landing" — **0 matches** in the grep. Nothing to fix.
- `landing/vercel.json` (NEW): apex→`www` 308 via `redirects[]` with `has.host` matcher. `$schema` set so Vercel's editor validates.
- `landing/next.config.mjs`: kept `reactStrictMode: true`; added `async headers()` returning `Strict-Transport-Security` (2 years incl. subdomains + preload), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`.
- `backend/core/config.py`: NEW `public_api_url: str = ""` field. `cors_origins` default extended with the two production origins.
- `backend/.env.example`: `PUBLIC_API_URL=https://api.abnplatform.com` block added next to `ABN_ENV`. `CORS_ORIGINS=` line extended with the two production origins. (No `ALLOWED_ORIGINS` env var — `cors_origins` is the canonical name; comment explains why.)
- `backend/main.py` CORS: now reads `_cors_origins = [o for o in settings.cors_origins_list if o]` and uses `_cors_origins or ["*"]` so an empty env doesn't 502 the dashboard in dev.
- `backend/api/routes/health.py`: `/health` response gains `"public_url": settings.public_api_url`. Additive only.
- `CLAUDE.md`: new `## DNS & Domains` section with the host→target table, manual registrar steps (Vercel A `76.76.21.21`, CNAME `cname.vercel-dns.com`, Hetzner A `46.225.165.62`), and the manual Vercel project step (add both domains under `abn-nine`).
- Backend suite: **1036 passed** (unchanged). Landing: `✓ Compiled successfully`, `✓ Generating static pages (33/33)`, zero errors / warnings.

## Batch 11 — NEXT
Fortnox end-to-end (needs org number + API key from Jacob).

## Batch 12
Branch-protection hardening (manual GitHub step — listed in CLAUDE.md under `## Branch Protection`).

## Batch 13
Bundle `alembic.ini` + `alembic/versions/` into the PyInstaller `abn-core.spec` so the Tauri desktop sidecar can run `alembic upgrade head` on a fresh install (Batch 8 follow-up).

## Manual prereqs for Stripe to go live
1. Stripe Dashboard → create two Products + Price objects (Starter free / Professional €299/mo recurring) and copy their `price_...` ids.
2. Stripe Dashboard → Developers → API keys → copy the `sk_test_...` (or `sk_live_...`) secret key.
3. Stripe Dashboard → Developers → Webhooks → add endpoint `https://api.abnplatform.com/api/billing/webhook`, subscribe to `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`. Copy the `whsec_...` signing secret.
4. Set all four env vars on the Node (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PROFESSIONAL_PRICE_ID`, `STRIPE_STARTER_PRICE_ID`). The billing routes return 503 until they are set.

## Blockers
- Fortnox: waiting for org number + API key from Jacob
- ~~DNS: Proton mail propagation (hello@abnplatform.com)~~ — **resolved 2026-05-25 (Production deploy event)**
- ~~MarketplacePage was a stub (local Set, no DB writes)~~ — **resolved 2026-05-25 (Batch 7)**
- Google Cloud OAuth: ~~waiting for mail-domain verification~~ → unblocked (Proton mail-domain verified); add the OAuth callback URL when ready

## Technical debt
- 7 duplicated package dirs (`agent_evolution/agent_evolution/`, `consistency/consistency/`, `delivery/delivery/`, `gdpr/gdpr/`, `inquiry_engine/inquiry_engine/`, `onboarding/onboarding/`, `opera/opera/`, `output_schema/output_schema/`) — cleanup sweep deferred
- Two OPERA runners coexist (`agent_engine/opera/runner.py` production, `opera/integrated_runner.py` legacy) — consolidation deferred
- `AgentBlackboard` table missing — reads degrade gracefully to empty list
- Blueprint version history missing — `POST /api/agents/{id}/rollback/{v}` returns 501
- `services/abn-security/` (Go) not compiled in this environment — CI job covers it
- Many modules use deprecated `datetime.utcnow()` — cosmetic warnings only

## ABN Innovation Philosophy

Build ABN like the greatest innovators built their work:

- **Ada Lovelace** — first to see what machines could truly become
- **Alan Turing** — redefined intelligence, built the foundation of computing and AI
- **John von Neumann** — architected how modern computers think and store
- **Steve Jobs** — made complexity invisible, obsessed over experience
- **Bill Gates** — saw software as infrastructure for the world

Rules:

- Never ask "does this exist?" — ask "should it exist, can we build it better?"
- If a module ABN needs doesn't exist: use Python, Linux, open source — adapt it, make it ABN's own
- Every feature must be 100,000x better than competitors — or don't ship it
- Think in platforms, not features. Think in decades, not sprints
- ABN has no real competitor — because no competitor thinks like this

## Rules
- Always read CLAUDE.md first
- Grep before building — no duplication (Engineering rule #1 in CLAUDE.md)
- All prompts to Claude Code in English; code/comments/identifiers in English (rule #3)
- Act as 4 engineers: Architect, Engineer, Reviewer, Optimizer
- One batch at a time, green build before next (rule #8)
- After every batch: update this file + CHAT_LOG.md and push to GitHub (see `## Session Management` in CLAUDE.md)

## OPEN ITEMS
Unfinished sub-tasks / open questions live here (referenced by CLAUDE.md §4.2 PRE-/clear HALT PROTOCOL). Empty = nothing pending.

### ⚠️ CONSOLIDATED SIGN-OFF BEFORE PUBLISHING (all softened-now, NOTHING invented — Jacob must approve before the public site goes live)
The public copy is built honest + scoped, but these claims need founder/legal approval before publish. All are SOFTENED already (no over-absolutes ship); this is the review gate, not a blocker to building.
- **No-Data wording (scoped form, site-wide):** the canonical line is "raw operational data is designed to stay inside the customer environment in normal use; ABN Platform AB does not receive a copy of it." Confirm this exact phrasing is acceptable. Appears on: Hero metric, TrustSection eyebrow + founder quote, /transparency commitments + 8-step, legal-center lede, observer step, layout metadata, about, subprocessors, invoice-management.
- **TrustSection founder quote (D2)** — ships EXACTLY Jacob's approved line ("We built ABN to run on your own server… ABN Platform AB never receives a copy of it."). Confirm.
- **SOC 2 (D3):** label is now "SOC 2 Type 1 readiness" (the doc is readiness, NOT a completed attestation/certification). Confirm wording.
- **Regulated-sector claims (SITE-B):** Healthcare (whole page), Financial Services (risk/compliance), Manufacturing ("not controlling machines unless built, certified and governed").
- **Regulated process claims (SITE-C):** Compliance monitoring ("does not guarantee legal or regulatory compliance"), Document intelligence ("must not claim legal/medical/financial/regulated advice"), Anomaly detection ("signals, not final truth").
- **Compliance/GDPR claims (SITE-A):** company/security "commitment", invoice-management "Designed for compliance" + "legal terms depend on deployment", pricing GDPR FAQ.
- **EU AI Act + DPA wording** (legal-center / legal docs) — §10.2/§10.3 forms.
- **Developer API (SITE-E) — RESOLVED as early-access (was the "public API reality" open question).** Jacob confirmed there is NO live public v1 API (only infra: Stripe/Nango/Vercel). The /api page is reframed to honest early-access: no callable base URL, no runnable request samples, no key-issuance; endpoints/payload shown only under "Planned API surface — not callable today". Confirm the early-access framing reads right; when a real public API ships, this page graduates from "planned" to live.
- **Agent catalog (SITE-E) — generator framing.** /agents presents the 12 §8 roles as EXAMPLES the Blueprint Generator creates (not 12 fixed products); EXECUTOR/EXECUTE_CHANGE shown as "planned · V2 — not available today". Confirm.
- **ORG-NUMBER GAP:** the 559XXX-XXXX placeholder was removed (footer + subprocessors); a REAL Swedish reg.no must be added on both before publish.

- BACKEND-TENANT-2c-2 tenant isolation on proposal READ endpoints (backend-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: closes the proposal READ surface (DISCOVERY-flagged id-only → cross-tenant read = data leak). Reuses the proven `_load_tenant_proposal_or_404` (2a) — no parallel pattern, no consolidation (TENANT-HELPER-1). **Rewired (`proposals.py`, id-only/caller-param → `current.tenant_id`, 404):** `GET /{id}` (detail, was id-only `_get_or_404`); `GET /{id}/report` (was id-only — its `report_refs` come from the **scoped proposal's own** `output_summary` JSON field, no separate cross-tenant resource lookup → **compound ownership intrinsic**, no path to mix another tenant's report data); `GET ""` (list — was **caller-param** `tenant_id`: an absent param returned EVERY tenant's proposals → now ALWAYS scoped to `current.tenant_id` via the fail-closed sentinel `"__no_such_tenant__"`; `total` + `pending` derive from the scoped query → **no cross-tenant count/total leak**). The 3 reads had **no `current` dep** → each gained `current = Depends(require_auth)` (reads VIEWER+; `require_role` on approve/reject untouched). Tenant-load is the **FIRST op** → cross-tenant 404s before any data; **404 not 403**; missing vs cross-tenant **indistinguishable** (both `{"detail": "Proposal X not found"}`); fail-closed both sides. **Dead-code cleanup:** the now-orphaned id-only `_get_or_404` (2a kept it "for the Tier-3 reads pending in 2c") removed; loader docstring updated — NOT the deferred loader consolidation. **API-CLEANUP grows:** proposals `list` `tenant_id` Query param (kept, ignored for authz). **Shipped Tuari-3 Approval Center** (`getProposals()` no-param + `getProposal(id)`) keeps working same-tenant (proven); frontend ORÖRD. **COMPLETION (safer):** **2a + 2b + 2c-1 (agent reads) + 2c-2 (proposal reads) make the audited AGENT + PROPOSAL surfaces tenant-safe**; STILL pending **2c-3** (report reads) + **TENANT-DISCOVERY-EXTRA** (~23 route families). DO NOT claim the multi-tenant server tier is fully tenant-safe until 2c-3 + EXTRA complete. Gates: full backend suite **2099 passed** (2089 + 10 in new `tests/test_backend_tenant_2c_2.py` — per read cross-tenant 404 + no-leak body; missing==cross-tenant; list scoped + param-ignored + totals/pending scoped + fail-closed empty tenant; same-tenant VIEWER list/detail/report 200). One existing test aligned (`test_proposal_wiring::test_proposals_api_returns_new_row` user `tenant_id="t1"` → `TENANT_ID`, the same latent mismatch 2a/2b/2c-1 fixed; the list now scopes to current). No migration/schema/runtime/role-model/frontend change; `reports.py` + agent reads + Tier-1/2 untouched; diff = backend/ (proposals.py + tests) + session. **NEXT (deferred):** **BACKEND-TENANT-2c-3** (reports get/download — `_load_tenant_run_or_404`; the LAST 2c piece) · **TENANT-DISCOVERY-EXTRA** (~23 tenant-bearing route families) · **TENANT-HELPER-1** (consolidate the 5 copied `_load_tenant_*` loaders, AFTER the family is complete) · **API-CLEANUP** · ROLE-UI-2 · RAIL-2 · APP-3 · APP-6. [Quality & correctness #1.]

- BACKEND-TENANT-2c-1 tenant isolation on Tier-3 AGENT reads + RAIL-1 (backend-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: closes the agent READ surface (cross-tenant READ = data leak). Tier-3 was too large for one PR → SPLIT per the prompt's preferred order: **2c-1 = AGENT reads (this)**, 2c-2 = proposal reads, 2c-3 = report reads. **Rewired (`agents.py`, id-only/caller-param → `current.tenant_id`, 404; reuse 2a `_load_tenant_agent_or_404`):** GET detail / runs / findings / finding-detail (compound) / trend / activity / insight-layer / settings / weekly-stats / **`/runs/{run_id}/verification` (RAIL-1 evidence, compound)**; the caller-param aggregates `roi-summary` + `anomaly-trend` (local `tenant_id` re-bound to `current.tenant_id`, fail-closed sentinel `"__no_such_tenant__"`); `list_agents` (now ALWAYS scoped — an absent param previously returned ALL agents); `POST /{id}/simulate` (a dry-run preview that fell between 2a mutating + 2b caller-param scope — id-only info-leak, closed). The GET family lacked a `current` dep → added `current = Depends(require_auth)`. **Compound ownership:** finding-trace keeps `finding.agent_id == agent_id` after the tenant load; the RAIL-1 verification reads `ABNAttestation` keyed by `cycle_id == run_id` ALONE and **ABNAttestation has NO tenant/agent column** → the only ownership proof is the `AgentRun` (now requires `AgentRun(run_id, agent_id=agent_id, tenant_id=current.tenant_id)` before returning the attestation; a caller can't pass their own agent_id + a victim's run_id). Same 404 message for cross-tenant / missing-run / missing-attestation (no existence leak). **RAIL-1 integrity proven:** same-tenant VIEWER GET still returns evidence; cross-tenant → 404; a run of ANOTHER of the tenant's agents → 404; frontend ORÖRD (it already sends agentId+runId — note the verification endpoint now requires the run's AgentRun row, true on the live OPERARunner path which writes both; the prior tests seeded only the attestation → made production-faithful by seeding the AgentRun). **API-CLEANUP list grows:** the Query `tenant_id` on `roi-summary`, `anomaly-trend`, and the optional `tenant_id` on `list_agents` (kept, ignored for authz). `regenerate` (NODE_ADMIN) already enforced current.tenant_id → unchanged; `simulate` had `current` already → only the loader swapped. **COMPLETION-CLAIM-GATE (code is truth):** a grep of `backend/api/routes` found tenant-bearing patterns in **23 files** → SAFER wording: **2a + 2b + 2c-1 make the audited AGENT surface (mutations + reads) tenant-safe**; the remaining audited pieces are **2c-2** (proposals `GET /{id}` + `/{id}/report` — still id-only) and **2c-3** (reports `GET /{id}` + `/{id}/download` — still id-only); other route families (standalone `roi.py`, `transparency`, `dna_phase`, `friday_report`, `mind`, `compliance`, `connectors`, `tenant_settings`, …) → **TENANT-DISCOVERY-EXTRA**. **DO NOT claim the multi-tenant server tier is fully tenant-safe until 2c-2 + 2c-3 + TENANT-DISCOVERY-EXTRA are complete.** Gates: full backend suite **2089 passed** (2073 + 16 in new `tests/test_backend_tenant_2c.py` — per agent read cross-tenant 404 + a no-leak body assertion (the 404 never carries the victim's name/tenant/run_id/finding-title/status); list excludes cross-tenant; finding-trace + verification compound; RAIL-1 same-tenant VIEWER 200; roi param-ignored). Six existing read-test files aligned (override tenant → resource tenant; the 3 caller-param isolation tests rewritten to the 2c model — a tenant-A user passing tenant_id=B gets only A's data; the verification tests seed an AgentRun). No migration/schema/runtime/role-model/frontend change; Tier-1 + Tier-2 routes untouched; diff = backend/ (agents.py + tests) + session. **NEXT (deferred):** **BACKEND-TENANT-2c-2** (proposals get/report reads — use existing `_load_tenant_proposal_or_404`) · **BACKEND-TENANT-2c-3** (reports get_metadata/download — `_load_tenant_run_or_404`) · **TENANT-DISCOVERY-EXTRA** (the remaining 23-file tenant-bearing route families) · **API-CLEANUP** (remove the ignored caller tenant_id params once the frontend stops sending them) · ROLE-UI-2 · RAIL-2 · APP-3 · APP-6. [Quality & correctness #1.]

- BACKEND-TENANT-2b tenant isolation on Tier-2 caller-param endpoints (backend-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: closes the WEAK family — endpoints that compared a **caller-supplied** `tenant_id` (query/body), not `current.tenant_id` → false security (an authenticated attacker passes the victim's tenant_id and the check passes). Now authorizes on **`current.tenant_id` ONLY** (admin pattern, 404). **Rewired (`agents.py`):** `instruct` (body req.tenant_id), `generate-from-process` (body + scoped ProcessGraph load + plan-limit on current.tenant_id), and the GET family — `ai-act-declaration`×3 / `instructions` / `intelligence` / `intelligence/benchmark` (added `current = Depends(require_auth)` since the GETs had no `current`, then scoped load + table filter to current.tenant_id). `regenerate` ALREADY used current.tenant_id → left unchanged. **API compat (LOCKED):** the caller `tenant_id` query/body params are KEPT (frontend still sends them) but **IGNORED for authz** + marked deprecated → **API-CLEANUP** later removes: the Query `tenant_id` on the 6 GETs + the body `tenant_id` on `InstructionRequest` + `GenerateFromProcessRequest`. 404 not 403; fail-closed on empty current AND resource tenant. Role guards UNCHANGED. Gates: full backend suite **2073 passed** (2062 + 11 in new `test_backend_tenant_2b.py` — param-bypass closed + param-ignored-for-authz + mutating 404 zero-side-effect); 5 existing Tier-2 test files aligned (overrides were tenant "t1" vs resources "t-test"/TENANT_ID); the generate guard-test's old "wrong body tenant_id → 403" corrected to 2b semantics (param ignored → succeeds). Diff = backend/ (agents.py + tests) + session. **NEXT (deferred):** **BACKEND-TENANT-2c** = Tier-3 reads/download/verification must become tenant-scoped (agent detail/runs/findings, proposal get/report, report download/metadata, **the RAIL-1 evidence `/runs/{run_id}/verification` endpoint**) — the LAST piece; **API-CLEANUP** = remove the now-ignored caller tenant_id params once frontend stops sending them; **ROLE-UI-2** (frontend button-gating, can proceed); **BRANDING/LANDING-DISCOVERY** (app-chrome marks + landing fixes); RAIL-2 · APP-3 · APP-6. **RULE: the multi-tenant server tier is NOT tenant-safe until 2a + 2b + 2c are ALL complete.** [Quality & correctness #1.]

- BACKEND-TENANT-2a tenant isolation on Tier-1 mutating endpoints (backend-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: closes the real cross-tenant IDOR gap (BACKEND-TENANT-1 Discovery) — route loaders were **id-only**, so on a multi-tenant server tier an AGENT_MANAGER/NODE_ADMIN of tenant A could act on tenant B's resource by id (unreachable on the single-tenant-per-Node desktop deployment; real for the server tier). **3 tenant-scoped loaders** (copy the proven `admin._load_tenant_agent_or_404`: filter id AND `current.tenant_id` → **404** on mismatch, never 403, never reveal cross-tenant existence, **fail-closed** on null tenant): `agents._load_tenant_agent_or_404`, `proposals._load_tenant_proposal_or_404`, `reports._load_tenant_run_or_404`. **Rewired Tier-1 MUTATING** to them via `current.tenant_id` (tenant gate = first data-access/authorization/side-effect-gating op): agents **run/pause/resume/settings-PUT/rollback** · proposals **approve/reject** · reports **deliver**. **rollback (NODE_ADMIN) is STILL tenant-scoped** (role ≠ tenant). Role guards UNCHANGED — wrong role → 403, wrong tenant → 404. **CODE-IS-TRUTH correction:** the prior belief that proposals were tenant-scoped was FALSE (approve/reject were id-only; AUTH-3a added only ROLE gating). Gates: full backend suite **2062 passed** (2039 + 23 in new `test_backend_tenant_2a.py`); 5 existing API tests aligned to genuine same-tenant + test_api_routes seeds a same-tenant agent/run; no migration/schema/runtime/role-model/frontend change; diff = backend/ + session. **NEXT (deferred):** **BACKEND-TENANT-2b** = Tier-2 caller-param endpoints (instruct / generate-from-process / ai-act / intelligence) must switch from a caller-supplied `tenant_id` to `current.tenant_id` (currently NOT real isolation). **BACKEND-TENANT-2c** = Tier-3 reads/download/verification must become tenant-scoped (agent detail/runs/findings, proposal get/report, report download/metadata, **the RAIL-1 evidence `/runs/{run_id}/verification` endpoint**). **ROLE-UI-2** = frontend mutating-button gating (VIEWER/AUDITOR/ANALYST) — can now proceed. **RULE: the multi-tenant server tier is NOT tenant-safe until 2a + 2b + 2c are all complete.** [Quality & correctness #1.]

- ROLE-UI-1 role helper + Admin-link gating (frontend-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: the minimal role-aware-UI foundation. **`lib/roles.ts`** (NEW, pure/React-free) mirrors the backend `require_role` **EXACTLY** — exact-set + NODE_ADMIN-superset, **NOT hierarchical**: `canManageAgents = {AGENT_MANAGER, NODE_ADMIN}`, `canAccessAdmin = {NODE_ADMIN}` (ANALYST/AUDITOR/VIEWER get NO manage affordances — they 403 on the backend), `canRunAgentActions = canManageAgents`, `canViewEvidence(isAuthenticated)` (VIEWER+, **never role-gated** — RAIL-1 keeps working). `normalizeRole` replicates backend `Role.normalize`: `strip().lower()` FIRST + ALL `_ALIASES` (both `_`/`-` variants); null/empty → VIEWER; **unrecognised → 'UNKNOWN' fail-closed** (deliberate safer divergence from the backend's `name.upper()` passthrough). **ONE use:** `Layout.tsx` renders the `/admin` NAV link only when `canAccessAdmin(user?.role)` (the `/admin` route + AuthGate + AdminPage 403 fallback all untouched; `SideNav` exported for the test). HARD LAW: frontend gating is UX only — the backend 403 is the guard. Gates: tsc ✓, vitest 177 (156+21: roles ×12, SideNav ×9) ✓, build ✓; diff = frontend/ + session. **NEXT (deferred):** **ROLE-UI-2** = gate the mutating buttons (run/simulate/pause/resume/instruct/settings/approve/reject) from VIEWER/AUDITOR/ANALYST via `canManageAgents`; **BACKEND-TENANT-1** = server-side tenant enforcement on the agent action endpoints (`_get_or_404` is id-only — harmless on a single-tenant Node, real on the multi-tenant server tier); **ANALYST-approve** = a backend policy decision (whether ANALYST regains approve — not a frontend call); RAIL-2 · APP-3 · APP-6. [Quality & correctness #1.]

- RAIL-1 evidence mode in the docked rail (frontend-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: built ONLY the `evidence` rail mode (workspace_menu stays reserved/unchanged). **Additive in-memory payload** on the provider — `open(mode, payload?)` / `toggle(mode, payload?)` carry `{agentId, runId}` (evidenceId/proposalId reserved/typed-unused); `{mode,payload}` is one atomic state; NO persistence/localStorage (APP-6). **Evidence renders the EXISTING `EvidenceRibbon` verbatim** (`getAgentRunVerification` → `GET /agents/{id}/runs/{run_id}/verification`, VIEWER-safe display-only) — no second fetch/mutation path. Pure `resolveEvidenceTarget(payload, routeCtx)` → ribbon|empty|inconsistent: needs BOTH ids (else honest empty, never a "latest run" guess); **integrity** — payload.agentId must match the route agentId or it's `inconsistent` (no fetch, no mixing a route agent with another agent's runId). **No-leak proven by test** (evidence → close → operator_assistant sees no runId). Reusable `OpenEvidenceButton` wired into AgentDetailPage HistoryTab (new "Bevis" column; `RunSummary.run_id` non-null, agentId = route agent). **Proposal affordance OMITTED** — `Proposal.agent_id` + `run_id` both nullable → not reliable. Gates: tsc ✓, vitest 156 (142+14) ✓, build ✓; diff = frontend/ + session. **NEXT (deferred tracks):** workspace_menu navigation/read-only (narrow) OR wait; **role-aware-UI helper** (own batch — `useAuth().user.role` is available but no proactive gating exists yet; needed before any mutating menu action); **APP-3** InstructBar deprecation → route into the shared rail; **APP-6** Conversation Ledger / rail persistence; **report-download client wrapper** (a future gap — `GET /reports/{id}/download` is VIEWER+ but has no client fn; the AgentDetailPage "Ladda ner PDF/Excel" buttons are stubs). [Quality & correctness #1.]

- CI-BUDGET-1 minimal CI cost hardening (OPS/CI only) [done — PR #141 open, awaiting Jacob review; do NOT auto-merge]: two measured zero-merge-risk wins in `.github/workflows/ci.yml`. **(1)** docker `Build & push images` → MAIN-PUSH-ONLY (the `push && refs/heads/main` `if` moved from the login STEP up to the JOB level → a PR skips the whole job, ~3m22/PR; it was building 4 images it never pushes on a PR). **(2)** the non-required Backend suite → JOB-LEVEL path-gate via a dependency-free `changes` detector job (bash `git diff "$base...HEAD"`, no new action) outputting `backend=true/false`; `backend-tests` gains `needs: [changes]` + `if: push || needs.changes.outputs.backend == 'true'` → skips on a frontend-only PR (~4m12 saved), runs on `backend/**`/`ci.yml`/every push. **CRITICAL law (now in CLAUDE.md):** job-level only — NEVER `on.pull_request.paths` (would skip the required Frontend/Landing/abn-security jobs that share this workflow → "Expected — waiting" merge-deadlock); CI cost cuts go in NON-required jobs only; never path-filter a required check without a same-name success job or de-requiring first. **T6 verified on PR #141:** required set = exactly 5 (Frontend/Landing/Shield/abn-security 45a/migrations — confirmed via `gh api .../branches/main/protection`); `changed-paths` PASS; the 5 required checks all registered + running (none stuck "Expected — waiting"); `Backend — 1518 tests` RUNS on this PR (correct — it touches ci.yml → detector backend=true); `Build & push images` ABSENT (cleanly job-skipped on a PR; non-required → no blocking context). Diff = ci.yml + session only; build-release.yml/crons/branch-protection/required-names/product-code all untouched. **Deferred OPS (separate tracks, NOT urgent):** CI-BUDGET-2 = disable pre-launch crons in the Actions UI (ZAP DAST / diffusion-daily / pulse / mind-weekly / release-bandwidth-guard — zero CI cost vs editing); RELEASE-FIX = macOS cross-arch in build-release.yml (burns ~25 min then fails); `Backend — 1518 tests` count-free rename in lockstep with the branch-protection key (it is non-required, so safe — but the rename still needs the UI edit). [Quality & correctness #1.]

- APP-2c Frontend fleet-health surfaces (frontend-only, display-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: consumes the APP-2b backend fields. **Display-only — shows true status, changes NO behaviour/backend/actions** (pause/resume/enabled/approval logic untouched). **client.ts:** `Agent` += optional `health_paused?`/`pending_human_approval?` (`AgentDetail extends Agent` inherits); `AdminOverview.agents` += optional `quarantined?`/`health_paused?`/`pending_approval?` — additive, nothing else breaks. **`lib/format.ts`:** ONE shared `agentStatusChips({health_paused?, pending_human_approval?})` → `["Pausad (hälsa)"?, "Väntar på godkännande"?]` (health first; undefined/false → no chip; **quarantined never composed**); the two labels added to the `statusTone` AMBER set so the EXISTING `StatusBadge` renders them amber — reuse the primitive, **no new chip design**. **AgentsPage + AgentDetailPage:** AUGMENT the base Aktiv/Pausad `StatusBadge` with the chips (never replace; `status` untouched). **AdminPage `OverviewTiles`:** +3 count Tiles (Karantän / Pausad (hälsa) / Väntar på godkännande) via `fmtCount(n)= n===undefined?"—":String(n)` (**undefined→"—", 0→"0"** — never a fabricated 0); inside `overview.data` (NODE_ADMIN-gated → a non-admin's 403 surfaces no count); `OverviewTiles` exported for tests. Dashboard aggregate OMITTED. Gates: tsc ✓; full frontend vitest **142 passed** (130 + 12: format ×6, AgentsPage ×3, AdminPage ×3); Vite build ✓. T3 — no quarantined on any VIEWER surface (only a doc-comment); T4 — `status`/`_agent_status` untouched + existing tests green; T5 — undefined→no chip/"—"; T6 — StatusBadge/statusTone reused, v7 only (the 🤖/✅/❌/hex the grep finds are pre-existing avatar/recharts/Tier3-Pulse-Shield code, not APP-2c); T7 — frontend/ + session only. **NEXT:** rail evidence/workspace_menu modes → APP-3 (InstructBar deprecation) → … → APP-6 (Conversation Ledger). **Role-aware UI** (the sidebar Admin link is visible to non-admins; the data is backend-403-gated + the page shows "Ingen behörighet") remains a separate future track. [Quality & correctness #1.]

- APP-2b Expose fleet-health fields (backend-only) [done — PR open, awaiting Jacob review; do NOT auto-merge]: closes the APP-2 flag-for-small-backend. Surfaces three already-existing `Agent` fields (`quarantined`/`health_paused`/`pending_human_approval`, shipped Batch 66/S2F3-1/72b) **role-correctly, additively, No-Data, no migration**. **LOCKED role-scope (Jacob):** VIEWER `/api/agents` (list) + `/api/agents/{id}` → `AgentResponse` +2 booleans (`health_paused`, `pending_human_approval`, set in `_to_response` via `getattr(…,False)` — reaches list + detail); NODE_ADMIN `/api/admin/overview` agents{} → +3 tenant-scoped COUNTS (`quarantined`, `health_paused`, `pending_approval`). **`quarantined` (security action) is NEVER on the VIEWER surface — only the admin count.** No reason/timestamp/incident/security-log/raw-evidence anywhere. `status` (active|paused from enabled) UNCHANGED — additive, non-breaking; the frontend composes the honest label from the booleans. Fail-safe defaults (False/0). Gates: full backend suite **2039 passed** (0 regressions); new `tests/test_app2b_fleet_health.py` (8 — VIEWER booleans present + quarantined/reason/ts ABSENT incl. a structural `AgentResponse.model_fields` assert; admin counts correct under a mixed + multi-tenant fleet; fresh→false/0; VIEWER→403 on /admin/overview). T4 status unchanged + no migration; T5 booleans/counts only (the reason/ts code the grep finds is pre-existing NODE_ADMIN quarantine/activation write-endpoints, untouched); T6 backend/ + session only. **NEXT:** the frontend fleet-health panel consuming these — additive optional TS fields: `Agent` + `health_paused?`/`pending_human_approval?`; `AdminOverview.agents` + `quarantined?`/`health_paused?`/`pending_approval?` — then rail evidence/workspace_menu modes → APP-3 → … → APP-6. [Quality & correctness #1.]

- APP-2 Control-room hardening (Översikt) [done — PR open, awaiting Jacob review; do NOT auto-merge]: hardened `DashboardPage` into an honest, scale-safe control room with **NO backend change** (the APP-DISCOVERY "harden-with-existing" path). **Headline fix:** the fresh-node `Systemintelligens = 100` defect — `lib/metrics.ts` `intelligenceScore` now returns `number | null` (**null when no runs carry a status**; the ring shows "Ingen körning ännu", never a fabricated 100; sublabel corrected to "Vägt mått på drift och lyckade körningar"). **Six scale/integrity safeguards:** #1 capped-window honesty (`RUNS_WINDOW=50`; Zone B "Senaste körningarna · upp till 50"; verification "av de senaste körningarna" — never a fleet total); #2 all derived series `useMemo`'d; #3 three distinct loading(`Skeleton`)/error("Kunde inte hämta")/empty/data states per panel (new `MetricCell` + 4-way ring gate — never a fabricated 0/100); #4 per-panel isolation (a failing `/runs` degrades only its panels; agents/reports/audit unaffected); #5 poll-safe (shared `LIVE` opts + `refetchIntervalInBackground:false` + react-query in-flight dedupe); #6 no new ROI divergence (home = recent-window indicator; canonical ROI stays Admin `/agents/roi-summary`). Plus an honest fresh-node "Kom igång" card (→ `/connectors`). **Decisions (Jacob delegated "best for ABN"):** canonical ROI = roi-summary; home shows a recent-window indicator, NOT a competing total (chosen over wiring roi-summary into the home to avoid a new failure surface + tenant_id/fresh-node fragility — safest at scale). Gates: tsc ✓; vitest **130** (125 + 5; T(scale) 500 runs/120 agents, fresh-node-no-100, error-isolation, capped-window-label); Vite build ✓; T4 v7 tokens only; T6 frontend/ + session; **InstructBar + rail UNTOUCHED**. CLAUDE.md gained the **control-room honesty standing rule**. **NEXT:** APP-2b (rail evidence/workspace_menu modes on real endpoints) → APP-3 (InstructBar deprecation) → … → APP-6 (Conversation Ledger). **Flag-for-small-backend (future):** expose fleet `quarantined`/`health_paused`/`pending_human_approval` on the agents list — the one thing blocking a truthful fleet-health panel. [Quality & correctness #1.]

- APP-1 Global docked Right Rail + ABN Operator Assistant shell [done — PR open, awaiting Jacob review; do NOT auto-merge]: FIRST app-track batch (node app, frontend/, Swedish, v7). **LOCKED design decision (Jacob): ONE shared, global, docked ABN Operator Assistant right rail — NEVER a chatbot per agent** (now a CLAUDE.md PERMANENT standing rule). Built ONLY the rail infrastructure + the Assistant SHELL — not chat/memory. NEW `lib/rail.tsx` (RightRailProvider/useRightRail — open/close + one active `mode` by construction, in-memory, NO persistence; `RailMode = operator_assistant` [built] `| evidence | workspace_menu` [reserved]; `useRailContext`/`deriveRailContext` — pure route→scope, nullable reserved slots {tenantId, agentId, runId, evidenceId, proposalId}, only agentId filled from the route, the rest null, never fabricated; live-derived so NO stale-context carry-over across routes). NEW `components/rail/RightRail.tsx` (docked column lg+ `static w-[352px] shrink-0 border-l` → MAIN flex-1 min-w-0 shrinks, NEVER overlay on desktop; mobile `fixed inset-0` separate view; null when closed). NEW `components/rail/OperatorAssistant.tsx` (context card + static "Exempel på frågor" per surface + DISABLED input "Assistenten kopplas in senare"; unknown surface → honest empty state). NEW `components/rail/RailTrigger.tsx` (the SINGLE shared "Fråga ABN" trigger, rendered once in the Layout chrome, never per-route). EDIT `components/Layout.tsx` (wrapped in RightRailProvider; 3-column shell LEFT NAV | CENTER chrome+main | RIGHT RAIL — additive). **/instruct NOT wired** (the locked decision — it scopes only by agent_id+tenant_id, lacks the workspace/run/evidence/proposal context a shared assistant needs; input is a disabled honest empty state; no conversation thread/memory/fabrication). **InstructBar UNTOUCHED** (deprecation = APP-3). Gates: tsc ✓; vitest **125** (114 + 11 new in `components/rail/RightRail.test.tsx`); Vite build ✓; T2 open/close/toggle/one-mode/context/empty/disabled-input; T3 routes unchanged + InstructBar not in diff; T4 v7 tokens only (no new colour/icon-lib/emoji; no overlay-on-content desktop); T5 no fabricated assistant output/context; T6 frontend/ + session only. **APP-TRACK ORDER:** APP-1 (this) → APP-2 (control-room hardening: Översikt/Settings-buildout/Reports page/role-aware UI) → APP-3 (InstructBar deprecation — route its capability into the shared rail) → APP-4/5 (TBD) → **APP-6 (Conversation Ledger / persistence — the only place rail/chat persistence is allowed)**. [Quality & correctness #1.]

- TRUTH-3 Final §10.1 over-absolute sweep — PUBLIC SITE TRUTH-CLEAN [done — PR open, awaiting Jacob review; do NOT auto-merge]: TRUTH-SWEEP fix-batch 3 of 3 (the last). Softened the remaining over-absolute No-Data / compliance / performance claims on the 3 product pages — **copy/claims only, design preserved (v7 byte-identical), no NEW metrics/guarantees/compliance-promises/capability-claims**. **/observer**: metadata + lede ("Never raw data, never PII, never the cloud" / "never on its way out") → scoped ("reads approved structures and allowed fields inside the customer-controlled environment, designed to minimise, filter and tokenise data before higher layers use it"); the "What it NEVER does / Three absolute noes" section → "Where the boundary sits / Three design boundaries" (items rescoped: "No raw values upstream" / "No state between runs" / "No raw data to the cloud" — last scoped to "not sent to an external LLM or to ABN Platform during normal operation; when external reasoning is enabled, only abstracted/tokenised context"; ✕ gold marker + grid preserved). **/company/about**: Mission "The LLM never receives raw values … zero data leakage" → No-Data-design scoped (removed "zero data leakage"); "Four rules we never break" → "Four rules at the core"; "No data transfers outside the EU, no subprocessors that see customer data — because there is no customer data to see" → "the local-first design is built to keep customer operational data inside the customer-controlled environment … any subprocessors are listed in our subprocessor register with the applicable safeguards". **/autonomous-engine**: "The No-Data guarantee begins at the prompt" → "ABN's No-Data design: raw operational data is not sent to the prompt during normal operation"; the unlabelled illustrative "8.6% … generic 5%" REMOVED → "your actual deviation level, not a generic industry default"; "Four archetypes. Never a fifth." → "Four active archetypes in V1" (body already states EXECUTOR is the locked fifth — heading was contradictory); "Never values" → "not raw values"; the 2 SOC-2 capability claims (EXECUTOR body + Tier-3 card) → "gated by … future approval, policy and security controls" (no unearned cert). Gates: build + tsc GREEN (46); T2 — 0 over-absolutes render on the 3 pages + scoped language present; T3 — links unchanged (/company/contact + mailto); T4 — design intact (only #F2C94C/#2E2822 tokens; no icon-libs/emoji); T6 — diff landing/ + session only. CLAUDE.md gained the **final truth-language rule** (public claims scoped/sourced/registry-gated; no absolute trust language unless legally approved; illustrative numbers labelled-as-example or removed; legal-doc defined-term carve-out). **⚠️ FLAGGED FOR FOUNDER/LEGAL SIGN-OFF (NOT touched — out of scope, backend-coupled, T6 guard):** the full-site render grep found "No-Data Guarantee" still in 2 legal docs — `docs/legal/DPA_template_sv.md` (a legally-scoped DEFINED TERM — the T2 carve-out; read by backend `scripts/build_dpa_pdf.py`) + `docs/legal/SECURITY_CHECKLIST.md` ("No-Data Guarantee; data never leaves the Node" in the SOC2/ISO readiness checklist). Both render on /legal/* and need legal review/scoping in the consolidated pre-publish sign-off. **PUBLIC SITE IS NOW TRUTH-CLEAN (SITE-A→E + TRUTH-1→3).** NEXT: APP-DISCOVERY (the node app) after the truth cleanup. [Quality & correctness #1.]

- TRUTH-2 Pricing page enterprise-safe + registry-gated [done — PR open, awaiting Jacob review; do NOT auto-merge]: TRUTH-SWEEP fix-batch 2 of 3. **Founder decision (Jacob, LOCKED): ABN is NOT publicly launched**, so /pricing must not lock ABN into exact prices, agent limits, connector availability or a free-plan promise unless founder-approved. Rewrote `landing/app/pricing/page.tsx` to **4 enterprise-safe plans** — Private Pilot (first controlled deployment, onboarding, DNA mapping, initial agent blueprints) / Professional (small teams, a limited number of controlled agents on approved systems) / Business (multi-department, expanded controlled agents, approval workflows, integrations, audit) / Enterprise (regulated environments, custom governance, security review, deployment support, advanced compliance). Every price slot renders `renderClaim('pricing.plan.<slug>.price')` → **"Contact us"** (no exact price renders until a registry entry is `founderApproved` + `approved`). Agent capacity is QUALITATIVE (Limited / Expanded / Custom capacity — no numbers); the comparison table is qualitative (Best for / Controlled agents / Connectors / Governance & audit / Support / Deployment); the connector FAQ → `getClaimFallback('pricing.connectors_availability')`; the GDPR FAQ is scoped (No-Data egress, never "we never see"). CTAs → /company/contact ("Request a pilot" / "Contact us" / "Request a deployment review"). **Removed:** the €299 tier, "Starter Free"/"Free forever"/14-day trial/"Start free →"/"Start trial"/"Talk to sales", the 14-connector "out-of-the-box" FAQ, the "Most popular" badge + featured gold tier, the exact-number comparison table, "Prices excl. VAT", "we never see the data". Registry (`lib/publicTruthRegistry.ts`) extended: `pricing.model` + the 4 `pricing.plan.*.price` (founderApproved/needsReview → "Contact us") + `pricing.free_plan` (blocked) + `pricing.connectors_availability` (planned/needsReview) + `pricing.professional_eur` flipped to blocked (€299 historical record). **DESIGN PRESERVED** (v7/DetailPageShell byte-identical — only copy/claims); no new components/colors/icon-libs/emoji. landing `npm run build` (46) + tsc GREEN; landing/ ONLY. T2 — prerendered /pricing shows the 4 plans + "Contact us" + "Request a deployment review"; €299 / "Up to 25 agents" / "Free forever" / "Start free" = 0 in the render (those strings survive only in the registry's blocked records). **⚠️ NEEDS JACOB (before publishing):** confirm the 4 plans + the deployment-review pricing model; **real prices are set at go-live** when the deployment/operating cost is known + founder-approved (flip the `pricing.plan.*.price` / `pricing.model` registry entries to `founderApproved` + `approved` to render them); connector availability stays the scoped registry fallback until real connectors are live. NEXT: TRUTH-3 (§10.1 over-absolute sweep: observer/about/autonomous-engine). [Quality & correctness #1.]

- TRUTH-1 Public Truth Registry + hard fabrications removed [done — PR open, awaiting Jacob review; do NOT auto-merge]: TRUTH-SWEEP fix-batch 1 of 3. Built the permanent **Public Truth Registry** (`lib/publicTruthRegistry.ts`) — the "smart algorithm" that matches every public claim to its real source (`measured`/`backend`/`legal`/`founderApproved`/`planned`/`unknown`) + approval, and renders ONLY real + approved data via `canRenderClaim` / `renderClaim` / `getClaimFallback`. Secrets / internal thresholds (§16) never appear in `displayText`; the module is data + pure helpers (no runtime backend call — the static site can't couple to a private service). Removed the 3 hard fabrications the TRUTH-SWEEP found: (1) **FigOpera "2.4 € / RUN"** (homepage) → registry-gated code-true "OPERA LOOP"; the pentagon + 5 vertices (O/P/E/R/A) are byte-identical. (2) **/status** — the hardcoded "99.9 % uptime", "0 incidents" and all 7 components "Operational" were fabricated (the page is static, no live monitoring) → honest "monitoring is being prepared" + a neutral "Monitoring planned" chip; uptime/incidents render registry fallbacks; the methodology section (definitions) + cross-links kept. (3) **/changelog** — the fabricated v1.1.0 (no such release), wrong dates, "10 system connectors" (no live connectors) and "Full dark theme" (stale — v7 light replaced it) → only the gh-verified releases render (version + date + title; `VERIFIED_RELEASES`), detailed per-release notes gated behind `changelog.detailed_notes` (needsReview). **DESIGN PRESERVED** (v7, pentagon/5-phases, figures byte-identical — only claims/copy changed); no new components/colors/icon-libs/emoji. landing `npm run build` (46) + tsc GREEN; landing/ ONLY. **⚠️ NEEDS JACOB (changelog real releases):** evidence via `gh release list` — the SOURCE repo (abn-systems/ABN) has v1.0.0 (2026-05-22 "Desktop installer") + v1.0.1 (2026-05-30 "First complete working installer", latest); **v1.1.0 does NOT exist**; the PUBLIC mirror `abn-systems/abn-releases` is **EMPTY** (so /company/download honestly shows "preparing" — no public installer yet). Jacob to (a) confirm these are the real public-facing releases, (b) supply real per-release notes if a detailed log is wanted, (c) decide when to publish the installer to the mirror. PERMANENT RULE added to CLAUDE.md: no public metric/status/claim is hardcoded — add it to the registry first. NEXT: TRUTH-2 (pricing connector-list + "we never see the data" + €299, via registry) → TRUTH-3 (§10.1 sweep: observer/about/autonomous-engine). [Quality & correctness #1.]
- SITE-E agent catalog + developer-API reframe [done — PR open, awaiting Jacob review; do NOT auto-merge]: **THE LAST PUBLIC-SITE BATCH — after merge the public site (SITE-A→E) is content-complete.** PART 1 (/agents, net-new): a catalog-only page (app/agents/page.tsx + lib/agents.ts — the lib/industries/processes precedent, NOT a [slug] route) listing the 12 §8 example roles (Invoice Auditor / Schedule Planner / Delivery Monitor / Subcontractor Audit / Anomaly Monitor / Contract Renewal / Compliance Evidence / Document Intelligence / Supplier Risk / Reporting Preparation / Customer Operations / Maintenance Workflow), each a card (name + archetype badge + role + what-it-does + a link to its process page). FRAMING LAW upheld (ABN is a generator, not a catalogue): the page states the roles are EXAMPLES the Blueprint Generator creates; the AgentRole TYPE excludes EXECUTOR (`Exclude<Archetype,'EXECUTOR'>`) so no example can be a live executor; EXECUTOR is shown only as "planned · V2 — not available today". The §8 universal contract (does-not-do / inputs / outputs / approval gates / safe-failure) is stated ONCE (not per card). §14 AgentEngine pattern, DetailPageShell. Nav: Footer "Agent catalog" + a Products-dropdown "Agent catalog" entry → /agents. PART 2 (/api, honest reframe): Jacob confirmed NO live public v1 API → removed every callable claim that would 404 (the runnable curl/Python/TS Quickstart samples, "Get your API key"/"generate a key from the dashboard", "no SDK required"); added an early-access banner; kept the §11.2 integration + security model, the §11.3 webhook wording, §11.4 "SDKs optional"; the endpoint reference + example payload remain ONLY under "Planned API surface — not callable today"; CTAs → "Request early access" (/company/contact). landing `npm run build` (46 pages) + tsc --noEmit GREEN; link audit clean (Products+Footer → /agents; agent cards → existing /solutions/* process pages; no 404, no callable /v1). landing/ ONLY — no frontend/ (node app), no backend, no migration. **Fact-based law upheld: the fabricated callable API is removed/reframed, not relabelled.** NEXT: the public site is done — remaining is go-live (Jacob, last): real connectors after the org-nr, a real public API if/when built, prod secrets, EV cert, the release; + the node-app agent-icon batch (separate). [Sign-off list consolidated above — incl. the API now resolved-as-early-access.] [Quality & correctness #1.]
- SITE-D homepage Hero + trust/legal + legal-center [done — PR open, awaiting Jacob review; do NOT auto-merge]: the highest-stakes copy on the site is now fact-based + scoped. (1) Hero (D8) → §9: H1 "The controlled agent operating system for business operations." + the §9 lede; the 3 trust metrics replaced with the §9 qualitative points ("Local-first by design" / "Fast value, slow certainty" / "Logged agent runs") — the 72h/00%/05/5 NUMBERS are GONE (the 72h was a non-code-true onboarding figure; fact-based law). (2) D6 LedgerSection → REMOVED entirely (the fabricated "284 400 € … actually returned to your accounts" ROI + the breakdown numbers) and its slot replaced with the §9 "Built for every company with operational work" section (new ScopeSection.tsx, git-mv'd from LedgerSection.tsx — same v7 idiom, NOT a new design primitive). (3) TrustSection → D1 eyebrow scoped ("we can't look" gone), D2 founder quote = Jacob's EXACT approved line, D3 "SOC 2 attestation" → "SOC 2 Type 1 readiness" (target unchanged). (4) OperaSection D7 → the "95 % of plans match" fabricated rate replaced with the code-true "skips the LLM when confidence is high enough"; the 5-phase rows + the FigOpera pentagon are BYTE-IDENTICAL (locked constraint confirmed, untouched). (5) /transparency → "we never see your data" + "we never store your data" softened to scoped, AND the §15 8-step "How ABN protects customer data" folded in on the existing page (DetailPageShell + NumberedStep) — no new page (rule #1). (6) legal-center → lede "data never leaves" softened to scoped; restructured to the §15 4 categories (Trust Center / Data Protection / Compliance / Reliability) via an extended lib/legal.ts model (LEGAL_CATEGORIES: doc → /legal/<slug>, link → existing page, or page-less text; NO invented documents; existing /legal/[slug] URLs unchanged). PLUS 2 extra §10.1 softenings the T4 grep surfaced on adjacent trust pages (about + subprocessors "customer data never leaves" → scoped). landing `npm run build` (45 pages) + tsc --noEmit GREEN. landing/ ONLY — no frontend/ (node app), no backend, no migration. **Fact-based law upheld: the fabricated Ledger ROI and the "95 %" rate are removed (not relabelled).** NEXT: SITE-E (Developers area + agent catalog at /agents). [Sign-off list consolidated above.] [Quality & correctness #1.]
- SITE-C process pages [done — PR open, awaiting Jacob review; do NOT auto-merge]: all 8 process pages now live at /solutions/[slug] — the 5 existing rewritten IN PLACE to the spec §7 final copy + 3 new added (contract-renewal, compliance-monitoring, document-intelligence), each on the existing DetailPageShell with ONE §14 signature pattern (ABN's own illustrations, ZERO COPY): invoice-management→ROILedger, scheduling→ConfidenceGate, delivery-monitoring→Observer, subcontractor-audit→TrustLayer, exception-handling(Anomaly detection)→PatternLibrary, contract-renewal→OperaLoop, compliance-monitoring→CultureRules, document-intelligence→TokenFlow. /solutions hub + the "By process" nav now list all 8 (the §4.3 process library); lib/industries.ts processLinks widened onto the 3 new pages (financial-services +contract-renewal +compliance-monitoring; healthcare +compliance-monitoring +document-intelligence; construction +document-intelligence; manufacturing +compliance-monitoring — each maps to a §6 workflow). Link audit: every nav/footer/page link resolves, no 404, no /agents. landing `npm run build` (45 pages) + tsc --noEmit GREEN. landing/ ONLY — no frontend/ (node app), no backend, no migration. **⚠️ FACT-BASED HONESTY FIX (Jacob's explicit instruction "nothing invented"):** removed the fabricated pilot metrics (94% / 2.6s / €8,000–45,000) from the old invoice page — they were NOT spec-sourced and §19 flags pilot-metrics; the rewritten pages carry only §7 ABN copy. **⚠️ FOUNDER/LEGAL SIGN-OFF BEFORE PUBLISHING (softened per §7/§10/§19, NOT invented):** (1) Compliance monitoring (/solutions/compliance-monitoring) — "ABN supports compliance workflows. It does not guarantee legal or regulatory compliance." (2) Document intelligence (/solutions/document-intelligence) — "ABN must not claim to provide legal, medical, financial or regulated advice. It prepares structured summaries and evidence packages for qualified human review." (3) Anomaly detection — "ABN flags anomalies as evidence-backed signals, not final truth." **⚠️ ARCHITECTURE DECISION (flagged for review):** the existing 5 process pages were individual page.tsx files; I consolidated to a /solutions/[slug] dynamic route + lib/processes.ts data module (the SITE-B /industries + legal/[slug] pattern) rather than write 8 near-identical individual files — this honours rule #1 (no-duplication, a CLAUDE.md LAW) and makes /solutions consistent with /industries; the /solutions/<slug> URLs are UNCHANGED (zero breakage). The batch said "follow the existing individual-file pattern, do NOT re-architect" — per CLAUDE.md's meta-rule (a prompt that would cause a rule violation → follow the rule + flag), I deviated and am flagging it. If you'd rather keep individual files, say so and I'll refactor. NEXT: SITE-D (home Hero + trust/legal + legal-center 4-cat + plain-language trust page), SITE-E (Developers + agent catalog at /agents). [Carry-over: SITE-A/SITE-B softened legal/No-Data claims + the org-number gap (559XXX removed from footer + subprocessors; real reg.no needed) still await Jacob.] [Quality & correctness #1.]
- SITE-B industry pages [done — PR open, awaiting Jacob review; do NOT auto-merge]: the core IA bug (industry aliased to a single process) is FIXED — the public site now has real sector pages. Created the All-Industries hub at /industries + the 6 sector pages at /industries/[slug] (a dynamic route mirroring legal/[slug], backed by the new pure data module lib/industries.ts — the lib/legal.ts precedent, NOT a new component), all built on the existing DetailPageShell with ONE §14 signature pattern each (ABN's own in-repo illustrations, ZERO COPY): financial-services→NoDataBoundary, healthcare→ConfidenceGate, construction→ROILedger, logistics→PatternLibrary, ecommerce-retail→Observer, manufacturing→OperaLoop, hub→ProcessGraph (§14 explicit for "All industries"). Copy is the §6 final public copy (scoped No-Data + controlled-agent/human-approval/evidence framing); each page links to the RELEVANT existing process pages (the correct many-to-many mapping, not the old 1:1 alias) + "See all processes" → /solutions. Repointed the "By industry" links in SiteHeader (the mega-menu) + Footer (Solutions column) off the process pages onto /industries/[slug]; "By process" stays on the process pages; All industries → /industries. Link audit: every nav/footer/page link resolves, no 404, no /agents link. landing `npm run build` (33 static + /industries + 6 SSG slugs) + tsc --noEmit GREEN. landing/ ONLY — no frontend/ (node app), no backend, no migration; the process pages unchanged beyond being link targets. **⚠️ FOUNDER/LEGAL SIGN-OFF BEFORE PUBLISHING (softened per §6/§10/§19, NOT invented):** (1) Healthcare (/industries/healthcare) — the whole sector page is regulated; it renders ONLY the §6.2 safe wording ("ABN supports operational workflows around healthcare delivery. Clinical decisions, patient treatment and regulated medical judgment remain with qualified human professionals") and never the forbidden claims (no diagnosis / no medical advice / no replacing clinicians / no autonomous patient-care / no guaranteed regulatory compliance). (2) Financial Services (/industries/financial-services) — "risk and compliance", "regulatory reporting preparation" framing (honest: ABN prepares findings, named employees approve). (3) Manufacturing (/industries/manufacturing) — the §6.6 boundary "not described as directly controlling machines, safety-critical equipment or production decisions unless that exact capability has been built, certified and governed" (uses the word "certified" in a limiting sense — §19 flags it). NEXT: SITE-C (process pages — rewrite the 5 in place + add Contract renewal / Compliance monitoring / Document intelligence; then the industry processLinks can be widened), SITE-D (home/legal), SITE-E (Developers + agent catalog at /agents). [Health-sweep this batch: the industry pages can only link to the 5 existing process pages today — Contract renewal / Compliance monitoring / Document intelligence (referenced in §6 finance/healthcare workflows) don't exist yet, so those workflows are listed as text but link to "See all processes"; SITE-C closes this.] [Quality & correctness #1.]
- SITE-A public global chrome [done — PR open, awaiting Jacob review; do NOT auto-merge]: brand mark → the interactive "/"-symbol (new AbnMark.tsx client component, exact brand-HTML glyph geometry g-slash/g-b/g-n + terra dot, / → B → N → / reveal on hover/click, prefers-reduced-motion static "/", currentColor stroke so it reads on the light header AND the dark footer) swapped into SiteHeader + Footer + DetailPageShell + sign-in + sign-up + legal/[slug] + subprocessors; the old landing ABNFlower.tsx DELETED (node-app frontend/src/components/ABNFlower.tsx untouched). Nav restructured (Products dropdown §4.2 with page-less items as non-link text; Solutions split "By industry"/"By process"; no-broken-state — "By industry" points at the CURRENT process pages, NO /industries/* or /agents link). Footer rebuilt to the 5-col §4.4 taxonomy (page-less items plain text; removed the 🇸🇪 emoji + the 559XXX placeholder + the stale "autonomous backoffice" tagline → "The controlled agent operating system for business operations"; fixed the dead /#marketplace anchor → plain text). §17.3/§12.4 phrase scrub in the chrome + claim files (Hero subline + 3 trust metrics, observer, security, solutions hub ×5 + the 5 solution pages, invoice-management feature card, subcontractor metadata, layout title/OG metadata, pricing GDPR FAQ) using the scoped No-Data truth (egress, not local-DB). landing `npm run build` (33 pages) + `tsc --noEmit` GREEN; git diff is landing/ ONLY; backend/node-app/services untouched. **⚠️ SIGN-OFF NEEDED BEFORE PUBLISHING (softened now, not invented — list per the SITE-A mandate):** (1) company/security "That commitment is not just architectural — it is measured" (was "guarantee"); (2) invoice-management feature card "Designed for compliance" + "built to support Swedish accounting-law and GDPR requirements. Exact legal terms depend on your deployment and agreement." (was "Accounting-law safe" + "compatible … without an extra DPA"); (3) pricing FAQ "Is ABN designed for GDPR?" — "built for GDPR by architecture … Exact legal terms depend on your deployment and agreement." (was the absolute "Yes — by architecture … never leaves … without any cross-border transfers"); (4) scoped No-Data wording now in Hero subline + 00% metric + observer step + layout description/OG ("raw operational data is designed to stay inside your environment in normal use" / "ABN Platform AB does not receive raw values; the customer node processes approved fields locally") — Jacob to confirm the exact legal phrasing. **ORG-NUMBER GAP:** the 559XXX-XXXX placeholder was REMOVED from the footer + the subprocessors legal page; a REAL Swedish reg.no must be added on both before publishing. NEXT: SITE-B (6 industry pages + All-industries at /industries/[slug], repoint the "By industry" nav off the process pages). [Quality & correctness #1, no calendar pressure.]
- AUTH-3b read gating [done — PR open, awaiting Jacob review; do NOT auto-merge]: the auth foundation is COMPLETE — every GET/read NOT on the anonymous allowlist now requires an authenticated user (require_auth, VIEWER+). Applied two ways, both intrinsic to the router: ROUTER-LEVEL APIRouter(dependencies=[Depends(require_auth)]) on the 15 routers with no public route (proposals/agents/connectors/connector_generator/reports/tenants/transparency/delivery/tenant_settings/dna_phase/friday_report/roi/agent_memory/onboarding/mind — also covers the 2 read-like POST soft flags preview/inquire; per-route require_role on writes still applies on top, get_current_user request-cached); PER-ROUTE require_auth where the router has an intentionally-anon route (gdpr records+retention [LEAVE /transparency — public legal declaration]; flows /runs+/runs/{id} [LEAVE /flows/health]; billing /subscription [LEAVE /webhook]; main.py /api/status [LEAVE /]). Soft flags hardened: /api/health/detailed (keep /health anon), connectors /catalogue + connector_generator /generate/preview, reports /{id}/download → require_auth (docstrings corrected). The LOCKED anonymous allowlist = exactly 6 GETs (/ , /health, /api/flows/health, /api/deployment/version, /api/auth/setup-needed, /api/gdpr/transparency) + the POSTs (auth login/refresh/setup, the 3 signature-verified webhooks) — a ground-truth route scan enforces it (6 ungated GETs, all intentional). 403≠401 unchanged (a VIEWER passes every read — no over-gating). Backend 2025→2032 (+7: tests/test_auth3b_read_gating.py — a STRUCTURAL none-missed guard over the REAL app + 401/200/soft-flags/allowlist). Extends-not-breaks: read-only test files got the get_current_user fixture; opt-in discipline preserved (gdpr /transparency stays public so its test keeps the real path). No migration, No-Data. THE PERMANENT JWT AUTH FOUNDATION (AUTH-1 bootstrap + AUTH-2 login + AUTH-3a writes + AUTH-3b reads) IS DONE — same on desktop + server, no localhost bypass, the server-tier (0.0.0.0) un-authenticated exposure fully closed. NEXT: V1 build is COMPLETE (the V1 promise is code-true AND auth-enforced); remaining = #8 the go-live ops chapter (Jacob by hand, live last: Hetzner alembic → prod secrets [JWT + node_signing_key + the webhook secrets — non-placeholder] → Stripe/OAuth → EV cert → Tauri verify → cut the Release last; Fortnox after the org-nr) + the deferred 🟡/🔴 backlog (role-aware UI, stay-logged-in, WebAuthn MFA, the action-type taxonomy, UTC-sweep, the 🔴 families, the Self-Improvement Kernel). [Health-sweep this batch: (1) `/` root + `/api/status` were app-level (main.py) reads neither 3a nor the 3b inventory listed — kept `/` anon (node-identity probe, no customer data) + gated `/api/status` (internal scheduler/module status, like /health/detailed); flag for Jacob to confirm `/` should stay anon. (2) gdpr /transparency is a deliberate PUBLIC legal declaration (docstring "no auth required") — kept anon (not customer data); the 3a/3b inventory's "gdpr reads" generalization didn't catch it. (3) mind /reports list+detail were ungated reads (not in the inventory) — gated via router-level require_auth. None block AUTH-3b.] [Quality & security #1, no calendar pressure.]
- AUTH-3a node-wide WRITE gating [done — PR open, awaiting Jacob review; do NOT auto-merge]: the headline un-authenticated WRITE exposure is CLOSED — require_role(AGENT_MANAGER) on every write/action (proposals approve/reject; agents run/pause/resume/settings/simulate/instruct/generate; connectors create/patch/sync/connect/authorize/callback/api-key/configure; flows run; reports deliver; delivery config/test; tenant_settings; dna_phase start/approve; friday_report send; onboarding start/1-5/preflight) + require_role(NODE_ADMIN) on destructive/sensitive (connectors delete x2; tenants create/patch/delete; gdpr export/erase/verify-payload; agents rollback; billing checkout/portal) — 43 routes, mirroring the proven email_drafts pattern. Real decided_by from the authenticated user on proposals (current.email or current.user_id; the spoofable body field DROPPED). Anonymous allowlist LOCKED (auth login/refresh/setup/setup-needed; /health + /flows/health [the pre-login banner — no-lockout]; the Stripe/Slack/Teams signature-verified webhooks [their own auth, not double-gated]; /deployment/version). 401≠403 preserved (interceptor acts only on 401). onboarding CONFIRMED no pre-login caller → gated. The human-approval gate is now auth-enforced. Backend 2016→2025 (+9: tests/test_auth3a_write_gating.py — a STRUCTURAL none-missed guard [every write route has get_current_user, the allowlist does not] + 401/403/200 + decided_by-from-user + webhooks-not-401). Extends-not-breaks: ~23 existing write-test files got the get_current_user fixture (inject NODE_ADMIN; role-tiering asserted in the new file); the test_remaining_routers_api gdpr tests opt in via as_admin=True so the /me tests keep real auth; the one direct-call test passes db by keyword. No migration, No-Data. NEXT: AUTH-3b (require_auth on ALL GET reads across ~15 routers — proposals/agents/connectors/reports/flows/transparency/gdpr/roi/agent_memory/delivery/tenant_settings/friday_report/tenants/billing reads + onboarding GET — and the soft flags: /api/health/detailed, connectors /catalogue + /generate/preview → require_auth) = the FINAL auth-foundation step. Deferred 🟡: role-aware UI (hide actions a role can't do — the calm 403 floor exists); stay-logged-in (secure Tauri plugin); WebAuthn MFA. [Health-sweep this batch: (1) proposals docstrings said "ANALYST+" but the role model + plan say AGENT_MANAGER — tightened to AGENT_MANAGER in the new code (the docstrings updated). (2) gdpr :188 is /verify-payload (a dev PII-check tool labelled "ADMIN+"), NOT "rectify" as the plan said — gated NODE_ADMIN per the plan's line reference (safe; tighter than its stale docstring). (3) reports /{id}/download is a read (AUTH-3b) but serves report FILES — confirm the path-traversal guard stays the protection there. (4) /api/health/detailed leaks internal component status anonymously today → fold into AUTH-3b. None block AUTH-3a.] [Go-live secret checklist: stripe_webhook_secret/SLACK_SIGNING_SECRET/TEAMS_VALIDATION_TOKEN + JWT secret + node_signing_key must be non-placeholder.] [Quality & security #1, no calendar pressure.]
- AUTH-2 frontend login [done — PR open, awaiting Jacob review; do NOT auto-merge]: the operator can log in — a login screen + AuthProvider (in-memory tokens ONLY, the securest choice — no localStorage/disk/plugin; re-login on restart) + the axios interceptor (one seam in client.ts: attach Bearer, 401→refresh-once→retry→else clear+login, loop-safe via _retry + auth-path exclusion + single-flight) + logout (revokes /logout + clears) + the SetupScreen first-operator flow (gated on /setup-needed → /setup → auto-login since SetupResponse has no tokens) + the AuthGate app guard (unauth → setup(if needed)/login, error→login safe default; auth → app), design system + Voice Swedish, calm error copy (401/403-locked/429). Frontend-only, gates nothing (main works end-to-end setup/login→app), no backend, no migration, No-Data. Frontend 99→114 (+15: lib/auth.test.tsx T1/T2/T2b/T2c/T3/T6 via a dependency-free axios mock adapter; AuthGate T4/T4b/T5/T5b; LoginScreen T7/T9); typecheck + build ✓; backend 2016 unchanged. NEXT: AUTH-3 (node-wide require_role per the role-per-route plan: approve/reject=AGENT_MANAGER, destructive=NODE_ADMIN, reads=require_auth; connectors ×10 / onboarding ×7 / reports / flows / agents writes too; decided_by from the authenticated user; closes the 0.0.0.0 server-tier exposure; verify each UI call tolerates a 401→login redirect). Deferred 🟡: stay-logged-in (a secure Tauri store plugin — layers ON in-memory, never localStorage); WebAuthn MFA on login; shorter access token + silent-refresh tuning. [Health-sweep this batch: (1) bundle is one 1.03 MB chunk — a pre-existing Vite warning, not introduced here (future code-split). (2) the in-memory posture means a page reload/Tauri restart drops the session → re-login (accepted, secure V1 behaviour). (3) AUTH-3 must confirm each existing data query tolerates a 401→login redirect once gating bites.] [Quality & security #1, no calendar pressure.]
- AUTH-1 first-operator bootstrap [done — PR open, awaiting Jacob review; do NOT auto-merge]: a fresh node can create its first operator securely — fail-CLOSED zero-users POST /api/auth/setup (+ GET /api/auth/setup-needed) + the server create_admin.py CLI, both via the single auth/bootstrap.py::create_first_operator (first NODE_ADMIN, operator-chosen bcrypt-12 password, tenant-linked [first existing Tenant else create the node Tenant], race-safe via the fixed BOOTSTRAP_USER_ID unique guard → 409, 403 once a user exists, fail-CLOSED on error → 500, password never logged/returned, no default/bypass). Reuses accept-invite's User-creation + password.py; gates NO route (main works); no migration (auth tables exist). Backend 2009→2016. STEP 1 of the permanent JWT auth foundation (A). NEXT: AUTH-2 (frontend login: login screen + secure token storage [in-memory access + secure refresh; httpOnly cookie for a hosted web tier] + axios request/response interceptor [attach token, 401→refresh] + logout + the OnboardingWizard setup step consuming /setup, design system + Voice Swedish). THEN AUTH-3 (node-wide require_role: approve/reject=AGENT_MANAGER, destructive=NODE_ADMIN, reads=require_auth; decided_by from the authenticated user; closes the connectors/onboarding/reports/flows/agents 0.0.0.0 exposure). Order = the no-broken-state guard (login before gating). Security notes (go-live): JWT secret + node_signing_key must not be the "change-me" placeholder; 8h access token is generous (post-foundation tuning).
- Tuari-3 Approval Center [done — PR open, awaiting Jacob review; do NOT auto-merge]: the human-approval surface is live — per-proposal Trust Cards on ProposalsPage ("Agenten vill: <action>" / Varför / Bevis (the embedded Evidence Ribbon) / Uppskattat värde / Risknivå / Godkänn + Avvisa with a REAL required reason prompt, replacing the hardcoded one), built from the design system, curated WHAT-not-HOW, Voice Constitution Swedish, calm. Thin client helpers (getProposal+ProposalDetail, run_id on the FE Proposal type, proposalRiskLabel) over existing endpoints; approveProposal(id, note?) gained the optional note; DashboardPage approve mutation wrapped for the new signature. Frontend typecheck ✓, vitest 99 ✓ (+8), build ✓; backend unchanged (2009). The THREE Tuari trust surfaces (Evidence Ribbon → Mission Control → Approval Center) are COMPLETE — the customer can see per-run proof, the fleet overview, AND approve/reject agent proposals. NEXT: re-check the V1 plan; remaining = #8 ops-todos (Hetzner alembic stamp, Stripe live, OAuth client creds, EV cert, Tauri install click-test, the release — DEFERRED per Jacob: build everything first, go live last as a ~1-week block). Deferred 🟡/🔴: RBAC/identity (who-can-approve / dual-approval / assignee / decided_by — approveProposal posts no decided_by, ApprovalRecord defaults "node-operator"); tier-3 live write-back (V2); a backend-exposed per-proposal severity (one-source risk); the single action-type taxonomy (3-place enum); UTC-sweep; the dead attr-extraction branch tidy; the Self-Improvement Kernel (AFTER-V1, full spec logged).
- Bridge-4 Proposal action_type [done — PR open, awaiting Jacob review; do NOT auto-merge]: a tier-2 Proposal now carries the REAL proposed action_type (e.g. schedule_change) from the signed blueprint's PROPOSE_CHANGE step, not the defaulted agent_pause. Root cause: the runtime AgentBlueprint dataclass has no steps[] (models capabilities) → live path defaulted. Fix 2.1a: from_dict-only proposed_action_type field on AgentBlueprint (the ral_required_fields precedent — NOT in to_dict/sign, canonical signature byte-unchanged), mapped via the existing _PROPOSE_ACTION_MAP; safe agent_pause default kept as the genuine fallback; the live U_notify notification gate input is now accurate (a correction — thresholds/weights unchanged, suite verified). No schema/approval/threshold/tier/signature change; no migration (Proposal.action_type column exists). Backend 2003→2009. NEXT: the Approval Center (the LAST Tuari surface — now with CORRECT Proposal data: the Trust Card showing the true "the agent wants to <action> — why — evidence — impact — risk", approve/reject, from the design system, curated WHAT-not-HOW, Voice Constitution Swedish). Deferred 🟡: tier-3 live write-back (V2); the dead attr-extraction branch tidy; a single action-type taxonomy consolidation (3-place enum, the one-source-of-truth lesson); UTC-sweep; the Self-Improvement Kernel (AFTER-V1).
- E2E-3 tier-2 PROPOSE proof [done — PR open, awaiting Jacob review; do NOT auto-merge]: the human-approval half of the V1 promise is PROVEN end-to-end — a tier-2 PROPOSE agent (canonical generator, scheduling read→analyze→propose graph) runs through every gate to success with an earned confidence ≥0.50 and produces a real Proposal(pending); takes NO risky action without approval (RollbackRecord count==0, tier==2 cannot auto-execute, tier-3 EXECUTE TierBlockedError — fail-CLOSED, structural); and the pending→approved transition works via _record_decision + an immutable ApprovalRecord (approved→write-back EXECUTE = honestly-deferred tier-3/V2 boundary, NOT asserted). ADDITIVE — a test only, no production change, no migration. Backend 1999→2003. BOTH halves now proven (E2E-2 tier-1 prove-every-value + E2E-3 tier-2 never-act-on-risk-without-approval). NEXT: Approval Center (the last Tuari surface, now UNBLOCKED — real Proposals to render: the approval UX — Trust Card with action/why/evidence/impact/risk, approve/reject, built from the design system, curated WHAT-not-HOW, Voice). Deferred 🟡: tier-3 live write-back (V2); Fas-3 quarantine tile; the Self-Improvement Kernel (AFTER-V1).
  - Health-sweep (E2E-3): (1) Bridge-3 carry-over — Proposal action_type=agent_pause (safe default) not schedule_change because AgentBlueprint.to_dict() drops steps[] (its own small batch — carry steps in to_dict OR _derive_action_type read capabilities). (2) repo-wide datetime.utcnow() DeprecationWarnings (cosmetic; a future UTC-sweep batch). (3) CHAT_LOG.md is behind on per-batch ## headings (S2F1-2 … Tuari-1 records in CLAUDE.md lack CHAT_LOG headings) — a doc-hygiene backfill, not code.
- Bridge-3 propose vocabulary bridge [done — PR open, awaiting Jacob review; do NOT auto-merge]: the fourth root-hole is fixed — a tier-2 PROPOSE agent now produces a real Proposal(pending). propose.* maps to its domain-correct real capability at generation before signing (scheduling.propose_schedule / generate_dispute_letter; draft_email agnostic; logistics→honest no-op) so the execute phase RESOLVES → run success → save_proposal_from_run fires; the run carries a real EARNED confidence from observed events (≥0.50) so the commitment gate returns save_proposal. Weakens no gate (a no-evidence run still escalates → no Proposal; tier-3 EXECUTE stays TierBlockedError). generator.py-only (+46/-3); no confidence-path/threshold/model/tier change; no migration. Backend 1992→1999. NEXT: E2E-3 (tier-2 PROPOSE full-chain proof: ProcessGraph → signed tier-2 PLANNER → run success → findings → a real Proposal(pending) + audit + the negative no-action-without-approval assertion + the pending→approved transition; approved→write-back EXECUTE = honestly-deferred tier-3/V2 boundary). THEN Approval Center (the last Tuari surface, now unblocked — real Proposals to render).
  - Bridge-3 follow-up (small, NOT blocking E2E-3): the produced Proposal's action_type=agent_pause (the safe default) rather than schedule_change, because AgentBlueprint.to_dict() DROPS steps[] → proposals_persistence._derive_action_type scans an empty list. The Proposal is valid+pending (agent_pause ∈ VALID_PROPOSAL_TYPES). Fix = carry steps in AgentBlueprint.to_dict OR have _derive_action_type read capabilities when steps is empty (touches the Proposal-creation path — its own small batch).
- Tuari-2 Mission Control trust strip [done — PR open, awaiting Jacob review; do NOT auto-merge]: the home screen now echoes the proof — DashboardPage (already a Mission Control) gained a trust strip showing fleet verification ("N av M körningar verifierade mot godkända system" from runs[].attestation_summary, client-side), reports-ready (a thin read-only getReports helper over the existing endpoint), and honest fleet/decision cards ("X av Y agenter aktiva"; pending only when > 0; € reframed "Potentiellt värde"). Built from the design system, WHAT-not-HOW curated at the aggregate, Swedish. Frontend typecheck ✓, vitest 91 ✓, build ✓; backend unchanged (1992). NEXT per the V1 plan: the Approval Center (the last Tuari surface — needs E2E-3 tier-2 PROPOSE first) + #8 ops-todos (Fortnox LAST/ignored for now — ABN org-nr pending). Deferred 🟡: E2E-3 (tier-2 PROPOSE family end-to-end + the approval artifact); a Fas-3 quarantine/health fleet tile (needs an AgentSummary field add — a small backend batch); a server-side aggregate/read-model for the overview; the Self-Improvement Kernel (AFTER-V1). [Release plan = compass; quality & security #1, no calendar pressure.]
- Guardrails-config PR (chore/guardrails-upgrade) awaiting Jacob review — do NOT auto-merge; Jacob checks the 6 verification exit codes + the new CLAUDE.md §4.1/4.2/4.3 + deny[] for false positives before merging.
- Manual (Jacob, GitHub UI): add "abn-security — Go build & test (45a)" as a required status check in branch protection.
- Version lockstep drift (future OPS batch): the canonical version is hand-mirrored in `frontend/src-tauri/tauri.conf.json` + `backend/core/config.py` `abn_version`; the two package.json files are drifted at 1.0.0. (Batch 74 removed the landing `DownloadView.tsx CURRENT_VERSION` mirror — the download page now reads the version dynamically from the public release, so that copy is no longer a drift risk.) A later batch could add a CI/pre-commit assert that the 2 remaining mirrors match.
- **HUMAN STEP (Batch 74): mint `MIRROR_PAT`** — a fine-grained PAT with `contents: write` on `abn-systems/abn-releases`, stored as a repo secret named `MIRROR_PAT` on `abn-systems/ABN`. Without it, `mirror-release.yml`'s public-upload step fails loudly (by design). The mirror reads the private repo with the default GITHUB_TOKEN; only the public push needs the PAT.
- Batch 74 follow-up: if `release-bandwidth-guard.yml` ever opens its "Release bandwidth approaching limit" issue, plan a CDN / object-store batch for installer hosting. The guard is a conservative proxy (no live GitHub bandwidth API), so treat the alert as a trend signal.
- Stale CI gate label (flagged by Batch 75b — NOT fixed, out of test-only scope): the ci.yml backend job `name: "Backend — 1518 tests"` and the matching branch-protection required-check key both say 1518, but the real suite is 1535 (Batch 46 added +13, etc.). The name is cosmetic — pytest never asserts the count — so CI stays green; the recommended permanent fix (per CLAUDE.md's recurring count-drift note) is to rename the job to a count-free `"Backend — tests"`, which Jacob must change in ci.yml AND the GitHub Settings required-check key in lockstep. Until then leave both untouched so the check keeps reporting. (The Batch 74 freeze-clock open item is now RESOLVED — see CHANGES.)
- Batch 64 (INFRA Task 2 CI gap-fill) follow-ups: (1) the count-free rename of the required `Backend — 1518 tests` job — SAME item as the line above; Batch 64 made the ci.yml *comment* count-free but deliberately did NOT touch the job name or the branch-protection key (lockstep rename is its own batch). (2) [SUPERSEDED by Batch 66a — CodeQL was REMOVED (GHAS not enabled); re-adding it, and revisiting Go coverage, is gated on enabling GHAS — see the dedicated "Re-add CodeQL" item below]. (3) Wire ZAP → SARIF to the Security tab (`security-dast.yml` currently surfaces via the `zap-dast` workflow artifact; native SARIF needs a converter — deferred to keep the advisory DAST job hermetic/never-red).
- Batch 65 (Task 3 adversarial gap-hardening) — KNOWN LIMITATION (surfaced, NOT a bug, NOT shipped red): the gateway regex PII scrubber (`services/abn-llm-gateway/.../pii_scrubber.py`) is plaintext-only by design, so obfuscated PII (base64 / unicode-homoglyph / IBAN) in a NON-tokenised free-text field reaches the provider in `redacted`/`full` mode — it is neutralised only in `no_data` mode (the abstractor drops all values to types). NOT a No-Data-guarantee violation: `no_data` tenants and every tokenised field are fully protected in all modes; tested in `TestObfuscatedPII`. Future hardening options for Jacob to weigh: (a) decode-then-scrub (base64/homoglyph-normalise before the regex pass), (b) an IBAN pattern, (c) document `no_data` as the recommended default for high-assurance tenants. Low urgency — defence-in-depth, not an open hole.
- Re-add CodeQL if/when GHAS (GitHub Advanced Security) is enabled — removed in Batch 66a because its SARIF upload requires GHAS (a PAID add-on on private repos, not enabled), so it was red on every PR. SAST stays covered free by Semgrep (`security-sast.yml`). The `codeql.yml` workflow is preserved in git history (fully reversible); re-adding would also revisit CodeQL Go coverage for `services/abn-security`. Future OPTION, not a todo.
- Batch 66 Task 4 — **COMPLETE** (Part 1 #75 + Part 2 PR #76, branch `feat/batch-66b-shield-runtime`). Active layer shipped: always-on monitor on the EXISTING scheduler tick, attack-SUCCESS detectors (reuse `ShieldGuardian.run_checks()`, 3 originals untouched), conservative confirmation gate, `deliver_alert` reuse (no new notifier), metadata-only `shield_alerts` persistence, fail-OPEN-on-monitor/detector-error / fail-SAFE-on-confirmed-breach monitor that SETS `Agent.quarantined`. Backend 1583→1595 (+12); SQLite migration verified, Postgres half on CI. DEFERRED — **INVESTIGATED + CONFIRMED PERMANENT** (Discovery pass, Batch 69 Phase 0.5; per Jacob): **B1c runtime leak-detector — NOT buildable as a Shield quarantine detector** (No-Data leaves no metadata artifact by design); future defense-in-depth = a separate fail-CLOSED gateway residual-PII guard OR an advisory hygiene check, neither a quarantine detector. Do NOT build/force it as a Shield detector. (Discovery detail so future Phase 0.5 need not re-investigate: the 3 candidate seams all fail — `LLMGatewayLog.had_unmapped` detects LLM token *hallucination*, not leakage; `abn_llm_calls.{raw_values,customer_data,pii}_sent` are hardcoded `0` *guarantees*, not measurements; Observer `cycle.py` `sent_to_llm=0`/`sent_outside=0` are hardcoded constants; the Batch-65 canary/no-leak check is a *pre-merge pytest assertion*, not a runtime signal. A genuine leak leaves no durable aggregate artifact by design — the exact opposite of the 3 working success-detectors, each of which keys off a structurally-impossible persisted row. Every path to a real runtime measurement breaks a guard: hold/persist raw data, read `Finding` payload columns, or add in-gateway/in-agent instrumentation. The standalone #81 doc PR was closed; this note is the canonical record.)
- Batch 66 Phase D — gate-coverage DECISION (resolved for the main path): added **gate 3** at the canonical manual-run entry `POST /api/agents/{id}/run` — a quarantined agent's manual run is refused with 409 at the safe boundary (ALL tiers, not just tier-3). So: gate 1 (scheduler) stops scheduled runs, gate 2 (tier-3 pre-flight) refuses privileged writes, gate 3 (manual-run API) refuses manual runs. ~~STILL DEFERRED~~ **CLOSED by Batch 72b**: the conversational `/instruct` `run_now` path is now gated (via the unified create-gate at the top of `OPERARunner.run()` + a UX wrapper), and the single unified gate inside `OPERARunner.run()` is built (Option A). NOTE: 72b's unified gate is for `pending_human_approval`; the Batch-66 *quarantine* gates remain replicated as before. A future consolidation of quarantine ALSO into the run() boundary is a clean follow-up (not forced — quarantine's existing placement is deliberate + proven).
- **Batch 67b — Task 6 Part 2 (the autonomous write path) — DONE (PR open, awaiting Jacob review; Task 6 COMPLETE).** Shipped: the all-conditions `mind_autonomy.autonomy_allowed` gate (platform ∧ tenant ∧ approved policy ∧ clamp ∧ Accept(B)-post-change ∧ not-quarantined, fail-CLOSED), the bounded `apply_autonomous_threshold_adjustment` write (out-of-band = refusal; RollbackRecord committed ATOMICALLY with the AgentSettings write via the existing `_handle_set_threshold`; metadata audit in `RollbackRecord.new_state`; reversible via new `_rb_mind_adjust_confidence_threshold` in `_ROLLBACK_DISPATCH`), the `MindAgent.run_autonomous_adjustments` consumer (weekly runner only, tightens-only, safe direction), and `mind_runner.py` wiring (fail-soft). **Both Part-1 design problems RESOLVED:** (1) Accept(B) ceiling → new public `ABNMetaAgent.accept_candidate(blueprint_dict)` builds the POST-change blueprint via `_build_blueprint` + runs `accept()` over the 8 V_GLOBAL invariants, required True before any write (admin opt-in cannot override the constitution); (2) "dry-run an adjustment" → new small `simulate_threshold_adjustment` (before→after + clamp + honest directional projection, `effect_modelled="directional_only"`), NOT a reuse of `simulate_run`. No new schema/migration (reuses RollbackRecord/AgentSettings/Tenant.policy). 19 tests (refusal proofs + fail-closed + Accept(B) ceiling + default-off), backend 1617→1636, 0 regressions.
- **Batch 68b — Design system Part 2 (the breadth) — DONE (PR open; Batch 68 COMPLETE).** Transcribed the 48 extended motifs (`icons/extended.tsx`) + 10 principles (`icons/principles.tsx`) into the SAME registry (appended to `ABN_ICONS`, not refactored) → **80 marks total** (12 core + 48 extended + 10 principle + 10 status); + a dev gallery `pages/IconGalleryPage.tsx` at `/dev/icons` (additive route). Palette constants (no per-icon hex), kebab keys unique-verified, 80-mark smoke-render test green. typecheck ✓, vitest 74→75, build ✓. No backend/DB. **Remaining design-system follow-ups (small, NOT blocking):** (a) expose `quarantined` on the frontend `Agent` type in `api/client.ts` so `agentStatusToIconName` reads it without an explicit arg; (b) optionally adopt `<AbnIcon>` in existing views where ad-hoc status text/illustrations are rendered (pure UI polish). Status-enum note carried from 68a: "Tänker"/"Väntar" have no single backend status string — mapped from transient run-state tokens when supplied (don't invent backend statuses).
- Doc/test naming reconciliations surfaced in 67a (cosmetic, not fixed): the real Mind privacy test is `test_mind_no_customer_data_in_report` (the Batch-67 prompt + discovery called it `test_mind_no_customer_payload_in_metrics`); and `alembic check` reports pre-existing repo-wide autogenerate drift (TIMESTAMP↔DateTime, REAL↔Float, unique-index-vs-constraint across ~19 tables) — NOT introduced by 67a (the new `mind_autonomy_policies` table is absent from the diff) and NOT what `migrations-dual.yml` runs (it runs `alembic upgrade head`, which is green). A repo-wide migration-autogenerate-alignment pass is a possible future OPS batch.
- **Batch 69 — Task 5 Pulse hardening DONE** (PR open; EXTEND Batch 25, no rebuild): Issue auto-close/recovery, flap suppression (`fail_threshold` dial), unverified-URL advisory (`verified` flag — fortnox true, quinyx/hogia false), cron built-in-`GITHUB_TOKEN` (no PAT sprawl). Advisory-only + fail-OPEN + metadata-only preserved; no new table (auto-close/flap read existing `PulseResult` history). DEFERRED until LIVE connectors exist (post Task 1 — the integration layer): (a) Pulse coverage expansion beyond the 3 hardcoded TIER-3 connectors; (b) dynamic `pulse:`-block-driven coverage; (c) verify the quinyx + hogia health URLs and flip their YAML `verified: true` (today they are placeholder guesses, advisory-only). B1c No-Data-leak detector remains PERMANENTLY deferred (see the Batch-66 Task-4 OPEN ITEM above; standalone #81 doc PR closed/folded into this batch's Phase 0.5).
- **Batch 70 — image-build CI flake RESOLVED** (PR open; OPS hygiene): the `docker` ("Build & push images") job in `ci.yml` was red on main (anonymous `moby/buildkit` Docker Hub pull timing out at buildx bootstrap + `fail-fast:true` cascade). Fix = `fail-fast: false` on the matrix + `driver: docker` on `setup-buildx-action` (removes the anonymous pull; no secrets). Advisory job (NOT a required check). DEFERRED-UNLESS-NEEDED: Docker Hub auth via `docker/login-action` + `DOCKERHUB_USERNAME`/`DOCKERHUB_TOKEN` repo secrets — only warranted if the base-image (python/nginx/node) pulls inside the Dockerfiles ever start rate-limiting (the buildx-bootstrap pull, the actual failure, is gone with `driver: docker`).
- **Batch 71a — generator ↔ Accept(B) reconciliation DONE** (PR open; security-critical prerequisite). `generate_blueprint` now declares the constitutional posture in `policy_constraints` (single-source `_constitutional_policy_constraints(tier, policy)`, every line mapped to a real enforced mechanism, tier-aware, re-derived after AVM). Generated ANALYZER/T1 + PLANNER/T2 blueprints now pass Accept(B) with 0 violations (verified empirically + new regression tests — the Batch-43 gap where only hand-built fixtures were tested). Defuses the latent sweep landmine. CLAUDE.md Batch-43 drift annotated honestly. Suite 1644→1647. **NEXT: Batch 71b** — the synchronous fail-CLOSED Accept(B) sign-time gate (now safe: legitimate blueprints pass). Do 71b only AFTER 71a merges + is proven.
- **Batch 71b — Accept(B) synchronous fail-CLOSED sign-time gate DONE** (PR open). The gate sits in `generate_blueprint` after 71a's `policy_constraints` re-derive + before `sign_blueprint` (single choke: trigger auto/verified, orchestrator, API); reuses `ABNMetaAgent.accept_candidate` (no new verifier, 8 invariants unchanged). FAIL-CLOSED: violation OR checker error → `AcceptBRefusedError` (new, subclasses `BlueprintValidationError`) → not signed/persisted/run. Defense-in-depth (sweep/manual/Mind) unchanged. Tests +4 (allow-legit T2, block-violator T1, fail-closed-on-error T4, auto-path-no-run T3). Suite 1647→1651. **Accept(B) sign-gate COMPLETE (71a+71b).** Remaining Agent-Creation items unchanged (see next bullet).
- **Agent-Creation-Intelligence — remaining items (from the Discovery):**
  - **Prio 2 / 72a — DONE (PR open):** AgentNeedScore always-value decision (wires the computed ROI) + report-only fork. `decide_agent_need` → {FULL_AGENT / MONITOR_DRAFT / INSIGHT_REPORT / OBSERVED_ONLY}; report outcomes generate tier-1 READ_ONLY agents (reuse tier-ceiling); OBSERVED_ONLY surfaced. Floor + Accept(B) intact; no gate/schema/run-path change. Suite 1663.
  - **Prio 2 / 72b — DONE (PR open; Prio 2 COMPLETE).** Risk-scaled human create-gate. DECISION (Jacob signed off): **OPTION A — ONE unified gate at the TOP of `OPERARunner.run()`** (`_raise_if_pending_approval`), covering all 5 run entry points at the single choke; per-entry-point wrappers (scheduler skip, manual-/run 409, /instruct message) are convenience only. New `agent_engine/create_gate.py` `requires_human_approval` (tier-3 / EXECUTE_CHANGE ⇒ pending, fail-CLOSED, No-Data) sets `Agent.pending_human_approval` at sign-time. `PendingApprovalError` → run recorded `awaiting_approval`. Schema: 4 columns (`pending_human_approval`/`pending_reason`/`approved_by`/`approved_at`, `sa.false()`), migration `c5e9b1a7f3d2` (revises `b8d3f1a26c47`, dual-DB, SQLite-verified). `POST /api/admin/agents/{id}/approve-activation` (NODE_ADMIN, tenant-scoped, idempotent, ABNActivityLog audit) + `GET /agents/pending-activation` queue. **V1 reality: NO generated agent is create-gated yet** (build_blueprint caps tier at 2); the gate is the forward-looking V2 boundary. **ApprovalRecord deviation (documented):** used ABNActivityLog (its proposals FK is dual-DB-unsafe for a non-proposal activation); Proposal-queue surfacing is a future UX add. Tests 27+2, suite 1663→1692, 0 regressions.
  - **Prio 3 (later):** hard minimum-conformance FLOOR (conformance is only 25% of the blended quality — a high-volume/low-fitness graph can still cross the threshold) + a **dry-run-before-deploy** gate at creation (`simulate_run` exists but isn't wired into `generate_blueprint`).
- **OPERA + Blueprint-creation integrity — Discovery DONE (read-only, authoritative). Engine-integrity queue (close in order, Discovery before build, one batch each):**
  - **[73 DONE — merged #89] run() status honesty.** `OPERARunner.run()` checked only Observe+Execute → a FAILED run persisted as "success" (Plan error swallowed → empty plan → masked success; Act failure read empty `{}` output → "success"). Fixed surgical: run() now checks Plan + Act status, fail-CLOSED → "failed" with a metadata reason; legitimately-empty plan stays a truthful success (the crux: `_phase_plan`'s only non-"success" path is its except → "partial"); Reason graceful-degrade + Observe/Execute untouched. No schema. Tests +9 (`test_opera_status_honesty.py`), suite 1692→1701.
  - **[76a DONE — PR open] RAL finding-shape + INFORMATIONAL attestation.** Domain→FIELD_MAPS resolver + `attest_and_stamp` (field_attestor) + capabilities emit non-PII critical values (logistics source_a/source_b) + source_records surfaced to `_phase_verify` + `_attest_informational` helper stamps `Finding.attested` + persists signed `ABNAttestation`. INFORMATIONAL (verdict + confidence byte-identical with RAL on/off — protects Batch 74), fail-safe, No-Data, no schema. Fixes the ~0 attestation_rate. Tests +20 (`test_ral_attestation.py`), suite 1743→1763, 0 regressions. **NEXT after merge: 76a_pii (P4 — Critic ↔ attested-fields PII reconciliation); the deferred-by-design split.** Hold at CLEAN — do NOT auto-merge. (76a merged #94; 76a_pii now DONE — see next bullet.)
  - **[76a-pii DONE — PR open] Critic attested-fields PII reconciliation (P4).** Narrow deny-by-default whitelist on the Batch-74 Critic PII scan: a declared FIELD_MAPS critical that is RAL-attested AND shape-correct is treated as not-PII (so a legit source-attested 16-digit invoice_number/total_amount no longer false-blocks once real adapters land), while genuine PII (real card/personnummer/email in FREE TEXT) STILL absolute-blocks. Hybrid wiring (G5 i+ii): pre-critic `_stamp_attested_fields` + key/container-aware `_check_pii(output, blueprint)` + single-source `is_attested_critical_value`. Fail-closed (smuggled/unattested/wrong-shape/unknown/missing-context all scan); No-Data (metadata-only); no schema. Tests +20, suite 1763→1783, 0 regressions. **NEXT: 76b** RAL min()-fold ENFORCEMENT. Hold at CLEAN — do NOT auto-merge. (76a-pii merged #95; 76b now DONE — see next bullet.)
  - **[76b DONE — PR open] RAL min()-fold ENFORCEMENT (RAL track COMPLETE).** Required-attestation failure/missing BLOCKS delivery (fail-closed, honest `verify_attestation_missing` reason) when `blueprint.ral_required_fields` non-empty; folded into the verdict via min() so a high Critic/ESE score can't override; empty ral_required_fields = byte-identical Batch 74. New single-source `VerificationRequirementMatrix`; `AgentBlueprint.from_dict` carries the required set (signature-safe); fail-closed (enforced) vs fail-safe (informational) on error; taxonomy class now live. No schema, No-Data. Tests +14, suite 1783→1797, 0 regressions. **RAL track COMPLETE → NEXT P2a** (hard conformance floor + VariantConformanceMap). Hold at CLEAN — do NOT auto-merge. (76b merged #96; P2a now DONE — see next bullet.)
  - **[P2a DONE — PR open] hard conformance floor on the create-path.** `CONFORMANCE_FLOOR=0.65` + optional `conformance` kwarg in `decide_agent_need` (de-facto GraphAdmissibility gate): conformance < floor → capped at report-only/observed, NEVER acting, regardless of blended quality/ROI/confidence (hard floor, no compensation, fail-CLOSED). Default None → byte-identical (every caller/test safe). `runner.py` passes `ensemble.fitness_score` + stamps it into stats JSON (audit). Honest `low_conformance` reason. Closes the confirmed create-path gap (high-volume/zero-fitness → acting). No-Data, no schema. Tests +9 (`test_conformance_floor.py`), suite 1797→1806, 0 regressions. VariantConformanceMap + KL-divergence DEFERRED to Spår-3. **NEXT: P2b** SimulationGate (last Spår-1 batch). Hold at CLEAN — do NOT auto-merge.
  - **[landing-truth-sync DONE — PR open] customer TEXT truth-sync + permanent Customer-Surface Sync Rule.** Standalone trust-surface batch (NO engine code, not Spår-1). Corrected the customer copy to the true 7-phase OPERA (Verify = Critic+ESE+RAL proves each value vs source, blocks unproven; + Deliver): home `OperaSection` reworded IN PLACE keeping EXACTLY 5 PHASES rows so the 5-vertex FigOpera pentagon stays byte-identical; figure-free `autonomous-engine` page gained explicit Verify+Deliver steps + "Seven phases" title + SEO meta in lockstep; Hero toned to V1 truth ("surfaces … proposing each step for your approval" not "takes over"; 72h reframed as a target, not "no exceptions"); root SEO/OG "acts autonomously" → "proposes … you approve". TEXT-ONLY (no CSS/layout/figure/colour — T5 diff confirmed), human-gated (Jacob approves at merge, NEVER auto-sync), no "AGI", no target-as-live. Added **Charter §9 Customer-Surface Sync Rule** (every batch changing a customer-described mechanism MUST include a human-gated TEXT side-fix in the same batch; never auto-sync; design never touched) + CLAUDE.md pointer. Landing build green; backend untouched (1806). **FOLLOW-UPS (separate, deferred):** (1) Swedish dashboard `OperaRunPanel` same 5-phase gap — couples with the deferred app-i18n track; (2) api public-liveness check (api.abnplatform.com advertised endpoints — live vs target); (3) optional (b) divergence-flag CI guard (reminder-only, never edits) once a declared phase source-of-truth is committed. Hold at CLEAN — do NOT auto-merge.
  - **[P2b-i DONE — PR open] CanSignBlueprint gate (Spår-1 COMPLETE at gate level).** A `CanSignBlueprint` check in `generate_blueprint` immediately before `sign_blueprint` (after 71b Accept(B)) — the single BlueprintReadiness choke. **FAIL-OPEN**: a brand-new blueprint with no history + a sound plan signs as before (the key guard — new-agent creation never breaks). Refuses ONLY on (a) a re-validated lineage whose recent SafeFailureRate (`1 − (failed/error+rolled-back)/total` over last 50 runs) is `< SAFE_FAILURE_FLOOR=0.50` with `≥ MIN_SAMPLE=5` runs (`blueprint_unsafe_history`), or (b) zero executable steps (`dry_run_defect`, via plan-only `simulate_run`). No quality/ROI compensates; whole call wrapped fail-OPEN. New single-source `agent_runtime/can_sign_blueprint.py`; `CanSignRefusedError(BlueprintValidationError)`; +2 Failure-Taxonomy classes (CANON_V1 27→29); `simulate_run` docstring corrected (plan-only, honest). **Lineage note:** `generate_blueprint` returns existing agents before signing, so at this choke the history branch is fail-open and the active effect is the dry-run-defect check; the history branch bites via a future re-sign path (built + unit-tested). No-Data, no schema. Tests +12 (`test_cansign_gate.py`), suite 1806→1816. **HONEST: NOT a 5-level harness.** **TARGET next: P2b-ii** = real write-suppressed OPERA dry-run harness + failure-injection + multi-level SimulationGate + true pre-sign SafeFailureRate (genuine new infra; own batch, may sit alongside Spår-3). **Spår-1 engine-integrity COMPLETE at the gate level** `[73 74 75 76a0i 76a 76a-pii 76b P2a P2b-i]`. Hold at CLEAN — do NOT auto-merge.
  - **[S2F1-1 DONE — PR open] PhaseContractValidator + weakest-link RunStatusReducer (SPAR 2 Fas 1 start).** Run status is now a weakest-link reduce over all phases (`failed > partial > success`, `agent_runtime/run_status.py`). Batch 73 made FAILED honest; S2F1-1 surfaces **PARTIAL**: an Execute `partial` (skipped capability) or a Reason `partial` (LLM fallback) → honest `partial` run status (was masked `success`) — **still delivered + flagged, never failed, never blocked** (verify `ok`/`flagged` map to success severity so needs_review keeps a success label; unknown status → failed = fail-CLOSED). Wired by replacing ONLY the terminal `_finish(...,"success")` with the reduce (failed early-returns kept = equivalent control flow + delivery gating). `PhaseContractValidator` (`validate_phase_result`) wired via `_append_phase` at every phase append → a malformed PhaseResult fails-CLOSED with `failed_phase_contract` (+1 taxonomy class, CANON 29→30). Delivery is independent of the status LABEL (G6 — the Batch-74 verdict is unchanged). No-Data, no schema. Tests +15 (`test_run_status_spine.py`) + updated `test_opera_status_honesty` reason-partial→`partial`, suite 1816→1831. **NEXT: S2F1-2** RuntimeStateMachine + TransitionGuard → S2F1-3 RunLock (own Discovery) → S2F1-4 idempotency keys → S2F1-5 Transactional Outbox (own Discovery). Hold at CLEAN — do NOT auto-merge. (S2F1-1 merged #100; S2F1-2 now DONE — see next bullet.)
  - **[S2F1-2 DONE — PR open] RuntimeStateMachine + TransitionGuard (SPAR 2 Fas 1, batch 2).** The OPERA phase order was only the SEQUENTIAL CODE PATH in `run()` — coded, not guarded. S2F1-2 makes it ENFORCED: new single-source `agent_runtime/runtime_state_machine.py` (`OPERA_PHASE_SEQUENCE` + `ALLOWED_TRANSITIONS` = the order declaration; `evaluate_transition`; `ForbiddenTransitionError`; `is_live_order_transition`). Wired into `_append_phase` (the S2F1-1 seam): it evaluates the transition from the phases-so-far to the new phase and raises `ForbiddenTransitionError` on a CLEAR block → `run()` catches → honest `failed` with a `forbidden_transition` reason (via the S2F1-1 path). **BLOCK only 3 legit-path-IMPOSSIBLE violations**: (1) no-resurrect (a terminal `failed` phase already recorded), (2) deliver-before-verify (deliver without a recorded non-blocked verify), (3) backward/repeat (next phase index ≤ prev recognised phase index — the live order is strictly increasing). **FAIL-OPEN on everything else** (unrecognised phase, START sentinel, forward-skip, guard-internal error) — the central guard (G3): a guard bug can NEVER block a legit run, double-protected (`evaluate_transition` self-wraps + `_append_phase` swallows a raising guard). ASSERTS the existing order ONLY — no reorder, no schema. +1 Failure-Taxonomy class `forbidden_transition` (maps_to "failed", CANON 30→31). No-Data (phase name + status + verify block flag only). Tests +18 (`test_runtime_statemachine.py`: T1 legit order passes, T2 deliver-before-verify blocked, T3 no-resurrect, T4 fail-open [unrecognised/forward-skip/guard-error/raising-guard-live], T5 live block→honest failed, T6 7-phase order intact, T7 No-Data), suite 1831→1849, 0 regressions. **NEXT: S2F1-3** RunLock (own Discovery: lease-table key agent+work_unit+case_key+action_scope, TTL/heartbeat recovery, entry-point coverage) → S2F1-4 idempotency keys → S2F1-5 Transactional Outbox (own Discovery). Hold at CLEAN — do NOT auto-merge. (S2F1-3 now DONE — see next bullet.)
  - **[S2F1-3 DONE — PR open] RunLock — dual-DB lease, per-agent run serialization (SPAR 2 Fas 1, batch 3; the FIRST behaviour-changing concurrency piece + first Fas-1 schema).** Discovery confirmed only the scheduler had a guard (best-effort, per-tenant, in-process); manual `/run`, `/instruct`, the orchestrator, and the auto-trigger had NONE → the SAME agent could run concurrently across entry points. RunLock = a dual-DB lease (`run_locks`, model on `Base` + migration `d4b9f1e7a2c6` chaining onto head `c5e9b1a7f3d2`, raw-SQL `IF NOT EXISTS`, applies from scratch on SQLite; Postgres half on CI migrations-dual) keyed on **`agent_id`** (the honest available unit — `case_key` is per-event/spans a run, `work_unit`/`action_scope` don't exist; `agent_id` PK ⇒ same-agent serialized, different agents never contend; tenant_id/domain/holder_run_id/acquired_at/expires_at = audit columns, read via `getattr` so a missing domain never crashes the lock). Single source `agent_runtime/run_lock.py`: `acquire_run_lock` (INSERT=acquired; `IntegrityError`→ stale `expires_at<now`? reclaim-once : contention; other exception→ `infra_error`; NEVER raises), `release_run_lock` (idempotent DELETE by (agent_id, holder_run_id) — only OUR lease), `RUN_LOCK_TTL=30min`. Wired in `run()`: acquire BEFORE the `try` (contention → benign `run_lock_contention` result, NO AgentRun persisted, return); release in the existing `finally` AFTER `_save_run_record`, only if ACQUIRED. **Decisions (locked):** A no-persist-on-contention; B generous fixed TTL (must exceed max run wall-clock so a live lease is never stolen; heartbeat=TARGET); C **HELD→fail-CLOSED refuse-fast, lock-INFRA-error→fail-OPEN log+proceed** (a broken lock must never DoS all agents — a CORRECTNESS gate, deliberately UNLIKE the security gates quarantine/pending/RAL/verify/CanSign/conformance which stay fail-CLOSED-on-DB-error and are untouched). `_running_tenants` KEPT (complementary throttle; its test still passes). +1 taxonomy class `run_lock_contention` (INFO, weight 0.0, maps_to `run_lock_contention` — NOT "failed"; CANON 31→32). No-Data (ids + slugs + timestamps only). Tests +15 (`test_run_lock.py`: T1 contention-benign-no-AgentRun, T2 different-agents, T3 stale-reclaim, T4 live-not-stolen, T5 release-on-success + on-exception, T6 fail-open-on-infra, T7 5-trigger choke coverage, T8 No-Data), suite 1849→1864, 0 regressions. **TARGET (logged):** ideal key (work_unit/case_key/action_scope) + heartbeat for runs exceeding the TTL. **NEXT: S2F1-4** idempotency keys → S2F1-5 Transactional Outbox (own Discovery). Hold at CLEAN — do NOT auto-merge. (S2F1-3 merged #102; S2F1-4 now DONE — see next bullet.)
  - **[S2F1-4 DONE — PR open] retry loop + step-level idempotency (TOGETHER) (SPAR 2 Fas 1, batch 4).** Discovery proved a step key alone is 100% dormant (no re-execution path) and that retry+idempotency are only meaningful together. `MAX_EXECUTE_RETRIES`/`PHASE_TIMEOUTS` were defined but referenced nowhere — the execute loop ran each step once (the "max 2/120s" copy was target-as-live). **Action Risk Ladder (locked PM decision):** retry ONLY safe/idempotent steps (read/analyze/local-report); NEVER auto-retry an external write/tier-3/propose/unknown — a failed write → honest failed + a human; fail-safe default (not-provably-safe → not retried). Built: `is_retry_safe(capability)` (single source in `agent_engine/capabilities` — whitelists the safe FUNCTIONS, all aliases inherit, writes/propose/unknown → False); `StepIdempotency` (`step_idempotency`, composite PK `(run_id,phase,step_id,tool_action)`; migration `e2c8a4f1d9b6` chains onto head `d4b9f1e7a2c6`, raw-SQL `IF NOT EXISTS`, dual-DB); `agent_runtime/step_idempotency.py` (`compute_step_id` stable hash, `is_step_completed` fail-OPEN, `record_step` idempotent, never-raise); `runner._execute_one_step` — idempotency short-circuit → SAFE: retry to `MAX_EXECUTE_RETRIES` + per-step timeout (`wait_for`+`to_thread`), each attempt on a COPIED context merged only on success (findings-once) → NON-eligible: run once, never retried. Exhausted → honest failed (S2F1-1 reducer). tier-3 coarse replay UNTOUCHED (G6). No-Data (ids+slugs+outcome+timestamp). Tests +17 (`test_retry_idempotency.py`); one merged test updated (`test_finding_production::test_sim_only_behind_flag` → fresh runner per run, since reusing one run_id now correctly short-circuits). Suite 1864→1881, 0 regressions. **NEXT: S2F1-5** Transactional Outbox (own Discovery; delivery-layer idempotency, distinct from step-execution). Hold at CLEAN — do NOT auto-merge. (S2F1-4 merged #103; S2F1-5a now DONE — see next bullet.)
  - **[S2F1-5a DONE — PR open] transactional outbox + OutboxPoller (SPAR 2 Fas 1, batch 5a — substantively CLOSES Fas 1).** Discovery: the proposal notification (Slack/Teams) is the ONLY real live external send on the per-run path (internal #1-7 are per-run_id idempotent = crash-safe; `_deliver_reports` #9 is a logging stub; Friday #10 has its own idempotency). Built a general transactional outbox + a daemon poller and migrated THAT one sender; internal writes NOT outboxed (over-reach). `OutboxEvent` (`outbox_events`, payload-free; migration `f3a9c1e8b524` chains onto head `e2c8a4f1d9b6`, dual-DB); `agent_runtime/outbox.py` (enqueue/claim/mark_sent/skipped/failed + backoff/dead-letter, single source); `OutboxPoller` daemon (`agent_runtime/outbox_poller.py`, mirrors FridayReportScheduler, wired in `main.py` lifespan step 6). **Transactional seam:** `save_proposal_from_run` does `flush → enqueue_outbox → commit` so the intent commits ATOMICALLY with the Proposal (rollback ⇒ no orphan). **Exactly-once** = at-least-once (poller re-delivers pending) + dedup: `idempotency_key` dedups enqueue; the existing `NotificationDispatch` row is the SEND-dedup token (poller checks before sending ⇒ no double-send on a crash between send and mark). No-channel ⇒ benign terminal skip; repeated failure ⇒ backoff ⇒ dead-letter (honest, never silent). No-Data: content re-derived at delivery from the Proposal row. Inline Batch-18 dispatch REMOVED; `NotificationDispatch` KEPT as correlation record (G7). tier-3 untouched. 5b (report-delivery #9 / Friday #10 migration) DEFERRED. Tests +13 (`test_outbox.py`) + 1 merged test refocused (`test_proposal_wiring::test_notification_dispatch_fires_on_proposal` → asserts the outbox enqueue). Suite 1881→1894, 0 regressions. **Safety Spine Fas 1 substantively COMPLETE** (S2F1-1..5a). NEXT per the V1 plan: Fas 2 Blueprint MUST-subset. Hold at CLEAN — do NOT auto-merge. (S2F1-5a merged #105; S2F2-1 now DONE — see next bullet.)
  - **[S2F2-1 DONE — PR open] runtime blueprint-signature verification, ALL agents (SPAR 2 Fas 2, batch 1 — FIRST Blueprint-integrity batch).** Discovery: the live `OPERARunner` verified the signature ONLY for tier-3 (Go-service preflight); tier-1/2 (every V1 agent) had NO runtime check → a post-sign `Agent.blueprint` DB mutation ran undetected, bypassing Accept(B)/CanSign/conformance/create-gate. Built `_verify_blueprint_signature_or_raise()` at the run() choke (after create-gate + tier-3 preflight, before Observe; covers all 5 entry points), reusing the existing `trust.blueprint_signer.verify_blueprint` (HMAC over the canonical form — the scheme production embeds at `generator.py:1080`; same check the dormant AAEA mission_layer did, now live). Verifies the RAW stored `agent.blueprint`. **No new crypto, NO migration** (signature already stored). Fail-CLOSED on BOTH missing AND invalid (+ uncertainty) → `BlueprintSignatureError` → honest `failed` (`blueprint_signature_invalid`); no phase runs. Empty/placeholder key: sign+verify symmetric → transparent for a valid sig yet still detects mutation (enabled everywhere, no bypass flag). Tier-3 Go-preflight KEPT beneath (belt-and-suspenders). +1 taxonomy `blueprint_signature_invalid` (HIGH, requires_human, not-retryable, maps_to "failed"; CANON 32→33). Of the 3 canon candidates only this built — BlueprintQualityScore already-covered (P2a/P2b-i); BrainProfile doesn't exist (build first); ToolBinding subsumed (in the signed blueprint); ArtifactSet RCU premature. Test sweep: 6 run()-seeders now sign their blueprint via the real `sign_blueprint` (gate NEVER weakened). Customer-Surface Sync §9: `landing/.../security` "Signed blueprints" copy reworded to truth (every agent verified before it runs). Tests +8 (`test_blueprint_verify_onpath.py`); suite 1894→1902, 0 regressions. **Makes TRUE:** the agent that runs is provably the agent that was compiled, gated, and signed. NEXT: re-check the V1 plan, then **Fas 3 (Fleet)** MUST-subset (AgentHealthScore hard floors + auto-pause, Safe Mode) — own Discovery. Deferred Fas-2: S2F2-2 (explicit quality floor, only if P2a/P2b-i insufficient), S2F2-3 (BrainProfile + ArtifactSet RCU). Hold at CLEAN — do NOT auto-merge. (S2F2-1 merged #106; S2F3-1 now DONE — see next bullet.)
  - **[S2F3-1 DONE — PR open] AgentHealth auto-pause (SPAR 2 Fas 3, batch 1 — FIRST Fleet-integrity batch; the V1 🟢 subset that makes "controlled" TRUE).** Discovery: auto-pause existed ONLY for confirmed security breaches (Batch 66 Shield → `quarantined`); there was NO auto-pause on OPERATIONAL degradation. Built `agent_runtime/agent_health.py` — `evaluate_agent_health` reuses the P2b-i `history_safe_rate` signal verbatim (window 50 / MIN_SAMPLE 5; unsafe = failed/error ∪ rolled-back; fail-open `(None,0)`); pause iff `safe_rate < HEALTH_SAFE_RATE_FLOOR` (0.50, sibling const) over ≥ MIN_SAMPLE runs. `AgentHealthMonitor.run_tick` mirrors the Shield monitor (interval 300s + whole-tick fail-OPEN), wired into `scheduler._tick` BESIDE the Shield (SEPARATE: health ≠ security). DISTINCT `Agent.health_paused` + reason + at columns (migration `a7d2f4c9e1b6`, inspector-guard `op.add_column` + `sa.false()`, dual-DB; chains onto `f3a9c1e8b524`); `quarantined` (security) untouched (ABN Code Law #6). Enforcement WIDENED to `quarantined OR health_paused` (no new mechanism): scheduler skip, manual /run 409, run() pre-flight `_raise_if_quarantined` → DISTINCT `AgentHealthPausedError` → honest `failed`. **Posture:** fail-OPEN on eval error (a health bug pauses NOTHING — never a fleet DoS), fail-SAFE on a computed floor breach; MIN_SAMPLE guard → new agents never false-paused. Reversal: manual `POST /admin/agents/{id}/un-health-pause` (recovery-auto rejected — a paused agent runs no new good runs, can't self-recover). Audit = agent columns (rate/floor/window + ts) + log. +1 taxonomy `agent_health_paused` (CANON 33→34). No fixture sweep (defaults False). Customer-Surface Sync §9: no copy describes agent auto-pause → none to sync. Tests +14 (`test_agent_health.py`); suite 1902→1916, 0 regressions; dual-DB migration green. **SAFETY SPINE substantively COMPLETE** (Fas 1 runtime + Fas 2 signed-blueprint + Fas-3 auto-paused-fleet). Deferred (🟡): S2F3-2 Safe Mode (premature — tier-1/2 already human-gated; per-agent auto-pause is V1-sufficient), S2F3-3 Supervisor Reconciliation + richer health signals. NEXT per the V1 plan: re-check at this Fas boundary, then **Compliance-as-runtime MUST-subset** (EU AI Act risk classification + ComplianceGate + basic Legal Centre) — own Discovery. Hold at CLEAN — do NOT auto-merge.
  - **[NoPayload-1 DONE — PR open] NoPayloadProof SchemaClean — classification-driven schema scan (V1-plan 🟢 #5 No-Data PROVABLE, the SchemaClean leg).** Discovery: redaction is REAL + enforced (gateway strips/tokenises/abstracts; outbound prompt payload-free by construction) but the PROOF was ASSERTED not measured (`abn_llm_calls` hardcodes raw_values_sent=0; the "no payload columns" guarantee lived in 7 SCATTERED per-table tests with no unified scan). Built `core/no_payload_proof.py` (single source): `TABLE_CLASSIFICATION` buckets ALL 53 `Base` models into MUST_BE_PAYLOAD_FREE (23 cross-tenant/telemetry/proof/transparency) or ALLOWED_LOCAL_OPERATIONAL (30 customer/operator local data+config); `is_clean_column_shape` allow-list (numeric/bool/temporal always clean; String/Text via `_ALLOW_SUFFIX`/`_ALLOW_EXACT`); documented `COLUMN_EXCEPTIONS` for the free-text-but-clean metadata columns (shield message, pulse spec_diff/error_message, mind suggestions, control_plane violations/action_taken, ai_act intended_purpose/human_oversight/capabilities/data_sources, diffusion pattern_vector, long_term fact_payload, llm_calls sent_event_types/sent_statistics, roi formula); `scan_schema()` + `schema_clean()` factor + `assert_table_payload_free()`. **THE CRITICAL DISTINCTION:** No-Data = EGRESS, NOT the local DB — so Finding/AgentRun/Proposal/Event (customer-derived local) are ALLOWED + NEVER false-flagged (protected by egress controls, not the scan). **FAIL-CLOSED:** an unclassified new table fails (the permanent guard — forces classification); a payload-shaped column on a must-be-clean table fails; a justified exception is a documented entry, never a heuristic loosening. **No-Data meta:** the scan reads column METADATA only (names+types+classification) — `scan_schema()` takes no DB, never samples a value. Consolidated the 7 scattered tests to the one single source (kept DiffusionPattern cross-tenant anonymity check + value-greps). `schema_clean()` = the first REAL factor of the multiplicative canon (other factors NOT faked). AuthBase (operator identity) = separate metadata, out of scope. Customer-Surface §9: internal proof, no copy change (No-Data now CI-proven, WHAT not HOW). Tests +15 (`test_no_payload_proof.py`); suite 1933→1948. **No migration, no model change, no run-path change, no new vendor deps.** **Makes PROVABLE:** the SchemaClean leg — no raw customer payload column exists where it must not, and none can be added unnoticed (CI-enforced). NEXT per the V1 plan: re-check, then **NoPayloadProof batch 2** (gateway runtime/redaction proof: emit payload_clean into LLMGatewayLog + assert task_description is token/abstract-only — prove existing enforcement). Then 🟢 #6 one-agent-family end-to-end, #7 Tuari (uses the design system), #8 ops-todos. Deferred 🟡: ExportClean, auditor-queryable proof endpoint, full data-class taxonomy. Hold at CLEAN — do NOT auto-merge.
  - **[NoPayload-2 DONE — PR open] NoPayloadProof gateway runtime/redaction proof (measured, per-call) — V1-plan 🟢 #5 No-Data PROVABLE, the RUNTIME legs (3.2 RuntimePolicyClean + 3.3 LLMPayloadClean).** Discovery: redaction is REAL + the outbound prompt payload-free BY CONSTRUCTION (abstractor `_walk_tokens` emits only `_#`-tokens + type-labels + `task_description`), but the one verbatim free-text seam — `task_description` (abstractor.py:168) — was concatenated raw + NOT redacted, and payload-cleanliness was ASSERTED not measured. Built: (B2/G2) redact `task_description` BEFORE assembly via the EXISTING `PIIRedactor.redact_value` (strong-PII only, NO name heuristic → H_feel's own prose never false-flagged, parser sentinels idempotent — no engine change); (B2/G3) post-assembly `_count_pii_residue(redactor, prompt)` between `:252` and `:256` → residue>0 → `LLMGatewayError(stage="payload_scan")` fail-CLOSED via the SAME error path (no provider call, mapping wiped at :324); new `"payload_scan"` in PIPELINE_STAGES; (B3/G4) `payload_clean: bool` + `payload_residue: int` on `LLMGatewayResult` + threaded into `GatewayAudit.log`/`log_gateway_call` → recorded per call (happy path True/0 but MEASURED). **Migration `b1f4d7c2e9a3`** (revises `a7d2f4c9e1b6`) — 2 dual-DB columns on `llm_gateway_logs` (`payload_clean` BOOL server_default true + `payload_residue` INT server_default 0; inspector-guard + batch_alter_table downgrade, S2F3-1 pattern; SQLite-verified, new head `b1f4d7c2e9a3`). (B4/G10) `runtime_policy_clean`/`llm_payload_clean` in `core/no_payload_proof.py` = real measured factors (mirror `schema_clean()`); ExportClean/AuditClean stay 🟡 (not faked). **TWO fail-postures:** residue scan fail-CLOSED (SECURITY gate); task_description = REDACT-then-measure (NOT a name-scan — safe for all 4 callers). **No-Data meta (G5):** records a boolean + a COUNT + the stage, NEVER a value sample even on detection (verified — the offending value never appears in any LLMGatewayLog column). local_only (no network, G6) records True/0, byte-identical otherwise. Customer-Surface §9: `landing/.../company/security` LLM-policy copy now states the No-Data guarantee is MEASURED per call (WHAT not HOW, no component named). Tests +16 (`test_no_payload_proof_gateway.py`); suite 1948→1964, 0 regressions. **Makes PROVABLE:** every outbound call is MEASURED payload-free before it leaves — the runtime moat is measured not asserted; the last asserted-not-measured seam (`task_description`) is closed. **DEFERRED 2b: `abn_llm_calls` hardcoded `raw_values_sent=0` UNTOUCHED** — measuring it threads through 4 callers, 3 via the SEPARATE HTTP service (no in-process prompt) → a mixed measured/asserted state, a deliberately-separate batch. NEXT per the V1 plan: re-check, then 2b OR 🟢 #6 one-agent-family end-to-end. Hold at CLEAN — do NOT auto-merge.
  - **[E2E-1 DONE — PR open] Report edge — a real report file on the live OPERA path (V1-plan 🟢 #6 one-agent-family end-to-end, batch 1 of a 2-batch SPLIT; E2E-2 = the full-chain proof test).** Discovery: the chain's MIDDLE (compile→OPERA-V→verify→approval→audit) is fully BUILT + hardened + already exercised live (`test_opera_runner.py`); the one real gap was a real REPORT FILE — the live `_generate_reports` (runner.py:1732-1750) was a stub (filename strings + "Report queued" log, no file), while the real DGE renderer (dge/orchestrator.py:241) is orphaned (called only by the un-wired AAEA executor.py:490 + deprecated integrated_runner.py:491). **GO/FALLBACK (Step 1) → FALLBACK** (deterministic Markdown, stdlib, NO LLM): DGE is closer than feared (`_attestation_passed` accepts a dict-with-summary :76-90; `genome` a plain dict :334-348) but heavier than a drop-in — it renders attested numbers from `attestation["field_attestations"]` rows the live `_attest_informational` (runner.py:1361) doesn't surface (only a summary dict), calls the gateway for narrative every run (:268), and resolves per-domain templates + weasyprint (G8-forbidden). DGE stays the future richer path. Built: **`agent_runtime/report_render.py`** (NEW single source) — `render_markdown_report` (deterministic, no clock/LLM/network — header + summary + recommendation + findings table type/severity/impact/attested/reference) + `write_report_file` (writes under `settings.report_storage_path/<tenant>/<run_id>.md` — the reports-API root + traversal-guard, downloadable for approval; `_slug` path-safe). **`_generate_reports` rewired** (runner.py) — stub replaced with a real render+write returning `[path]`; `report_paths` persistence already wired (run() :334 → AgentRun :2073), **NO schema/migration**. `_deliver_reports` STAYS a stub (external send = 🟡). invoicing.py:108 marker untouched (file produced at runner level). **Guards:** runs THROUGH the gates (generation in `_phase_deliver`, AFTER verify — report only for a run that passed every gate; blocked/failed → no report, honest); fail-isolated (render/write error → `_phase_deliver` except → honest `failed`, no crash/no fake pass); No-Data (deterministic, no LLM, local ALLOWED_LOCAL_OPERATIONAL artifact). Customer-Surface §9: `landing/.../autonomous-engine` "Deliver" stage reworded "a verified report produced for your review" (removed imprecise "report signed"; WHAT not HOW, no AGI). Substrate: Invoice Auditor (carrier_billing) on seeded events + invoicing sim — no live connector. Tests +10 (`test_report_edge.py`: T1 real file on live path, T2 content-from-findings, T3 through-the-gates pass→report/block→none, T4 fail-isolated, T5 no-LLM/local-artifact, T6 report_paths persisted, T7 deterministic+empty-renders+phase-order). `backend/reports/` gitignored; no new heavy dep; middle exercised not edited. **Makes TRUE:** a human can now approve a REAL, verified report artifact on the live path — the last missing artifact in the chain. NEXT per the V1 plan: **E2E-2** — the full-chain end-to-end proof test on sample data (events → graph → signed agent → OPERA-V → real findings → real report file → Proposal → audit), asserting every stage's real artifact. Then 🟢 #7 Tuari (Evidence Ribbon, uses the ABN design system). Deferred: report DELIVERY (external send), live-connector observe (after ABN org-nr). Hold at CLEAN — do NOT auto-merge.
  - **[E2E-2 — 4.2 HALT (docs-only on branch `feat/batch-e2e-2-mnd-fullchain-proof`, commit e1ea21e) — root-hole found, NOT merged] The full-chain proof could not be written honestly: the create→run chain didn't compose on sample data.** Two read-only probes proved: the CANONICAL `generate_blueprint` (all gates, signed) emitted action_map capability names NOT in `CAPABILITY_REGISTRY` → 0 findings; the LEGACY generator emitted resolvable names (8 findings) but lacked 71a `policy_constraints` → failed Accept(B). NO production change, NO test written (G2: stop+report, never patch production / fabricate a blueprint in a test batch). FIXED BY Bridge-1 below. (The E2E-2 proof test itself is still TODO — after the family/RAL decision.)
  - **[Bridge-1 DONE — PR open] Capability vocabulary bridge (Option 1c) — domain-aware mapping at GENERATION, before signing (fixes the E2E-2 root-hole).** Discovery refined the gap to the **no-pattern-match fallback only** (`generator.py:600` `sorted({s["action"]...})` = action_map ids; a pattern match already emits resolvable `pattern.capabilities`). Built domain-aware helpers in `blueprint_generator/generator.py` (before `build_blueprint`): `_DOMAIN_CAPABILITY_FAMILY` (invoice_audit/carrier_billing/project_billing→invoicing; shift_planning/clinical_scheduling/payroll→scheduling; delivery_matching/delivery→logistics), `_FAMILY_RUNTIME_CAP`, `_SYSTEM_RUNTIME_READ`, `_REPORT_RUNTIME_CAP`, `_runtime_capability_for_step` (report=flat; fetch_data=domain-family-wins else source-reader else honest no-op; analyze=domain-family else honest no-op; propose/execute left as-is), `_bridge_capabilities` (STEP-ORDER, deduped — read before analyze). The no-pattern `capability_list` now calls `_bridge_capabilities`; pattern-match branch UNCHANGED. + flat `report.pdf/.excel/.word/.csv` → `reporting.generate_*` aliases in `CAPABILITY_REGISTRY` (G3, domain-agnostic). **Signed = executed** (bridge runs BEFORE the gates+sign at 1025-1133 → Fas-2 verify coherent). REJECTED 1a (flat alias = domain-blind/wrong) + 1b (post-sign = signed≠executed). G6: an unmapped domain keeps the action_map id → logged skip (honest no-op), NEVER a wrong-domain cap. Empirical: a no-pattern invoicing agent → caps `[invoicing.read_invoices, invoicing.analyze, report.generate_excel, report.generate_pdf]` (was action_map ids), signed, Accept(B)=True, **real findings (was 0)**. **No-Data unaffected; NO migration; every gate/RAL/AVM/legacy untouched.** Tests +10 (`test_capability_bridge.py`). **OUT OF SCOPE (the E2E-2 family decision):** the 76b RAL-field↔finding-shape mismatch (a logistics-industry invoicing agent still fails 76b — logistics RAL needs distance_km/route_id the invoicing findings lack) → target a 76b-consistent finance/retail invoicing family; `propose.*`+`fetch_data.generic_api` unmapped (tier-2/no-target). NEXT: the E2E-2 family/RAL + tier decision, THEN E2E-2 passes honestly. Hold at CLEAN — do NOT auto-merge. (Note: the E2E-2 confirm-Discovery then found a THIRD gap — RAL domain-map didn't recognise the detected vocabulary → fixed by Bridge-2 below.)
  - **[Bridge-2 DONE — PR open] RAL domain-vocabulary alignment (one domain→family source of truth) — fixes the third root-hole.** The E2E-2 confirm-Discovery proved (3 probes): a no-pattern generated agent's findings were never attested because RAL's `DOMAIN_TO_FIELDMAP` didn't recognise the DETECTED-domain vocabulary (`invoice_audit`/`shift_planning`/`delivery_matching`) → `resolve_domain_field_map("invoice_audit")=None` → `_attest_informational` skipped attestation → `attested=False` → general delivered UNATTESTED, RAL-required industries fail-CLOSED at 76b (same vocabulary-drift class Bridge-1 fixed, now in RAL). **Fix = full single-source consolidation (G3):** NEW `core/domain_family.py` (`DOMAIN_FAMILY` + `resolve_domain_family`) = the ONE domain→family GROUPING consumed by BOTH the capability bridge AND RAL (a fourth map can't drift). `field_attestor.py`: `DOMAIN_TO_FIELDMAP`→`_FAMILY_TO_FIELDMAP` (family→FIELD_MAPS key); `resolve_domain_field_map` delegates to `resolve_domain_family` (signature + all Batch-76a `test_domain_resolver` cases preserved). `generator.py` (Bridge-1): `_DOMAIN_CAPABILITY_FAMILY` removed → uses `resolve_domain_family`; `_FAMILY_RUNTIME_CAP` gains `delivery`→logistics caps (capability behaviour preserved). Each consumer keeps its own family→target map (capability vs FIELD_MAPS key; `logistics`≠`delivery` in RAL FIELD_MAPS but share logistics capability funcs). **G4: attestation now RUNS, no gate weakened** — a finding genuinely lacking its RAL fields STILL fail-CLOSES. **G5: domain-correct** — invoice_audit→invoicing field family (NEVER logistics, the false-proof guard); unknown→None (honest empty). Empirical: (A) general clean-invoicing → `attested=[True×4]` (was [False×4]), delivers VERIFIED; (B) finance → passes 76b + delivers attested (was blocked); (C) carrier/logistics → status=failed, no report (gate still bites). **RAL thresholds/attest logic/finding-shape/sim/gates/schema UNCHANGED; No-Data; NO migration.** Tests +5 (`test_ral_domain_bridge.py`, T1-T6 with T1+T2 combined). **Bridge-1 (capability) + Bridge-2 (attestation) → a generated agent is now fully runnable AND verifiable.** NEXT: **E2E-2** — the full-chain proof, now honest: happy = a finance/retail invoicing tier-1 agent delivering an ATTESTED report (graph→signed agent→run→attested findings→real report file→audit→non-empty RAL attestation; NO Proposal — tier-1 auto-deliver); negative = carrier/logistics (findings but RAL-fail-CLOSED, no delivery). Deferred: E2E-3 (tier-2 PROPOSE/approval artifact), propose.* capability mapping, legacy-generator retirement. Hold at CLEAN — do NOT auto-merge. (CI note: PR #113 went all-red on a GitHub Actions BILLING block — "job not started"; NOT code. After billing restored + re-run, all 6 required checks green; merged #113.)
  - **[E2E-2 DONE — PR open] The full-chain end-to-end PROOF (honest, post the three fixes) — V1 #6 COMPLETE.** The earlier E2E-2 HALTed (chain didn't compose); E2E-1 + Bridge-1 + Bridge-2 fixed the three root-holes, so a canonically-generated agent now runs the whole chain on sample data AND delivers a VERIFIED (attested) report. ADDITIVE — a test, NO production change; no fourth gap found. `tests/test_e2e_invoice_auditor.py` (3): **happy path** (clean-invoicing graph → the CANONICAL `generate_blueprint` (real gates, NOT manual sign / legacy generator — the prior blind spot) → run) asserts every stage's real artifact: ProcessGraph row, SIGNED agent (signature; pattern_id=None=no-pattern path; tier=1; Accept(B) re-confirmed), AgentRun row, real Finding rows (impact>0), **finding.attested=True** (Bridge-2 payoff, was [False×4]), real report FILE on disk (non-empty, contains billing_deviation + attested col), **non-empty ABNAttestation** (summary ≠ "no attestations"), ABNActivityLog rows, **0 Proposals** + pending=False (tier-1 auto-deliver); **negative path** carrier/logistics → signed + findings but logistics RAL needs distance_km/route_id the invoicing findings lack → verify fail-CLOSES → status=failed, report_paths==[] (gate bites); **No-Data+deterministic** monkeypatch LLMGateway.call→raise → still delivers (E2E-1 renderer uses no LLM), 2nd run same findings_count, report under storage root. Tier-1 honesty: read-only takes no risky action ("never acts on risk without approval" by construction; risky-action gate = tier-2/72b). No live connector; No-Data; backend/reports/ gitignored + tmp_path; no migration; no new deps; chain EXERCISED not edited. Tests +3 (suite 1989→1992). **V1 #6 COMPLETE → the full V1 MUST list is DONE (#1-3 Safety Spine, #4 Compliance, #5 NoPayloadProof, #6 one family end-to-end).** NEXT per V1 plan: 🟢 **#7 Tuari** (the customer-control surface: Trust Card, Run Inspector/"Why", approval UX, Evidence Ribbon = RAL/attestation made visible — the FIRST frontend, MUST use the ABN design system) + #8 ops-todos (Fortnox LAST, after ABN org-nr). Deferred: E2E-3 (tier-2 PROPOSE Proposal/approval artifact), report delivery (external send), live-connector observe, NoPayload-2b, ExportClean + auditor endpoint, propose.* capability mapping, legacy-generator retirement. Hold at CLEAN — do NOT auto-merge. (CI note: PR #113 went all-red on a GitHub Actions BILLING block — "job not started", NOT code; after billing restored + re-run all 6 required green. Same billing risk may recur — if a fresh PR is all-red in 3-5s with "job not started", it's billing, not code.)
  - **[Tuari-1 DONE — PR open] Evidence Ribbon / Run Inspector — V1's FIRST customer frontend (🟢 #7 Tuari, first surface).** Makes the proof VISIBLE: the full V1 MUST backend is done but the RAL attestation lived in the DB + an unconsumed `/verification` endpoint. ADDITIVE (frontend only; NO backend/endpoint/gate/migration change). Built: **`client.ts`** `getAgentRunVerification` + `RunEvidence`/`EvidenceStatus` + pure `curateRunEvidence` (the SINGLE curation point — maps `/verification` → the SAFE WHAT subset, DROPS the HOW: signature hash, per-field `domain`, raw `sample_mismatch`, `cross_references` never read → never reach the view; derives status/verified_count/total_count/sources_count/corrected_count/unchecked). **`components/agents/EvidenceRibbon.tsx`** (NEW) from the ABN design system (Card + `TrustLayer` mark + v7 sage + Geist) — shows status + "Bevis: Stark/Måttlig/Begränsad · N av M värden verifierade mot godkända system" + Källor + Ej kontrollerat + Korrigerade värden; honest (Score-Honesty: strength WITH limits, never a bare number; empty state on 404). **`AgentDetailPage.tsx`** upgraded the shallow `AttestationPanel` (was a per-run summary-string list) → renders the EvidenceRibbon for the latest attested run; removed `attestationBadge`; FindingsListSection/FindingTraceView reused for drill-down. **Curation moat-guard (G3):** WHAT-not-HOW, enforced in the helper + asserted by test. **Voice (G4):** Swedish verifierad/bevis/godkända system/attestering/ej kontrollerat; no scan/autonom/hype/AGI (rendered). Read-only; No-Data (customer's own LOCAL findings, role-gated, owner-scoped on the single-tenant Node — not egress). Tests +4 (`EvidenceRibbon.test.tsx`) + updated AgentDetailPage.test.tsx (attestation test → Evidence Ribbon WHAT) + insights.test.tsx (mock the helper); frontend typecheck ✓, 79 tests ✓, `npm run build` ✓ (required gate); backend unchanged (1992). **Makes TRUE:** the customer can now SEE that an agent proved every value — trust through shown evidence. NEXT per V1 plan: remaining Tuari surfaces (Approval Center = tier-2 PROPOSE approval UX [needs E2E-3]; Mission Control = overview) + #8 ops-todos (Fortnox LAST). Deferred 🟡: server-side curated `/runs/{id}/evidence` read-model (defence-in-depth WHAT/HOW boundary); E2E-3 (tier-2 PROPOSE); the rest from E2E-2's list. Hold at CLEAN — do NOT auto-merge; verify CI's 6 required green before merge.
  - **[Compliance-1 DONE — PR open] ComplianceGate — risk-class as a gate + auto-forge declaration (V1-plan 🟢 #4 Compliance-as-runtime MUST-subset; NOT the Legal Kernel).** Discovery: ABN already DOCUMENTS compliance (Batch 37 `ABNRiskClassifier`+`ABNComplianceDeclaration`) and ENFORCES a constitutional subset at sign-time (V_GLOBAL Accept(B)), but the two never met — `risk_class` was a pure PDF field that gated nothing, and `forge()` was API-on-demand only (a signed running agent could have NO declaration until someone opened the dashboard). Built the THIN gate: `agent_runtime/compliance_gate.py` `evaluate(blueprint)->(ok,risk_class,violations)` REUSES the existing `ABNRiskClassifier.classify` (single classifier — no new one); FAIL-CLOSED predicate (a) risk-class ceiling refuse if above `{limited,minimal}` (transparent today, forward-guard) + (b) tier-3/EXECUTE vector must declare the human-oversight posture (EU-AI-Act Art.14). Wired in `generate_blueprint` AFTER Accept(B), BEFORE `sign_blueprint` (single create choke; gate returns a tuple, generator raises `ComplianceRefusedError(BlueprintValidationError)` — no circular import); on refusal NOT signed/persisted/run. **AUTO-FORGE (fail-SOFT):** `_forge_compliance_declaration` after the agent is persisted calls the existing `ABNComplianceDeclaration.forge` (every agent ships its Art.11/Annex-IV declaration AT CREATION) + writes a `compliance_evaluated` `ABNActivityLog` audit row; any error logs+rolls-back+continues (documentation never blocks a gated agent); re-`db.refresh(agent)` after (forge commit expires it — preserves the returns-a-loaded-agent invariant). **Two fail-postures (locked):** GATE fail-CLOSED, AUTO-FORGE fail-SOFT. **data_class** DERIVED from domain (single-source `risk_classifier.derive_data_class`, NO column) — surfaced in gate+audit+`to_dict()` (computed at read). **Composes existing primitives** (72b human-oversight + ABNActivityLog audit + No-Data/GDPR_COMPLIANT data-min) — reimplements none. **🔴 Legal Kernel stays OUT** — rule-based classifier over tier+domain+steps, no law fetched/interpreted; `NO_AUTONOMOUS_LEGAL_DECISIONS` reinforced. +1 taxonomy `compliance_blocked` (HIGH, requires_human, not-retryable, maps_to "failed"; CANON 34→35). **No migration, no new classifier, no new crypto, no new vendor deps.** Customer-Surface Sync §9: `landing/.../company/security` compliance copy now states each agent is risk-classified + ships its declaration at creation (WHAT not HOW, no component named, no AGI; landing build green). Fixture transparency held (the 4 `generate_blueprint`-invoking test files stayed green). Tests +17 (`test_compliance_gate.py`); suite 1916→1933. **Makes TRUE:** every ABN agent is risk-classified + EU-AI-Act-documented at the moment it is signed — enforced + proven at creation, not generated on-demand for an auditor. **Safety Spine (Fas 1+2+3) + Compliance MUST = done.** NEXT per the V1 plan: re-check, then **NoPayloadProof MUST-subset** (schema scan + runtime payload-policy + LLM-gateway redaction proof — make No-Data demonstrable). Deferred 🟡: full data-class taxonomy, RoPA/DPIA-at-runtime artifacts. OUT (🔴): Legal Intelligence Kernel (auto-fetch/interpret/file law). Hold at CLEAN — do NOT auto-merge.
  - **Customer-Surface Sync §9 candidate — CLOSED in S2F1-4.** The retry "max 2 attempts / 120 s timeout" copy was target-as-live; S2F1-4 made retry REAL for safe steps (up to 2 + per-step timeout) and reworded the truth in the `runner.py` docstrings + `landing/app/autonomous-engine/page.tsx` + `landing/components/OperaSection.tsx` (safe read/analysis steps retry; external writes are never auto-retried — human-gated). Design untouched, no overstatement.
  - **[74 DONE — PR open] P1(b) Build 1: Critic + End-State Evaluator on the live path.** Wired Dual-Brain Critic + ESE into `run()` as a fail-CLOSED pre-delivery verify stage — CALLED the functions, did NOT flip to the AAEA executor (avoids double-run / run_id collision / dashboard breakage). `_phase_act`→build-only; new `_phase_verify` + `_phase_deliver`. Crux: the critic's POSSIBLE_NAME heuristic false-positives on ABN's own bi-capitalised prose → categorise issues (strong-PII/contradiction/policy/unknown = fail-CLOSED block; POSSIBLE_NAME + confidence-vs-tier = soft flag). Verdict→honest status (extends 73): block→failed (not shipped), ESE flag 0.50-0.70→delivered+needs_review, ≥0.70→delivered; empty-but-correct still ≥0.50→success. No-Data (abstract-only LLM critic). No schema. Tests +12 (`test_verify_onpath.py`), suite 1701→1713.
  - **[75 DONE — PR open] MND: ABN Failure Taxonomy v1.** `core/failure_taxonomy.py` — single-source `FailureClass` (27 canon classes) + `FailureSpec` registry with the six fields (severity/retryable/requires_human/auto_pause_weight/safe_to_show/safe_to_feedback) + truthful reference-map (`maps_to` real token or None + `emitted_by`). API get()/iter_all()/all_classes()/is_known(). CATALOG ONLY — no emitter rewiring, no live-path change, no schema; weights are calibratable placeholders. The shared failure vocabulary RAL/AgentHealth/Safe Mode/Supervisor/RunStatusReducer will read. Tests +20 (`test_failure_taxonomy.py`), suite 1713→1733.
  - **[76a0i DONE — PR open] real finding-production (context-threading + permanent sim-guard).** Two Discoveries found the live path produced 0 real findings (fresh-context-per-step isolation in `_run_capability`) + `read_*` simmed unconditionally (no adapter wired). Fixed: P1 thread ONE shared context (read→analyze→report collaborate) + read findings once (no double-count); P2 PERMANENT sim-guard (`capabilities/_source_guard`, single source) — no adapter + `allow_simulated_findings` False (prod default) → genuinely empty + `needs_connector` (never fabricated); adapter error + sim off → truthful failure; P3 `needs_connector` reason (observe_no_data), truthful empty success. Flags: `enable_capability_context_threading`=True, `allow_simulated_findings`=False. PERMANENT RULE: no fabricated/simulated finding on any prod/customer surface. P4 (PII-on-findings) deferred to 76a. Tests +10 (`test_finding_production.py`), suite 1733→1743. **Real adapter injection into OPERA is itself target (Spår 3 DNA) — prod runs are truthfully empty until a connector is wired; sim is dev-only behind the flag.**
  - **[76a DONE — PR open] RAL finding-shape + INFORMATIONAL attestation on the live path.** RAL `field_attestor`/`cross_reference_checker`/`attestation_report` were built but NOT on the live path; `Finding.attested` was structurally False (no writer; `findings_persistence.py:136` reads `raw.get("attested", False)`) → `attestation_rate_pct` ~0 for ALL agents. 76a (INFORMATIONAL — no verdict change, enforcement is 76b): (a) capabilities emit critical-field VALUES + the natural key (icke-PII-shaped so the Critic can't false-block) + logistics `source_a`/`source_b` for cross-ref; (b) **explicit domain→FIELD_MAPS resolver** `resolve_domain_field_map` (carrier_billing/project_billing→invoicing, payroll/clinical_scheduling→scheduling, sales_pipeline→None, invoicing/scheduling/logistics/delivery→themselves; unknown→None truthful); (c) `attest_and_stamp` + `cross_reference` + `build_attestation_report` run informational in `_phase_verify` via new `_attest_informational` helper (source_records surfaced `_phase_execute`→`run()`→`_phase_verify`) + **write `Finding.attested` back onto each finding** so the metric reflects reality; fail-safe (any error → un-attested, never failed), No-Data (local compare; report=HMAC metadata). **P4 (PII-on-findings) SPLIT OUT to 76a_pii** (sim values are non-PII so no Critic false-block before P4). Tests +20 (`test_ral_attestation.py`), suite 1743→1763. Then **76a_pii** (P4) → **76b** RAL min()-fold ENFORCEMENT (hard-block when `blueprint.ral_required_fields` non-empty + attestation fails) → **P2a** conformance floor + VariantConformanceMap → **P2b** SimulationGate.
  - **[76a-pii DONE — PR open] Critic attested-fields PII reconciliation.** 76a put critical VALUES on findings; once a REAL adapter feeds a real invoice, a legitimate 16-digit `invoice_number` (≡ card shape) trips the Batch-74 Critic `CARD_NUMBER` regex on a LEGIT source-attested field → delivery WRONGLY blocks. Fix while latent. Discovery: the Critic runs BEFORE `_attest_informational` set `attested` → naive flag-read never fires live; chose the HYBRID (G5 i+ii): pre-critic STAMP + scan-reads-flag. Single-source deny-by-default predicate `field_attestor.is_attested_critical_value(field,value,finding,*,domain)` — not-PII ONLY when (a) declared FIELD_MAPS critical for the resolved domain, (b) `finding["attested"] is True` (strict), (c) shape-correct (`_value_shape_ok`: numeric→number; identifier→compact token `^[A-Za-z0-9][\w./:+\-]{0,63}$`, no whitespace/@). `_check_pii(output, blueprint)` key/container-aware walk skips a node only when the predicate holds; `runner._stamp_attested_fields` hoists `attest_and_stamp` (stamp-only) before the critic; `_attest_informational` still persists post-verdict (idempotent). **GENUINE-PII BLOCK UNCHANGED (G2)**: real card/personnummer/email in FREE TEXT still absolute-blocks; blueprint=None/unresolved domain → byte-identical Batch 74; RULE 3 + absolute-block path untouched. No-Data (metadata-only decision). No schema. Tests +20 (`test_critic_attested_fields.py`: T1 legit-not-blocked+control, T2 genuine-PII-still-blocked [core safety], T3 smuggled-unattested, T4 wrong-shape, T5 unknown-field, T6 fail-closed, T7 73/74/76a0i/76a intact, T8 No-Data), suite 1763→1783. **NEXT: 76b** = RAL/cross-ref min()-fold ENFORCEMENT (+ VerificationRequirement Matrix). Hold at CLEAN — do NOT auto-merge.
  - **[76b DONE — PR open] RAL min()-fold ENFORCEMENT (RAL track COMPLETE).** "An agent whose policy REQUIRES proof may not deliver unproven output." When `blueprint.ral_required_fields` is NON-EMPTY, a finding whose required fields are unattested (mismatch OR missing) BLOCKS delivery — folded into the `_phase_verify` verdict via min() (runs after Critic+ESE pass, before the 76a informational persist; can only pull DOWN → a high Critic/ESE score can't override a failed REQUIRED attestation). Empty ral_required_fields → byte-identical Batch 74 (the key guard). Honest status=failed + Batch-75 `verify_attestation_missing` reason. New single-source `agent_runtime/verification_requirement.py` (`verification_requirement(blueprint) → RequirementDecision`; enforced⇔non-empty required; reuses the AVM-injected set, no re-listing; documented per-class policy + `verification_class`). `AgentBlueprint.from_dict` carries `ral_required_fields` onto the runtime object (NOT in to_dict/sign → canonical signature unchanged). `runner._required_attestation_block_reason` + `_finding_required_attested` (fail-closed: attested is True AND every required field present). FAIL-CLOSED for enforced agents (check error→block); fail-safe for informational (requirement unreadable→no block, G7). Taxonomy `VERIFY_ATTESTATION_MISSING` now LIVE (maps_to "failed", severity HIGH). No-Data (metadata-only); no schema. Tests +14 (`test_ral_enforcement.py`: T1 required+attested delivers, T2 mismatch blocks, T3 missing blocks fail-closed, T4 min()-override, T5 empty byte-identical, T6 matrix, T7 fail-closed/fail-safe, T8 73/74/76a* intact, T9 No-Data), suite 1783→1797. **RAL track COMPLETE. NEXT: P2a** hard conformance floor + VariantConformanceMap. Hold at CLEAN — do NOT auto-merge.
  - **[P2a DONE — PR open] hard conformance floor on the create-path.** `calculate_quality_score` (`process_graph/statistics.py:66-77`) blended fitness at only 25% → high-volume + fitness=0.0 reached ~0.72-0.75 → crossed VERIFIED 0.70 → an acting agent from a graph we don't model. Fixed with a HARD floor (cap, not blend): `CONFORMANCE_FLOOR=0.65` + an OPTIONAL `conformance` kwarg in `decide_agent_need` (the de-facto GraphAdmissibility gate) — conformance < floor → capped at report-only/observed, NEVER acting, regardless of quality/ROI/confidence (no compensation, fail-CLOSED, evaluated first). Default None → byte-identical (every caller/test safe). `runner.py` passes `conformance=ensemble.fitness_score` + stamps it into stats JSON (audit). Honest `low_conformance` reason. No-Data, no schema. Tests +9 (`test_conformance_floor.py`), suite 1797→1806. VariantConformanceMap + KL-divergence DEFERRED to Spår-3. **NEXT: P2b** (5-level SimulationGate — the LAST Spår-1 batch). Hold at CLEAN — do NOT auto-merge.
  - **P2(b): dry-run-before-deploy.** `simulate_run` exists but is never called in `generate_blueprint` (confirmed by reading the full create path).
  - **Then: the big DNA/Agent-OS Discovery map (areas A–G)** — DNA/Observer-modes/Event-Envelope/case-key/no-loss; 6 graph levels/GraphConfidence/Work-Units/Genesis Package; BrainProfile/ToolFit/Verification/Simulation compiler; runtime supervisor/Trust-Kernel/Coordinator/Jurisdiction; local Control-Plane SQLite registers/Health; Evolution/Shadow-mode/Kill-switch/invariants; Tauri control room — one area at a time, Discovery before build.

## CHANGES
Operational-config change log (newest first).
- Batch 75b (test-hygiene, TEST-ONLY) — froze the clock in the 6 calendar-flaky tests: test_anomaly_trend.py::{direction_improving, alert_triggered} + test_mind_agent.py::{report_generated_correctly, suggestion_rollback_spike, suggestion_low_attestation, suggestions_are_swedish}. Added `freezegun==1.5.5` to backend/requirements.txt + a shared `tests/conftest.py::frozen_clock` fixture pinning `now` to 2026-06-17 (a Wednesday, mid-month); both modules opt in via `pytestmark = pytest.mark.usefixtures("frozen_clock")`. Root cause: the tests seed rows at `now - N weeks/days` and assert they fall inside a window the product computes from its own `datetime.utcnow()` read — on a Monday/month-start the seeded `now - 1 day` row drops into the previous ISO week. Product code (Batch 28 anomaly trend, Batch 30 Mind agent) is correct; only the tests were time-fragile. Proven both ways under a simulated ambient clock of 2026-06-01 (Monday + month-start = the real boundary that broke CI today): UNFIXED → exactly those 6 fail, 11 pass; FIXED → all 17 pass. `git diff main..HEAD` on backend/{agents,core,services,api} is empty — no product code touched; assertions unchanged. NOTE: the real backend count is 1535 (not 1518); the change is count-neutral (1535 collected with and without it). PR opened (do NOT auto-merge — Jacob merges this first to unblock PR #27's Backend gate).
- Batch 73 GITHUB_TOKEN chaining gap — RESOLVED. Jacob created a fine-grained PAT (Contents: read/write on abn-systems/ABN) and stored it as repo secret `RELEASE_PAT`. `auto-tag.yml`'s release step now authenticates `gh release create` with `${{ secrets.RELEASE_PAT }}` (no GITHUB_TOKEN fallback — a missing PAT must fail loudly, never silently create a Release that cannot chain the installer build). The full chain (tauri.conf.json version bump → tag + Release → build-release.yml installer build) is now automatic end-to-end. PR #25.
- guardrails upgrade applied — CLAUDE.md §4.1 continuous-save / §4.2 pre-/clear HALT protocol / §4.3 self-identity anchor; .claude/settings.json deny[] (15 durable-artifact entries) + PreToolUse Bash hook; .claude/hooks/guard.sh (extended BLOCK pattern). Guard verified: 4 BLOCKED (exit 2), 2 passed (exit 0). Config-only, no backend/services/tests/frontend/landing touched, no version bump, no ci.yml change.
